<!-- SPDX-License-Identifier: EPL-2.0 -->

# 2471 — oracle-bug family comparison (2026-07-02, post-fixes)

Input: `~/git/graphviz/tests/2471.dot` · corpus id `2471` · oracle = headless
native dot (`GVBINDIR=/tmp/ghl`, estimate metrics) · port = dot-engine at
`chore/verify-oracle-bug-family`.

> **Visual side-by-side:** [`2471.html`](2471.html) · golden:
> [`2471-golden.svg`](2471-golden.svg) · ours: [`2471-ours.svg`](2471-ours.svg)

## Verdict

**structural-match, maxΔ 9063.3 (numeric only — identical element tree)**

| | native C oracle | dot-engine |
|---|---|---|
| stderr / exit | exit 1; `trouble in init_rank` + 6× `triangulation failed`/`Pshortestpath failed`; loses 6 edges (n204→n32, n207→n33, n207→n42, n231→n3, n232→n3, n288→n30) | exit 0; 6× `triangulation failed` + 6× `Pshortestpath failed`; loses the SAME 6 edges |
| edges rendered | 1200 / 1200 | — |

## Upstream status

OPEN, `xfail(strict=True)` (tests/test_regression.py:4359); the worst init_rank instance; cited by the #2796 reporter

## Inputs verification (right-for-the-right-reason)

Ranking inputs line-identical (253 rank2 calls). x-aux inputs line-identical after fix f0e28a6 (rec_reset_vlists). Residual numeric deltas are NS behavior from C's infeasible/cyclic recovery state — deliberately not chased (no recovery-state porting).

## Disposition

Accepted A4: port follows C's acknowledged-broken outcome from verified-identical inputs. Re-measure when upstream fixes #2471.
