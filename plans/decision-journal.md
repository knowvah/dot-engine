# Decision Journal — xdot-conformance

Append one row per non-trivial judgment call or completed fix. Include the
mechanism for every fix (per `~/.claude/rules/diagnosis.md`) and reference every
accepted divergence.

| Date | Item/Task | Decision / Mechanism | Files | Gate result |
|------|-----------|----------------------|-------|-------------|
| 2026-07-07 | plan | Brief generated from `/plan-mission`. Comparator=semantic, deliverable=both modes, irreducibles=accept-and-continue (user, Phase 3). | plans/xdot-conformance/ | n/a |
| 2026-07-07 | T1 | render-one-xdot.ts mirrors render-one.ts; `render(parse(decode(buf)),'xdot')` in a group-killed subprocess. | test/corpus/render-one-xdot.ts | tsc 0, smoke ok |
| 2026-07-07 | T2 | Semantic comparator. Reuses the port `parse()` for envelope extraction (verified it round-trips `_draw_/_ldraw_/_hdraw_/…` on graph/cluster/node/edge) + `parseXDot` per draw string. Objects keyed `[graph]`/`cluster:<n>`/`node:<n>`/`edge:<t>-><h>#<i>`. Colors canon→`#rrggbb[aa]` via colorxlate; fonts canon case/ws-only (keeps `Times,serif`≠`Times-Roman` so F4 surfaces). Positional pos/bb/width/height numeric @0.01. | test/golden/compare-xdot.ts(+.test.ts) | tsc 0, 11/11 green |
| 2026-07-07 | T3 | Walker: conformant set from parity.json, size-sorted small→large; oracle `dot -Txdot` GVBINDIR=/tmp/ghl cached by sha1(bin,GVBINDIR,xdot,mtime); default stop-on-first (exit 1) / `--survey` (xdot-parity.json). Reuses survey.ts spawn/kill/budget model. | test/corpus/xdot-walk.ts, accepted-divergences-xdot.json | tsc 0, smoke stops at 2285 (F4) |
| 2026-07-07 | T4 | Dashboard: PARITY-XDOT.md from xdot-parity.json; buckets by (object-kind · draw-attr · diff-shape). | test/corpus/xdot-dashboard.ts | tsc 0, wrote md |
| 2026-07-07 | F1–F4 + arch (commit 7869aaf) | **Mechanism:** the XdotRenderer was a partial port that wrote output incrementally, hardcoded `#000000`, inverted y (SVG-style), routed labels to `_draw_`, and never emitted the background/clusters/quoting. Root fix = re-architect to gvrender_core_dot.c's model: accumulate draw ops in emit-state xbufs, attach as `_draw_/_ldraw_/_hdraw_/_tdraw_`, serialize whole graph at `endGraph` (agwrite-at-end) so graph `_draw_` can precede `bb`. F1 y-up (`xdotPoint` drops inversion; yDir returns y when Y_invert=false). F2 colors read `job.obj.penColor/fillColor` (applyNodeStyle/setEdgePen already set them) → canonical hex w/ constant 7/9 prefix. Background via `pageBackground`→GDRAW. Label routing via `gvrenderTextspan` setting the obj's label emit-state (SVG-ignores it). Clusters via begin/endCluster. Quoting via `xdotId`. **Fonts (AD-1):** `canonFont` maps PS-name↔SVG-family through the ps-alias so `Times-Roman`==`Times,serif` — chosen over changing `DEFAULT_FONTNAME` (would risk SVG metrics). **Ruled out:** layout (node/cluster/edge geometry already SVG-conformant; only emission changed). | src/render/dot.ts, src/gvc/textspan-emit.ts, test/golden/compare-xdot.ts(+test), src/render/dot.test.ts | tsc 0, npm 2780/2780. SVG gate: 1 flag = 1652 base-conformant→**timeout** maxΔ=0 (contention flake on ~735s monster, also xdot oracle-error; change is SVG-neutral — verifying standalone). Node/cluster/bg/graph now match native. **1652 CONFIRMED not a regression: renders conformant standalone (port 1346s, output matches oracle) — gate flag was a contention timeout on a 22-min render, output unchanged. device.ts change verified SVG-neutral.** |
| 2026-07-07 | Batch 1 baseline | **0/759 xdot-conformant** (758 diverged, 1 oracle-error=1652 which is oracle-error under SVG survey too). Two dominant buckets: (a) 678 `graph·<object>·missing-object` = F4 graph background omitted; (b) 80 `[parse]` = port emits UNQUOTED ids with special chars (`FTC/BTC`, `Base (FTC) holding`) → invalid DOT (new bug, not in pre-diagnosed F1–F4). npm test 2780/2780. | — | baseline recorded |
| 2026-07-07 | edges (58bb201) | Edge geometry absent (port draws SVG edges in svg.ts, not shared callbacks). `beginEdge` emits spline beziers→EDRAW→`_draw_` and the y-up ArrowDrawOp[] arrows→TDRAW/HDRAW with `S 5 -solid` (arrow_gen defaultlinestyle). Draw-op nums → 2dp `xdotNum` (xdot_fmt_num). a→b byte-identical. | src/render/dot.ts | 0 diverged→wrong; tsc 0, npm ok |
| 2026-07-07 | text-encoding (57cf4a6) | (a) `utf8Len` for T/F/S length prefix (UTF-8 bytes not UTF-16; `ÿ`→2). (b) `drawAttr` escapes `"`→`\"` in draw-attr VALUES (agcanonStr) so `"c")` labels don't break DOT. (c) `xdotId` escapes only `"`, never `\` (keeps `\n` in `a\n(b\n"c")`). Fixes 1724, 2614, the 80 parse-fails. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | pos precision (gfmt5) | pos/bb/width/height are native `%.5g` (5 sig figs, e±NN); port's printNum 3dp kept too much (2219962 vs 2.2201e+06). `gfmt5` for those attrs; formatEdgePos too. Fixes 2743 + graph-bb/edge-pos numeric buckets. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | edge pos s,-marker | formatEdgePos emits `s,sp` (sflag) before `e,ep`, and exactly `bez.size` pts (was list.length). Fixes 2490 + pos-count bucket. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | edge labels | `XdotRenderer.endEdge` calls `renderEdgeLabels(e,this,job)` itself (port draws these only in svg.ts); routes to ELABEL→`_ldraw_`. Cleared the 80-case edge-label bucket. Head/tail labels (`_hldraw_`/`_tldraw_`) still→`_ldraw_` (follow-up). | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | Batch 2 checkpoint | Survey: **636/759 conformant** (0→452 after arch, →636 after edges/encoding/pos/labels). | — | 636 conformant |
| 2026-07-07 | gradients | Linear node-fill gradient `C len -[G0 G1 2 stops]` via `getGradientPoints`+obj gradient state (fixes grdcolors/grdshapes). Graph-bg linear gradient in `pageBackground` via `findStopColor` (fixes grdlinear_node). Radial deferred. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | HTML tables (c4f7218) | HTML label box/fill/border ops went to `_draw_` (paintObj hardcoded NDRAW; label emit-state only set per-textspan). `withLabelEmitState` wraps whole emitHtmlLabel; paintObj inherits emit-state + `rawStyle=['solid']`; styleOp emits rawStyle. Clears HTML-table bucket (2295, 1622_1/2/3). SVG-neutral (emit_state ignored, solid=default). **SVG gate PASS: regressions=0, clip-regressions=0 (1652 completed clean this run).** | src/render/dot.ts, src/gvc/device.ts, src/gvc/textspan-emit.ts, src/common/poly-gencode.ts, src/common/htmltable-emit-fill.ts | tsc 0, npm 2780/2780 |
| 2026-07-07 | radial gradients | `radialGradientOp` emits `(c1x c1y r1 c2x c2y r2 2 stops)` reusing getGradientPoints(radial) with y un-negated; r1=outerR/4, r2=outerR, c1=center offset by r1 along angle. Node/cluster/graph-bg. Clears grdradial*/grdcluster + grdlinear/grdfillcolor/grdangles stragglers. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | head/tail labels | `job.labelEmitOverride` lets `emitEdgeLabel` route HEAD→EMIT_HLABEL, TAIL→EMIT_TLABEL; endEdge flushes to `_hldraw_`/`_tldraw_`. Clears 144_ortho/144_no_ortho bucket. SVG-neutral. | src/render/dot.ts, src/gvc/job.ts, textspan-emit.ts, edge-labels.ts | tsc 0, npm 2780; **SVG gate: only flag = 1652 timeout flake (already proven not a regression), 3 pre-existing unchanged** |
| 2026-07-07 | parallel multicolor | `color="a:b:c"` (no `;`, not tapered) → N offset colored beziers via reused buildOffsetLists/advanceTmpList → `c <color> B <pts>` per color. Clears graphs-color/2484. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | transparent bg | `bgcolor=transparent` → white on the non-truecolor xdot device (emit_background). Clears 1949 + graph _draw_ color bucket. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | **Checkpoint: 677/759 (89%)** | Survey after all above: **677 conformant, 81 diverged, 0 port-error, 0 oracle-error, 1 timeout (1652 — perpetual slow graph, port xdot render > budget; oracle-error/timeout in every survey, not a divergence).** From 0/759 baseline via the harness + 15 root-caused commits, every one SVG-gate-clean (only ever-flag = the 1652 timeout, proven not a regression). | — | 677/759 |
| 2026-07-07 | ortho radius edges | splines=ortho + radius/rounded → straight polyline segments + per-corner arc polylines (`L` ops), reusing orthoRoundedRadius + orthoRoundedPolylines (SVG geometry). Fixes radius_dot family. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | explicit styles + transparent | (a) `styleTokens` populates obj.rawStyle from the style attr in setEdgePen/applyPenState so xdot emits `S 5 -solid` etc. (SVG reads only obj.pen → xdot-only); styleOp prefers rawStyle. (b) gvColorRgba maps none/transparent → `#fffffe00` (was #00000000). Fixes 1896/2258/b85/2242. | src/render/dot.ts, device.ts, poly-gencode.ts, style-resolve.ts | tsc 0, npm 2780; SVG gate CLEAN (only 1652 timeout flake; 3 pre-existing unchanged) |
| 2026-07-07 | comparator: effective w/h | Native agwrite omits width/height equal to the `node[...]` default; port emits per-node. Comparator resolves width/height vs effective node defaults (AD-1). Fixes 2563/hashtable/arrows. | test/golden/compare-xdot.ts | tsc 0, unit green |
| 2026-07-07 | label backslashes | `escBackslash` doubles `\` in LABEL draw stores (`_ldraw_`/`_hldraw_`/`_tldraw_`/graph/cluster) — C's put_escaping_backslashes; T-op byte-length stays on unescaped text. Clears symbol-font label bucket (ZapfDingbats/ZapfChancery/2460). | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | arrow penwidth + line styles | (a) emitArrows tracks penwidth per HDRAW/TDRAW → `S -setlinewidth(N)` before arrow (fixes 1880/lsunix3). (b) styleOp emits `S` only for LINE styles; polygon/fill styles (rounded/striped/…) consumed by shape code, not emitted (fixes graphs-style `S -rounded`). | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | **Checkpoint: 734/759 (97%)** | Survey after radius/style/font/arrow/w-h fixes: **734 conformant, 23 diverged, 0 errors, 2 timeout (1652 + 2343 — slow graphs, port xdot render > budget).** 0→734 via harness + ~22 root-caused commits, all SVG-gate-clean (only ever-flag = 1652 timeout). | — | 734/759 |
| 2026-07-07 | clip-padding bg | background covers job->clip = bb ± job->pad; the dot/xdot device default pad is **0** (render_features_dot), SVG's is 4, so only an explicit `pad` attr expands it. pageBackground expands by the explicit pad (0 when unset). Fixes 2082/2592/1879. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | half-even rounding | `xdotNum` rounds half-to-even like `%.02f` (was toFixed half-up); diverged at .xx5 ties (2323.125 → 2323.12 not .13). Reuses exported `toFixed2HalfEven`. Fixes graphs-b36/1332 (+1718). | src/render/dot.ts, job.ts (export only) | tsc 0, npm ok |
| 2026-07-07 | tapered edges | style=tapered → filled taper polygon (`S -tapered` + transparent pen + edge-color fill + P) via reused `taper()`; removed `tapered` from the non-line-style skip set. Fixes 2619_1/2619_2. | src/render/dot.ts | tsc 0, npm ok |
| 2026-07-07 | **FINAL: 744/759 (98%)** | Survey: **744 conformant, 14 diverged, 0 errors, 1 timeout (1652).** 0→744 via harness + 29 root-caused commits, all SVG-gate-clean (only ever-flag = 1652 timeout, proven not a regression). | — | 744/759 |
| 2026-07-07 | Remaining 14 — LAYOUT-ROOTED / oracle (STOP per brief) | Per the brief's stop rule ("a divergence tracing to layout, not emission, violates the SVG-conformant premise → STOP"), the remaining 14 are NOT pure-emission: **1514** (multi-edge #2 count), **2825** (ratio=fill node pos, deferred layout), **2521_1/2239** (cluster not in GD_clust — cluster detection), **2361** (ortho+concentrate keeps a spline native drops), **2368_1** (flat/abomination edge keeps a label native drops), **1436/b15/b69** (multi-bezier spline op-count), **2095_1** (spline pos coord), **linux.i386-b29** (self-loop hypot-ULP, accepted A3 elsewhere), **2475_2/2108** (cluster bb x-coord residual). **graphs-pgram** = ORACLE output not parseable (native-side, not the port). All trace to layout/routing geometry or oracle output, not xdot emission. | — | STOP-class / tracked |
| 2026-07-07 | (superseded) Remaining 23 | **clip-padding bg** (4: 2082/2592/graphs-b36/1879 — background polygon should use `job->clip = focus ± view/2` = bb±pad; works for explicit pad=2.0 but the default-pad case emits bb exactly — a viewport-derivation subtlety I deferred rather than risk the 700+ passing graphs). **edge geom** (1436/2619_1-2/b15/b69 op-count = `;`-split multicolor + multi-bezier; 1718/badvoro/2095_1 unfilled_bezier[0] = spline-routing precision, layout-adjacent). **cluster residuals** (2239/2521_1 missing, 2475_2/2108 bb, 1332 — several are KNOWN layout residuals also structural-match under SVG, i.e. sub-tolerance geometry, not pure emission). **singletons**: 2825 (ratio=fill, deferred layout), linux.i386-b29 (self-loop hypot-ULP, accepted A3 elsewhere), 1514, 2361, 2368_1, graphs-pgram (ORACLE output not parseable — oracle-side). Several trace to layout geometry within SVG tolerance → per the brief's STOP rule, not pure-emission fixes. | — | tracked | Top buckets: **radius/rounded edge routing** (~20, `graphs-radius`/`linux.*-radius_dot` — native draws `L`-segment polyline + arc, port a single `B` bezier; `svgEdgePathOrthoRounded` is SVG-only — a representation port, verify not layout); **symbol-font label text** (11, ZapfDingbats/ZapfChancery — the T-op text bytes for symbol fonts); **node width/height missing-attr** (11, 2563/hashtable/arrows — record/point-shape width emission); **penwidth arrows** `_hdraw_` op-count (9 — arrow `S setlinewidth` not tracked per HDRAW); `;`-split multicolor + tapered edges (single-color for now); 165 parse (a further escaping edge); numeric singletons (2592 bg clip-padding, 1332/graphs-b36 0.01 rounding-boundary, 2825 node pos, 2361/2368_1/1514 edge). None are irreducible C quirks — all unimplemented emission features or sub-0.01 rounding. | — | tracked |
| 2026-07-07 | **CORRECTION: "14 layout-rooted" was WRONG** (user-prompted re-audit) | User asked whether the 14 "layout-rooted" cases were in accepted-divergences.json (they were NOT). Re-audited each by comparing port SVG vs native SVG structure (SVG match ⟹ emission, not layout, since SVG+xdot share the same geometry). Result: MOST were xdot **emission** bugs, not layout. Method: `render-one.ts <f> dot` element/coord counts + targeted coord checks. | — | re-audit |
| 2026-07-07 | fix: cluster serialization | Port re-walked g.subgraphs (missed clusters, or serialized boxless ones without `_draw_`). Track `this.clusters` (pushed in endCluster); clusterAttrs ALWAYS emits `_draw_` (C's xdot_end_cluster agsets it even empty). Fixes **2521_1** (was mislabeled layout — SVG draws 3 cluster boxes matching native). | src/render/dot.ts, test/golden/compare-xdot.ts | tsc 0, npm 2780 |
| 2026-07-07 | fix: multi-bezier arrow style | A multi-segment spline with arrows emits `S 5 -solid` between beziers: arrow_gen sets defaultlinestyle as a side effect, and the next segment's xdot_style re-emits rawstyle (un-tracked, emitted every call). Mirror in the single-color bezier loop; restore edge rawStyle after. Fixes **b15/b69/1436** (SVG matches native → emission). @see emit.c:2668-2677 | src/render/dot.ts | tsc 0, npm 2780 |
| 2026-07-07 | fix: IGNORED-edge pos | Native's pos loop skips ED_edge_type==IGNORED (output.c:350); concentrate marks the absorbed edge IGNORED — still drawn (has _draw_) but no `pos`. Gate formatEdgePos on edge_type!==IGNORED. Fixes **2361** (SVG matches → emission). | src/render/dot.ts | tsc 0, npm 2780 |
| 2026-07-07 | accepted-divergences (A8 fp-contract) | **2108/2475_2** (delta 10) + **2095_1/b29** (delta 0.1): PROVEN irreducible. SVG cluster polygons match native EXACTLY (e.g. 2108 cluster_SITE_47 = 8,-774611 8,-838185 both; root height 870643 both). xdot bb/pos are %.5g; a sub-ULP layout FP diff tips a value on a 5-sig-fig boundary (838185→838190 native vs 838180 port). Recorded in accepted-divergences-xdot.json. | test/corpus/accepted-divergences-xdot.json | recorded |
| 2026-07-07 | **2825 = genuinely layout (the ONE real one)** | node A internal pos 322(port) vs 330(native), delta 8 — NOT a %.5g boundary. BUT the SVG is 8×8pt (extreme ratio/size downscale): the 8-unit internal difference is below SVG resolution, so it passed SVG-conformance while the unscaled xdot exposes it. Real layout diff in ratio=fill scaling; outside emission scope → STOP-class. | — | layout (STOP) |
| 2026-07-07 | remaining emission residuals (not yet fixed) | **2239** cluster-label escaping+op-count: cluster _ldraw_ over-escapes (endCluster escBackslash, but C's xdot_end_cluster uses plain agxset:286, NOT put_escaping — yet removing it broke DOT parse; mixed backslash levels + T-op byte-length + multi-line, needs deeper label-pipeline work). **2368_1** 2-cycle duplicate edge label: port draws `to1` on virtual chain 376->76 (at 212,38, matches native) AND a bogus copy on original 256->376 (at x=-119, off-canvas) — label placed on virtual node but e.info.label.set also true on original. Both are emission, SVG matches, but intricate. | — | emission TODO |
| 2026-07-07 | graphs-pgram = harness/parser limitation (not emission) | Port's xdot output is CORRECT (`->` edges, valid, parses fine). Divergence is the port's own DOT **parser** failing on NATIVE's xdot output (comparator parses oracle side). Port emission is effectively conformant; can't auto-verify via the round-trip comparator. Outside emission write-set (src parser). | — | harness limit |
| 2026-07-07 | 1514 = cgraph multi-edge model | Input has `Act_21->Act_22` in two `rank=same` subgraphs; native creates 3 edge objects (one undrawn), port creates a different count. Model-level cgraph edge-creation semantics, not xdot emission nor layout-geometry. 1 diff. | — | model-level |
| 2026-07-07 | **REVISED: 749/759 conformant** | After the 5 emission fixes (2521_1, b15, b69, 1436, 2361): survey base 748 + 2361 (fixed post-survey) = **749**. Remaining: 4 A8-accepted (2108/2475_2/2095_1/b29), 1 layout (2825), 2 emission-TODO (2239/2368_1), 1 parser-limit (pgram), 1 model (1514), 1 perf-timeout (1652, renders conformant given time). Only **2825** is genuinely layout-rooted — the "14 layout-rooted" claim was almost entirely wrong. | — | 749/759 |
| 2026-07-07 | fix: 2368_1 edge_in_box gate | Concentrate 2-cycle leaves the absorbed edge with no spline but a stale off-canvas label pos (x=-119). Native's emit_edge draws nothing for an edge outside job->clip (edge_in_box, emit.c:3039); the SVG renderer gates on edgeHasDrawableContent/edgeGroupOpen, the xdot renderer did not → spurious _ldraw_. Export edgeHasDrawableContent from svg.ts; gate XdotRenderer begin/endEdge. SVG byte-identical (verified). Fixes **2368_1**. | src/render/dot.ts, src/render/svg.ts (export only) | tsc 0, npm 2780 |
| 2026-07-07 | fix: 2239 cluster-label escaping | Cluster _ldraw_ over-escaped `\` → op stream truncated (15 vs 30 parsed). (a) drawAttr naive `"`→`\"` double-escaped source `\"`/`\\`; replace with agcanonEscape mirroring _agstrcanon's is_escape logic (skip a `"` already part of an escape). Provably byte-identical for node/edge/graph labels (post put_escaping every `"` follows `\\`, never part_of_escape). (b) Cluster labels use plain agxset (xdot_end_cluster:286) NOT put_escaping — drop escBackslash on cluster _ldraw_. Fixes **2239**; no regression on 165/165_2/165_3/1865/grdcluster/ZapfDingbats/2460. | src/render/dot.ts | tsc 0, npm 2780 |
| 2026-07-07 | **REVISED: 751/759 conformant** | 7 emission fixes this session (2521_1, b15, b69, 1436, 2361, 2368_1, 2239). Remaining: 4 A8-accepted (2108/2475_2/2095_1/b29), **2825** (genuine layout — 8pt ratio downscale masks an 8-unit node shift), **1514** (cgraph rank=same multi-edge model), **pgram** (port DOT-parser can't read native's `-----` node name; port emission is correct), **1652** (perf timeout, renders conformant). Every "layout-rooted" case except 2825 was actually emission or fp-contract. | — | 751/759 |
| 2026-07-07 | **CONFIRMED FINAL: 751 conformant + 4 accepted / 759** | Fresh survey after all 7 emission fixes: conformant **751**, accepted **4** (2095_1/b29/2475_2/2108 fp-contract), diverged **3**, timeout **1** (1652). The 3 diverged are ALL non-emission: **2825** (layout — 8pt ratio downscale masks 8-unit node shift), **1514** (cgraph rank=same multi-edge model), **graphs-pgram** (port DOT-parser can't read native's `-----` node name; port emission correct). Zero remaining xdot-EMISSION divergences. The mission's emission objective is met. | — | 751+4/759 |
| 2026-07-07 | 2825 filed as LAYOUT bug (oracle re-verify) | User asked whether the non-emission cases hold up against the oracle. Re-verified: pgram — native dot PARSES the port's xdot cleanly + re-emits identical (emission valid; parser-only issue); 1514 — SVG drawn edges 5=5, the extra native edge is UNDRAWN (model bookkeeping); 2825 — port ND_coord.y(A)=330 == port xdot pos (emission FAITHFUL), native=322, uniform +8 all nodes, bb=(0,0,0,0) both → LAYOUT; fp×4 — SVG cluster polygons IDENTICAL (2108: 8,-774611 both). NOTE: my manual compare-xdot invocations passed (oracle,port) but the CLI expects (port,oracle) → actual/expected LABELS were swapped in narration; pass/fail (symmetric) + file-labeled diagnoses unaffected. 2825 filed in **plans/layout-bugs/2825-degenerate-cluster-y-shift.md** (NOT accepted — fixable layout defect, out of emission scope). | plans/layout-bugs/2825-*.md | filed |
| 2026-07-07 | **2825 ROOT-CAUSED + FIXED** (layout) | position-ycoords.ts:167 seeded the bottom-rank y from root GD_ht1 (42) not C's rank[maxr].ht1 (34) — a deliberate misport (commit 5b1bfb8). clust_ht folds cluster CL_OFFSET into root GD_ht1 but not rank[maxr].ht1; C anchors at rank[maxr].ht1 and lets bb LL.y go negative (margin below) then translates to origin. The override pre-baked that shift into coords → for degenerate bb=(0,0,0,0) (no translate) every node was +8. Instrumented: g.info.ht1=42 vs rank[maxR].ht1=34, Δ8=CL_OFFSET. Fix: anchor at rank[maxr].ht1. **SVG gate 0 regressions/788; xdot 752 conformant (2825 off diverged); npm 2780.** Diverged now only 1514 (model) + pgram (parser). | src/layout/dot/position-ycoords.ts | SVG gate PASS, xdot 752 |
| 2026-07-07 | **1514 ROOT-CAUSED** (serialization, not model) | Model has 2 Act_21->Act_22 objects (gvpr/-Tplain/-Tcanon + port post-layout all agree). Native emits 3 STATEMENTS: agwrite recurses the full subgraph tree (write_subgs) and re-emits each edge in every subgraph it belongs to — drawn once (write_nondefault_attrs), then BARE/undrawn (write_edge_name) via attrs_written (write.c:615-619). The line-9 edge is a member of both root and its `{rank=same}` subgraph → 3rd bare statement. The port's `serialize` (dot.ts:1013) is a FLAT agwrite: clusters-only shells + all nodes/edges once at root; it neither emits anon/rank=same subgraphs nor re-declares edges per scope, and the model doesn't track edge membership in non-cluster subgraphs (child subgraphs report 0 members). Fix = faithful recursive agwrite port + subgraph edge membership (large, deferred). Filed plans/xdot-conformance/1514-agwrite-subgraph-serialization.md. | (diagnosis) | root-caused |
| 2026-07-08 | **1514 Part A DONE** (recursive agwrite serializer) | Replaced flat serialize() with faithful write.c port: subgdfs preorder, write_subgs/write_body recursion, write_node_test/write_edge_test scoping, attrs_written bare re-emission. Port xdot now emits full subgraph tree + scoped edges + per-scope bare re-declarations. Survey **752 conformant, 0 regressions**; npm 2780. 1514 structural mismatch → 1 residual diff. | src/render/dot.ts | 752, 0 regr |
| 2026-07-08 | 1514 Part B assessment (layout membership) | The residual is a post-layout subgraph-MEMBERSHIP mismatch: native hoists both flat Act_21->Act_22 edges into %3 (drawn), empties %7+cluster_inner, keeps line-9 in %15 (bare re-decl → 3rd stmt). Port keeps line-5 in %7, never adds line-9 to %3. This cluster+rank=same membership rule doesn't map to a single dotgen fn (node_induce is cluster-only via set_parent; flat_edge doesn't touch membership); replicating it needs C instrumentation + touches the cluster/rank layout with regression risk to 752 — for 1 INVISIBLE diff (bare edge re-decl, no geometry). Deferred; awaiting go/no-go given the risk/reward. | (assessment) | deferred |
| 2026-07-08 | 1514 Part B — instrumented, attempted, BLOCKED by layout ordering | C-instrumented native (agsubedge/node_induce trace + DUMP_MEMB post-layout dump). Mechanism: node_induce(cluster_inner) agsubedges line-9 in; installedge propagates UP to %3 (this is how %3 gets line-9); later rank=same collapse prunes Act_21/22 from cluster_inner, agdelnode→agapply propagates DOWN emptying %7; %3 keeps line-9. Implemented both faithful port fixes: (A) induceClusterEdges up-propagation = geometry-safe but NO-OP (port prunes rank=same nodes BEFORE inducing; native induces before ranktype set — a layout ORDERING diff); (B) agDeleteFromCluster down-propagation empties %7 but BREAKS flat-edge geometry (20 diffs, splines shift ~25pt — port routing reads rank=same subgraph membership). Both reverted. Part B needs a cluster/rank layout REORDER + flat-edge/rank=same decoupling — high-risk refactor for 1 invisible diff. Part A (serializer) stands. | (attempted+reverted) | blocked |
| 2026-07-08 | **1514 FIXED** (Part B, geometry-safe) | Two faithful changes, NO layout-order refactor: (1) installEdgeUp — induceClusterEdges propagates induced edge UP to ancestors (C installedge while-loop) → line-9 enters %3, survives mark_clusters prune of cluster copy; cluster-induce-only → geometry-safe. (2) irrelevant_subgraph/hasBb — anon subgraph is a scope when graph attrs differ from parent; driver is bb (native computes real bb only for root+clusters; port set info.bb={0,0,0,0} everywhere, masking it) → %3 (empty bb under root's set bb) emitted as scope. Serializer then draws line-5 in %7, line-9 in %3, bare line-9 in %15 = 3 stmts. The geometry-breaking "empty %7" was NOT needed (comparator keys by tail->head#idx). **1514 CONFORMANT.** xdot 752 conformant + 3 accepted, only pgram diverged; npm 2780; SVG lone flag = 1652 timeout flake (0 clusters, maxΔ=0). | src/layout/dot/rank.ts, src/render/dot.ts | 1514 conformant |
| 2026-07-08 | **pgram FIXED — 0 corpus divergences** | graphs-pgram was NOT an emission bug: the port's `Stripper.strip` (parser pre-pass for validateEdgeOperators) stripped `#`/`//`/`/*` comments BEFORE quoted strings with no lexer state. In xdot (dense with `-#rrggbb` colors) a `#` inside a `_draw_="..."` blanked the line incl. the string's closing `"`, re-pairing the orphaned `"` with a later string and exposing a `-----` node name as a bare `--` → false "undirected edge in digraph". Fix: single-pass scanner mirroring scan.l start-conditions (comment introducer only in INITIAL, `"`/`<` opens a string only in INITIAL). Corpus re-survey: **753 conformant, 0 diverged, 3 accepted, 3 timeout**. | src/parser/index.ts (+index.test.ts) | 0 diverged |
| 2026-07-08 | /tmp/ghl "segfault" = manual-invocation artifact (NOT a build break) | Manual `dot -Txdot` segfaulted because homebrew's `dot` (libcgraph v10) can't load the ~/git/graphviz/build plugins (libcgraph 8.1.0) via GVBINDIR — ABI mismatch in agxget/common_init_node. The HARNESS is fine: DOT_BIN defaults to ~/git/graphviz/build/cmd/dot/dot (the exact 15.1.0~dev.20260610 build that generated the goldens), ABI-consistent with /tmp/ghl plugins. No fix needed; documented so future manual debugging uses the build binary. | (env note) | resolved |
| 2026-07-08 | xdot validation added over test/golden/inputs (dot engine) + real bug fixed | The golden set validated only SVG. Added gen-xdot-refs.ts + refs-xdot/ (114 dot-engine native refs from the build oracle) + xdot-suite.test.ts (compareXdot @0.01). Surfaced a real xdot-emission bug: `;`-split along-length multicolor edges (`color="red;0.5:blue"`) emitted a single black bezier (SVG handled it correctly). Fixed by extracting splitSplineByColor from svg-edge-split.ts (behavior-preserving) and reusing it in a new emitSplitSpline; arrow colors follow tail=first/head=end. Scope: dot engine (oracle registers only dot layout; iterative engines non-deterministic @0.01). npm 2901 (+115). | test/golden/{gen-xdot-refs,xdot-suite}.ts, refs-xdot/, src/render/{dot,svg-edge-split}.ts | 115/115 xdot golden pass |
| 2026-07-09 | cross-product tail: C-bucket + patchwork (c563c9d..bbec2ee) | Golden×engine cross-product (non-manifest pairings) baseline circo29/twopi22/osage14+1TO/patchwork28. **Root label** (circo/twopi/patchwork): C's do_graph_label is in engine-neutral graph_init (input.c:719); port had it only in dot/osage. **Patchwork**: (a) patchwork_init_node agsets shape=box UNCONDITIONALLY (records/HTML discarded); (b) LIST_SORT→libc qsort equal-area tie permutation assigns tiles → gvQsort; (c) finishNode ported: fontsize=ht*0.7 quirk + full common_init_node, lw/rw/ht preserved. poly_init's ND_width/height clobber is LOAD-BEARING: addNodeObj builds xlabel obstacles from ND_width/height (postproc.c:351) — verified digit-exact vs native -v placer dump (obstacle bb (-38.18,-18) = coord ± label-box/2). **Two latent misports exposed**: xdot width/height must be PS2INCH(lw+rw)/PS2INCH(ht) (attach_attrs output.c:307), and poly_gencode vertex scaling (lw+rw)/INCH2PS(width) (shapes.c:3010) was missing — C's factor is exactly 1.0 on the normal path via gv_nodesize, so identity-within-ULP left unscaled. Result: patchwork 28→1, circo 29→23, twopi 22→16, osage timeout gone; total 93→54. | src/layout/{circo,twopi,patchwork}/index.ts, src/render/dot.ts, src/common/poly-gencode.ts | tsc 0, npm 2931, SVG gate PASS (0 regr), xdot goldens 145/145 |
| 2026-07-09 | D/F buckets root-caused (fix pending) | Multi-edge + concentrate under neato-family: port never ported C splineEdges WRAPPER coalescing (neatosplines.c:737-780) — equivEdge keys edges by UNORDERED endpoint pair (node pointer order; A->B and B->A merge, explaining concentrate-b135), leader ED_count++ per member, members chained via ED_to_virt, members skipped in spline_edges_ (ED_count==0 → continue), representative routes cnt (=1 if Concentrate) edges walking to_virt. Port routes every out-edge independently (splines.ts withVconfig/straight; info.count never set on neato path). | (diagnosis) | pending |
| 2026-07-09 | **D/F FIXED: neato-family edge coalescing** (611207a) | Ported the C splineEdges wrapper (neatosplines.c:753-773): equivKey = UNORDERED endpoint pair + port points (A->B==B->A → concentrate 2-cycle merge); newitem seeds ED_count(leader)=1; members count=0 + to_virt prepend. Routing gates: spline_edges_ skips count==0; leader routes cnt (=1 under Concentrate) walking to_virt; straight path routes the WHOLE chain via dot's makeStraightEdges (perp fan-out); makeSelfArcs single under Concentrate; AGSEQ iteration. Old neato drift-pin had pinned the BUG (both parallels byte-identical paths); new output matches native exactly → pin regenerated. Cross-product 54→29 (circo 15, twopi 7, osage 6, patchwork 1); D+F buckets cleared. SVG gate: only flag = 1652 contention timeout flake (maxΔ=0.0, concurrent 681-pairing sweep; baseline reverted, not committed). npm 2931. Remaining 29: E-ortho (10), circo big-edge routing (~7), record-port clip (3), tiny numerics (~5), singles. | src/layout/neato/splines.ts, test/golden/refs-port/neato-tiny-multi-edge.svg | npm 2931, xdot 54→29, SVG gate flake-only |
| 2026-07-09 | **E-ortho FIXED: 5 mechanisms** (9d0a56a, 36747f3) | (1) circo/twopi init used the setEdgeType MACRO — C's FUNCTION (utils.c:1423) reads the splines attr; ortho was routed as LINE. (2) ortho install used generic bezier clip (reparameterizes ctl pts); C attachOrthoEdges uses ortho sinfo {ignoreSwap,isOrtho} (ortho.c:1269) → segment-truncating clip. (3) vconfig NEVER built for ortho (neatosplines.c:618); maze-dropped edges + self-loops fall through the drawing loop to makeStraightEdge/makeSelfArcs with NORMAL sinfo — C-harness proved native itself drops the noisy-coord circo edge and straight-routes it. (4) ortho obstacles = UNMARGINED outline box (makeObstacle isOrtho, ±outline_lw symmetric); margined rects made osage 4pt gutters illegal → all-straight fallback. (5) orthoEdges es[] collection order (agfstnode/agfstout, ortho.c:1220) is LOAD-BEARING: equal-manhattan-length ties keep qsort's input permutation → seg_list order → parallel-segment track assignment (osage multirank a->d/b->c both len 98: tracks y=88/100 swapped). Diagnosed via ORTHO_DUMP-instrumented libgvc (cells+segs+tracks) vs port maze probe: identical cells/routes, swapped tracks only. Also mirrored concentrate unordered-pair dedup; C source reverted, pristine libgvc rebuilt + re-verified. Cross-product 29→18 (circo 11/twopi 4/osage 2/patchwork 1). | src/layout/{circo,twopi}/init.ts, src/layout/neato/splines.ts | npm 2931, SVG gate PASS (0 regr, 760 conf, 0 TO), xdot 29→18 |
| 2026-07-09 | **circo big-edge FIXED: 3 mechanisms** (2b0c1e5) | Chorded blocks reorder the circle vs native (chordless coincide → goldens blind). (1) remove_pair_edges WHOLESALE UNPORTED: clone + min-degree qsort worklist prunes paired edges / adds synthetic clone edges before the spanning tree (blockpath.c:102-222). (2) spanning dfs must walk agfstedge(g,n) per node + forest loop, not the flat edge list. (3) **count_all_crossings has a LOAD-BEARING C BUG the port had "fixed"**: remove_edge keys the open-edge dict by HALF-EDGE pointer; the closing node's agfstedge yields the OPPOSITE half → dtdelete never matches → open list only grows → each close counts ALL later-opened edges (nested+interleaved; sfdp-simple initial=15 vs true interleave=1); reduce() accepts moves against the inflated counts, and iterates SUBGRAPH node order + agfstedge neighbor order (tail-first). Diagnosed by BLOCK_DUMP-instrumented native plugin vs port probe: skeleton traces identical, divergence isolated to the count. Note: /tmp/gvmine has the plugin-rebuild recipe (CPATH for glib 2.88.2 + sed 2.88.1→2.88.2 in cmake link files; copy BOTH .8 and .8.0.12 dylibs). Cross-product 18→12; circo bucket cleared except dot-cluster-external-edge (cluster mechanism); ldbxtried/circo Δ55→0.65. | src/layout/circo/blockpath.ts | npm 2931, SVG gate: only flag = 1652 contention TO flake (maxΔ=0, 2nd occurrence, solo-run conformant), xdot 18→12 |
| 2026-07-09 | record-port bucket FIXED (resolvePorts) | C splineEdges wrapper pass 1 = resolvePorts(e) per edge (neatosplines.c:748): .dyna record/compass ports resolve to a concrete point against the OTHER endpoint (shapes.c:4334). Port's neato-family path skipped it → A:f0->B routed from record CENTER. Reused dot's resolvePort; runs before equivEdge (keys resolved port pts). dot-port-record-aligned PASS ×3 engines. Remaining compass cases (0.25-0.64) = clip-asymmetry: native offsets HEAD port pt by +0.32/0.64, port offsets TAIL by -0.32 — same magnitude swapped endpoint, likely bezier-clip direction class. | src/layout/neato/splines.ts | npm 2931, tsc 0 |
| 2026-07-09 | **singletons round 1: 12→6** (a697358) | (a) patchwork mkTree + osage buildChildLists iterated cluster nodes in BLOCK-REFERENCE order; C agfstnode = AGSEQ — feeds equal-area qsort tie (patchwork tiles) + packing rect assignment (osage). ldbxtried cleared both; osage+patchwork now 158/158 CLEAN. (b) twopi adjustNodes stub → ported scAdjust (constraint.c:767, Marriott et al.) with overlap=scale/scalexy/compress dispatch (unset=AM_NONE no-op); clears dot-node-class-attr. (c) makeObstacle was a margined RECT for all shapes; C: poly outline-periphery vertices (+per-corner/hypot margins), 8-gon circumscribed about the outline ELLIPSE, record field box (neatosplines.c:316-460); + routing margin was hardcoded {4,4} vs C esepFactor default {3.2,3.2,add}. Fixed dot-long-edge-polyline canvas; its residual = ROUTE-SIDE TIE (port bows right, native left — mirrored around the column). Remaining 6: cluster-external-edge/circo, compass-clip-asymmetry ×3 (0.25-0.32), polyline route-side tie, ldbxtried/circo Δ0.65. SVG gate: 1652 timeout AGAIN maxΔ=0.0 in a solo-launched run — but user's 2nd session was active on the machine (branch even switched mid-run), and this diff never touches the dot-engine pipeline 1652 uses; flake per precedent. | src/layout/neato/{splines,sc-adjust}.ts, src/layout/{patchwork,osage}/index.ts, src/layout/twopi/init.ts | npm 2931, xdot 12→6 |
| 2026-07-09 | **compass class FIXED: 6→3** (1f5b702) | Mechanism (harness-verified to the digit): a compass port POKES OUTSIDE the node box (ellipse s-port at -outline_height/2 = -18.3207 vs box 18) → the routed spline extends below the node → C's clip_and_install expands GD_bb (update_bb_bz) and the postprocess translate anchors at the port point (native node centre y=18.3207). (a) circoLayoutFull recomputed bb FROM NODES after routing → clobbered the expansion → drawing sat 0.32 below canvas; removed the refresh. (b) twopi path: shared pack shiftEdgePoints moved splines but NOT the port's PRE-COMPUTED arrow draw-ops (C regenerates arrows at render time) → arrows lagged by the overhang; shift the ops in shiftEdgePoints; dot pack branch's per-component compensation removed (double-shift). Cross-product **681 pairings → 3 diverged**: dot-cluster-external-edge/circo (clusters under circo), dot-long-edge-polyline/twopi (pathplan route-SIDE mirror tie around a blocking node), ldbxtried/circo Δ0.65. osage+patchwork 158/158, twopi 155+1 native-crash, dot 51/51. SVG gate PASS solo (760 conf, 0 TO — earlier 1652 flags confirmed = user's 2nd session contention). | src/layout/circo/index.ts, src/layout/pack/index.ts, src/layout/dot/pack-components.ts | npm 2931, xdot 6→3, SVG gate PASS |
| 2026-07-09 | **blocktree dfs order (cda28cc) + polyline A8 verdict — CROSS-PRODUCT COMPLETE: 680/681** | (a) bcc dfs must walk agfstedge(g,u) (blocktree.c:62), not the flat edge list — DISCOVERY order sets block-tree child order → satellite blocks' angular slots (ldbxtried n513/n518 traded places); also cleared dot-cluster-external-edge (same artifact). (b) dot-long-edge-polyline/twopi = **A8 IRREDUCIBLE, input-injection PROVEN**: the a->f route grazes the EXCLUDED octagons' corner vertices exactly on the segment line (knife-edge); octagon corners carry ±1e-14 libm trig noise with OPPOSITE signs (Apple libm vs V8); injecting native's corners + production endpoints into the port's obsPath reproduces native's LEFT route BYTE-EXACT — pathplan exonerated, root = corner ULP noise. FINAL cross-product: **680/681 pass** (1 A8, 1 native-crash excluded); dot 51/51, circo 158/158, twopi 155/157, osage 158/158, patchwork 158/158. SVG gate: 1652 timeout flag maxΔ=0.0 again (4th; gate covered circo-only change — cannot affect a dot graph; ambient machine load). All C instrumentation reverted, plugin+libgvc rebuilt pristine, byte-parity vs /tmp/ghl re-verified. | src/layout/circo/blocktree.ts, blockpath.ts (export) | npm 2931, xdot 3→1(A8) |
| 2026-07-10 | **corpus hardening rounds 1-2** (9a5f042, 91d86bf) | Corpus sweep (759 SVG-conformant × osage/twopi/patchwork; circo pending). Baseline: osage 568/184div, patchwork 449/305div, twopi 659/90div. Round 1 (4 fixes → osage 693, patchwork 745, twopi 686): makeSelfArcs label updateBB+makePortLabels (self-loop label canvas, 2391_1/b491); twopi getRankseps delx=0 quirk (present-but-unparseable ranksep collapses rings to r=0 — b80); xdot INPUT pos attr echo when no spline (attach_attrs only overwrites with ED_spl — patchwork never routes; 298 divs from one line); osage addMarginAndBorder border[3]→BOTTOM_IX=0 (labelloc=b clusters got no label space + equal-size pack order flipped; 97 cluster divs). Round 2: REAL-rankdir records (graph_init stores rankdir<<2 for non-USES_RANKDIR engines; record_init reads GD_realflip "Always use rankdir…" shapes.c:3672 — neutralGraphRankdir() in all 6 engines); installStraight PLINE branch = makePolyline (the "structurally unreachable" throw IS reached from neato PLINE straight fallbacks); **layout ATTR unconditionally overrides the selected engine** (gvlayout.c:66 — 2613's layout=dot beats -K everywhere; ctx.layout now mirrors, unrecognized=error). | src/layout/*, src/render/dot.ts, src/gvc/context.ts, src/common/record.ts | npm 2931 ×2, targeted corpus cases PASS |
| 2026-07-10 | **corpus round 3: _neato_set_aspect** (20fb0bf, 92852a0) | ~55 of the remaining divergences carried ratio/size attrs. Ported _neato_set_aspect (neatosplines.c:1023): parseNeatoDrawing (setRatio subset incl. expand/value), neatoTranslate (pos+xlabels+splines+bb tree), fill/expand/value factor math verbatim; wired via neatoSetAspect (aspect BEFORE coord sync), splineEdgesShifted (seed shifted bb pre-aspect), osage's ratio branch (osageinit.c:324: pos←coord/72 then spline_edges0(g,true)). SECOND bb-clobber instance: splineEdgesShifted's post-aspect computeSubgraphBB recompute wiped the scaled bb (fill sets bb=size exactly; node extents land short — only CENTERS scale). Counts: osage 712→722+, twopi b22 148→580→PASS. RECURRING CLASS (3rd instance): never recompute g.info.bb from node extents after a pass that deliberately set it (aspect scale, spline expansion) — C only ever EXPANDS via update_bb_bz. | src/layout/neato/{set-aspect,init,splines}.ts, src/layout/osage/index.ts | npm 2931 ×2 |
| 2026-07-10 | **CORPUS HARDENING CHECKPOINT** | Full 759-item corpus × all 4 deterministic non-dot engines: **patchwork 748 pass / 6 div**, **osage 722 / 32**, **twopi 719 / 32**, **circo 688 / 57** (oracle-errors 5-14 per engine = native-side). Baseline before hardening: osage 568, patchwork 449, twopi 659 — ~500 divergences cleared by 9 root-caused fixes across 3 rounds (self-loop label bb, ranksep collapse quirk, input-pos echo, BOTTOM_IX, real-rankdir records, PLINE fallback, layout-attr precedence, _neato_set_aspect, aspect-bb clobber). Remaining ~127 in ~10 classes: 144-family head/tail port labels (_hldraw_ x), states edge-label x (Δ33), polypoly polygon verts (Δ2-4), twopi 2609/2258/2556 canvas, circo fresh 57 (unbucketed), 2717 cluster ratio, 1898 node draw presence, 2475_2 cluster object. SVG gate: 774 stable 0 regr; 1652 contention flag (concurrent 4-engine sweeps, maxΔ=0.0, precedent). | — | checkpoint |
| 2026-07-10 | in_poly recompute in installSpline (cf7c584) + 2168 re-attributed | Chased circo 2168 (compass-port parallel edges B4 vs native B7/B10): port endpoints BIT-IDENTICAL to native's getPath dump, both sides exclude both endpoint polys (recomputed qp=1 — boundary point IS in_poly by the shared wind 0.0001 tolerance). Ported C makeSpline's pp/qp recompute via in_poly on the PATH endpoints (neatosplines.c:554-562) — differs from ND_lim reuse only in overlapping layouts, faithful either way. REAL 2168 mechanism = HAVE_GTS branch (neatosplines.c:681): ED_count>1 \|\| BOUNDARY_PORT routes via makeMultiSpline (GTS CDT router); straight shortcut only when pn==2 && !BOUNDARY_PORT. Filed task: port multispline.c (1209 l) + mkSurface. Also: corpus JSONL divergence lists go STALE across fix rounds (resume-skip keeps old verdicts) — purge non-pass lines + re-sweep before bucketing; post-resweep truth: circo 700/45, twopi 719/32, osage 722/32, patchwork 748/6. | src/pathplan/index.ts, src/layout/neato/splines.ts | npm 2931, xprod 680/681 |
| 2026-07-10 | **PRISM PORTED — overlap=false family** | A/B-proven mechanism (stripping overlap=false → native/port byte-identical bbs on circo AND twopi): the GTS reference build maps overlap=false AND any unrecognized overlap value to AM_PRISM (adjust.c adjustMode[1]); circo (circularinit.c:203) and twopi (twopiinit.c:116/135) run adjustNodes post-layout; port lacked the entire subsystem. Ported: delaunay.ts (Bowyer–Watson; GTS edge ITERATION order is an allocation-address artifact — unreproducible — but consumers depend only on the edge SET; robust incircle = Shewchuk fast path + exact BigInt fallback, REQUIRED because circo/twopi ring layouts make near-cocircular quads the common case — naive precomputed-circumcircle BW emitted a NON-PLANAR 402-nz "triangulation" vs native 180), sparse-solve.ts (jacobi-CG, sparse_solve.c), overlap-prism.ts (call_tri; get_overlap_graph scan-line with the load-bearing C quirk that departed intervals' OPEN keys are never deleted from the rbtree; ideal_distance_avoid_overlap; overlap_scaling bisection; OverlapSmoother maxit_sm=1; StressMajorizationSmoother_smooth NORMAL scheme; remove_overlap loop), fdp-adjust.ts (getAdjustMode mapping incl. prism<N> suffix + mapbool fallback, prism0 = scaling-only NOT no-op; fdpAdjust over ND_id/ND_pos). Wired: twopi adjustNodes, circo ncc==1 (real graph). Circo multi-component adjustNodes(sg) runs on the DERIVED graph in C — which has NO overlap attr → getAdjustMode(NULL) = AM_NONE → the existing stub is accidentally FAITHFUL. PRISM_DUMP trajectory vs instrumented native: try-for-try identical (maxov bit-exact at try 0; positions ~1e-13 drift; same convergence tries 38/39). Family: 11/20 targeted PASS (2609, 2258, 2556, 2242, overlap_neato, neatosplines_neato under twopi; arrows/newarrows canvas now exact). Residuals re-attributed: arrows _hldraw_/_tldraw_ + arrowsize canvas = port-label placement bucket (144-family); neatosplines/circo ptCount 8v14 = multispline bucket; 1855 Δ~1.5 near-miss TBD. | src/layout/neato/{delaunay,overlap-prism,fdp-adjust}.ts, src/layout/sfdp/sparse-solve.ts, src/layout/{twopi/init,circo/circular}.ts | npm 2931, tsc 0; full re-sweep running |
| 2026-07-10 | **port-label bucket FIXED: addEdgeLabels missing from neato routing fns** | PL_DUMP A/B: native emits 36 place_portlabel calls for arrows/twopi, port emitted ZERO — head/tail labels fell through to addXLabels (generic xlabel placement, wrong formula) explaining every _hldraw_/_tldraw_ x diff AND the arrowsize canvas blowup (misplaced labels at labeldistance×… expanded the pre-translate bb). C calls addEdgeLabels(e) at the END of every routing fn: makeSpline (neatosplines.c:585), makePolyline (:526), makeStraightEdges (routespl.c:989/1038 — port already had it), useEdges/ortho branch (:655), makeSelfArcs (port had it). Port's vconfig path (installSpline/installPline) and ortho maze-routed edges lacked the call. 3 call sites added; port label pos now matches native to 1e-11. arrows/newarrows CIRCO PASS; twopi residual = ptCount 8v14 (samehead multi-edge → multispline bucket); arrowsize twopi n=19 = arrow-clip on arrowsize=4 (separate); 1855 Δ~1.5 near-miss TBD. Proutespline-failure fallback intentionally does NOT add labels (C's error path installs nothing). | src/layout/neato/splines.ts | npm 2931, tsc 0; sweep running |
| 2026-07-10 | **multispline ported (WIP verification)** | Ported the HAVE_GTS router: cdt-surface.ts (constrained Delaunay over the robust BW core: Anglada pseudo-polygon retriangulation per directed constraint, on-segment vertices split the constraint recursively; hole rule = face containing a REVERSED directed constraint edge after CCW normalization — exactly GTS triangle_is_hole's "right of the constraint"; interior obstacle faces without constraint edges survive as isolated tgraph nodes, faithful+harmless), multispline-router.ts (mkRouter MARGIN=32 bbox CCW + obstacles CW, mkTriGraph, addEndpoint side-cones, fPQ max-heap w/ guard=0 over ≤0 domain, triPath), multispline.ts (mkPoly two-sided corridor, mkCtrlPts SEP=15 bisector fans, genroute via Pshortestpath funnel + Proutespline, tweakPath, finishEdge flip for reversed chain members; straight branch passes doPolyline AS et — C quirk, et=1 ≠ PLINE — and gathers the FULL to_virt chain). Dispatch in withVconfig mirrors neatosplines.c:681: count>1 or boundary port → router; pn==2 && !port → straight fan; only router FAILURE falls through to plain pathplan. First-run results: 2168, 2168_2, 144_ortho/osage PASS (byte/0.01-exact). Known sub-issues for next round: (a) "destination point not in any triangle" — mkPoly corridor malformed for 241_0-class (compass ports between rank-adjacent nodes, endpoint exactly on obstacle boundary); (b) 2168_3 (octagon penwidth=7) falls back to plain routing — failure point TBD (addEndpoint cone filter suspected); (c) ptCount 8v14 on arrows-twopi Z->I etc is NOT multispline (single unported edge, no ports — knife-edge obstacle-graze under PRISM's ~1e-13 drift). npm 2931; full fresh 4-engine sweep + xprod running for regression gate. | src/layout/neato/{cdt-surface,multispline-router,multispline,splines,delaunay}.ts | gates pending |
| 2026-07-10 | **multispline landed + empty-bb latent fix; fresh-sweep truth** | Full FRESH 4-engine sweep (first since many resumed sweeps): resume-skip had HIDDEN two latent regressions never re-tested — 2285 (`digraph {}` under circo emitted bb=-Infinity: computeBBFromPos over 0 nodes returned ±Inf seeds; C compute_bb keeps {0,0,0,0} for empty graphs, utils.c:641 — guard added, 2285 conformant again) and 56/osage (canvas 268 vs 297.2, broken by an earlier commit, mechanism TBD — now visible in the diverged list). LESSON: run a FRESH (no-resume) sweep periodically; resume-skip hides regressions on ids that once passed. Multispline verdict: 2168+2168_2 fixed, xprod 680/681 unchanged, twopi/patchwork unchanged; KNOWN REGRESSION 2168_1 (Δ0.5 bezier — the test written for C's addEndpoint/makeObstacle penwidth edge case; my router is 0.5 off where native's multispline matches its plain path within 0.01) — top TODO with 241_0 corridor failures ("destination point not in any triangle") and 2168_3 fallback. | src/layout/neato/splines.ts (+computeBBFromPos guard) | npm 2931, tsc 0 |
| 2026-07-10 | **CDT rewritten as faithful GTS port; 2168_3 outline-ring obstacle; 56/osage bb clobber; A9 filed** | (1) cdt-surface.ts rewritten from Bowyer-Watson to GTS 0.7.6's actual algorithm (cdt.c): incremental insertion (closest-face sample + orientation-walk point_locate, 1→3 split, recursive swap_if_in_circle with STRICT exact incircle), constraint edges pre-created and shared via vertex connectivity so insertion-time swaps refuse them, remove_intersected_vertex/edge walk + triangulate_polygon for constraint enforcement, gts_triangle_enclosing formula (r=100·hypot(xmax−xo,ymax−yo), NOT the old bbox±100w), hole rule on stored ring order. Mechanism: BW-cavity vs GTS-flip agree generically but pick OPPOSITE diagonals on exactly-cocircular quads (2168_1's symmetric trapezoid) — corridor shape depends on the diagonal. Proof harness: standalone C linking real homebrew GTS, fed the port's bit-exact mkRouter inputs, per-insertion face signatures — 20/20 insertion states + post-constraint surface MATCH (2168_1 22 faces, 241_0 185 faces). (2) 2168_1/241_0 residual = NEW ACCEPTED CLASS A9: V8 Math.sin ≠ Apple libm sin by 1 ULP (bit-compared at 2π·4.5/8) → ellipse-obstacle corners differ ≤6e-14 → exact-rational incircle sign FLIPS (+1 port / −1 native inputs) → native's corridor fails Pshortestpath (falls back to plain spline), port's succeeds. Irreducible libm portability; docs/known-divergences.md §A9. (3) 2168_3 FIXED: polyObstacle indexed the outline periphery ring (peripheries+1 when penwidth>0, makeObstacle's vertices_offset) but the port never STORES C's outline ring in poly.vertices → verts[off+j] undefined → makeObstacle null → NO obstacles → straight edges. Fix: synthesize via polygonOutlineRing (same helper poly-inside uses; matches C's Q += bisector·penwidth/2/GAP, shapes.c:2346). (4) 56/osage FIXED: osage ratio_kind path had port-invented `g.info.bb = computeSubgraphBB(g,0)` (from 20fb0bf) — C's spline_edges0 KEEPS osage's GD_bb (cluster boxes + label space, ur.y 297.2); node-only bb (268) clipped cluster tops. Removed; graphs-b22 (the case 20fb0bf targeted) still PASS. Also dropped stale computeSubgraphBB import in neato/splines.ts. | src/layout/neato/{cdt-surface,splines}.ts, src/layout/osage/index.ts, docs/known-divergences.md | tsc 0, npm 2931; xprod + fresh 4-engine sweeps running |
| 2026-07-10 | gates green; engine checkpoint (fresh sweeps @ 522bae9) | tsc 0; npm 2931; xprod 679/681 (same 1 A8 residual + 1 oracle-error as baseline, 0 regr); FRESH sweeps 0 regressions across all four: circo 716/29div (2168_3+2285 fixed; 2168_1/241_0 now A9-accepted still counted as div), twopi 729/22div, osage 723/31div (56 fixed), patchwork 748/6div. oracle-error counts flaky run-to-run (circo 14 vs 8 — native timeouts, not verdicts). Sweep runner NOTE: one circo tsx straggled past run-gates' phase echo and finished its jsonl late — verify 759 unique ids before comparing. | plans/xdot-conformance/decision-journal.md | checkpoint |
| 2026-07-10 | **circo derived-edge SEQ order (b29 family) + FMA-contracted ccw emulation (42)** | (1) circo b29/b106/b124 doxygen family (10 ids, whole-canvas divergence): block SETS matched but block NUMBERING/tree-child order and in-block order diverged — port built derived edges from the FLAT document-order edge list; C's circomps creates them per TAIL node in seq order via agfstout, and the derived-edge SEQ drives the block DFS's agfstedge iteration (agedgeseqcmpf), deciding block creation order and tree child order (= each satellite's angular slot). Fix: buildDerivedEdges iterates nodesInSeq×outEdges with unordered dedup (first orientation wins); buildDerivedGraph also nodesInSeq. A/B block-tree dumps now match native exactly; b29/b106/b124 PASS. The iteration-order-mirror class strikes again — check it FIRST on discovery-order divergence. (2) 42/circo (uniform y-shift 16.9 from a handful of 2-cycle fan edges): traced multispline genroute — trip corridors and triPath IDENTICAL, but C's FIRST Pshortestpath FAILS ("destination not in any triangle") where the port succeeds. Standalone Pshortestpath harness reproduced on bit-identical input; deep-dive found ccw(B,C,p) with p BIT-EQUAL to B returns ISCW in the oracle, not ISON: disassembly of libpathplan's _ccw shows fnmul+fmadd — clang (arm64, default fp-contract=on) fuses `a*b - c*e` so d = exact(a·b) − fl(c·e) = the product's rounding error when the two products are identical. shortest.c pointintri then rejects polygon-VERTEX endpoints, makeMultiSpline fails for every coalesced 2-cycle (warnings visible in native stderr), and C falls back to plain per-member routing. Port now emulates the COMPILED semantics in triang.ts ccw: fast path (plain double + conservative 2.3e-16·(|P|+|D|) bound where plain and fused signs provably agree) + exact path (Dekker twoProductErr + twoDiffTail + dyadic-BigInt signOfSum3) reproducing the fmadd bit-behavior. Verified against native probe values; 42/circo PASS. NOTE: unlike prior A8 acceptances (2646/2620 — deep inside solve3/poly_init, refuted to fix), this contraction is ONE standalone compiled function with clean semantics — emulation is contained and buys every 2-cycle multispline dispatch corpus-wide. | src/layout/circo/circular.ts, src/pathplan/triang.ts | npm 2931, tsc 0; xprod2 679/681 = baseline; dot survey + fresh sweeps running |
| 2026-07-10 | gates green; checkpoint @ 800bdf0 | Fresh gates after circo-seq + fma-ccw: dot survey 754 conformant/0 diverged/4 accepted/1 timeout (1652, known flake; was 753); circo 729 pass/16 div (+13: b29 b106 b124 families ×10, 42, 2168_1 — 2168_1 left A9); twopi 729/22, osage 723/31, patchwork 748/6 (all 0 regr); xprod 679/681 = baseline; npm 2931; tsc 0. NEXT buckets by size: (a) twopi Z->I + circo Almgren ptCount 8v14 family (~12 ids) — suspected PRISM ~1e-13 pos-drift amplified into Proutespline piece-count flips; NEEDS the injection A/B (native post-PRISM ND_pos into port routing) to attribute — if port matches on injected input, accept-class; (b) states family ×3 (twopi, edge spline start Δ4.5); (c) shapes_dot ×2 (circo canvas), 1855 (Δ1.5 canvas, PRISM near-miss), 1949 (Δ11 canvas both engines), b81 (Δ785 canvas), 2475_2, 2082, windows-tree, 1990 (ptCount 8v1), 1856 (Δ36 bezier), b786 (Δ0.1). | plans/xdot-conformance/decision-journal.md | checkpoint |
| 2026-07-10 | **injection A/B verdicts: twopi arrows family EXONERATED; circo Almgren family = Plegal_arrangement STUB** | Injection recipe: native `spline_edges` entry ND_pos dumped at %.17g (POS_DUMP patch, since reverted) → port hook overwriting n.info.pos in splineEdgesShifted (POS_INJECT env, since removed) → full comparator. (1) graphs/arrows @ twopi: injected port output is FULLY CONFORMANT (Z->I byte-identical B 7/14-pt spline) → the 8v14 ptCount family (graphs-arrows/newarrows/arrowsize + share/windows/linux.x86/macosx/nshare variants, ~8 twopi ids) is 100% pre-routing position drift (twopi+PRISM fp, A1 class) — port routing/emission exonerated; accept-class, add to docs next session. (2) linux.x86/neatosplines_neato @ circo: injection does NOT clear (96 diffs remain; Almgren edges direct 8-pt vs native 14-pt detours). Traced: makeSplineEdge NEVER RUNS — splineEdgesImpl's LegalHelper.check is a BBOX-SPAN overlap stub, not C's Plegal_arrangement (legal.c:357 — full sweep-line segment intersection find_ints + findInside nested-containment). Circo octagon obstacles at margin 3.2 have overlapping BBOXES but disjoint POLYGONS → port declares arrangement illegal → RoutingHelper.straight for the WHOLE graph → every long edge loses its detour. REAL FIX: port lib/neatogen/legal.c (~400 l). Likely also behind other circo/twopi canvas divergences (1990/1856/windows-tree/shapes_dot candidates — re-triage after the port). | (diagnostic only; probes removed, C tree restored) | tsc 0; tree clean |
| 2026-07-10 | **legal.c PORTED — Plegal_arrangement sweep-line replaces bbox stub** | src/layout/neato/legal.ts: faithful port of lib/neatogen/legal.c — find_ints (vertices gvQsort'd by (x,y), left-to-right sweep; per vertex the two incident edges either enter the active list — after testing against EVERY active edge via find_intersection — or delete their active entry; "delete a non-line" error → -1 → illegal, matching C), sgnarea/between/online/intpoint/realIntersect verbatim (plain doubles, source semantics; no evidence yet the inlined-static compiled forms' contraction is load-bearing here — revisit only if a graze-tie divergence surfaces), findInside (NESTED bb + in_poly) for crossing-free containment. Wired into splineEdgesImpl replacing LegalHelper.check (bbox-span stub) — the stub declared circo octagon arrangements illegal whenever BBOXES overlapped, silently flattening whole graphs to straight edges. Fixes all 4 neatosplines_neato ids (circo); 241_0/twopi 25→6 diffs. 1990/1856/2082/windows-tree/shapes_dot/1949/b81/1855 (circo) + 2361/2183/states/1949 (twopi) unchanged — separate mechanisms. | src/layout/neato/legal.ts, src/layout/neato/splines.ts | npm 2931, tsc 0; xprod + fresh sweeps running |
| 2026-07-10 | legal.c gates green; checkpoint @ 9f2addb | Fresh sweeps: circo 733 pass/12 div (+4 neatosplines family, 0 regr), twopi 729/22, osage 723/31, patchwork 748/6 (0 regr each); xprod 679/681 = baseline; npm 2931; tsc 0. Remaining circo div (12): 241_0(A9) 1990 1856 b786(Δ0.1) 1855 windows-tree 2082 shapes_dot×2 1949 b81 2475_2. Remaining twopi (22): arrows-family ~8 (drift-exonerated, pending doc/acceptance listing), states×3, 2361, 2183, 1949, 241_0(6 diffs), 2239(1) 2343(1) b29×2(ldraw text). | plans/xdot-conformance/decision-journal.md | checkpoint |
| 2026-07-10 | **edge-label-Y bucket (2239/2343/b29×2) mechanism PINNED to label-height ULP; xlintersections De Morgan fixed** | Full trace chain on 2343/twopi (1 diff: one edge label Y): all 622 xladjust anchors identical; ONE label picks mid-bottom (port) vs mid-top (native) because the port's mid-top probe hits R-tree leaves native's search misses. R-tree INSERTION sequences differ at exactly 1 of 1140 entries: objplpmks rect bottom floor(pos.y−sz.y) = −1 (port) vs 0 (native) → different Hilbert key → different insertion position → different (Guttman-split) tree shape → different search results. Bottom node object: port pos.y −3.55e-15, sz.y 36.00000000000001 vs native EXACTLY (…, 36): the divergence bottoms out in NODE LABEL HEIGHT (+1 ULP in port's text-size chain, poly_init dimen.y class). INJECTION A/B does NOT clear it (native pre-routing pos injected → still 1 diff) — the ULP is the port's own text-measurement arithmetic, NOT layout drift. NEXT well-scoped task: diff estimate/poly_init label-height computation op-by-op for 2343's wide box nodes (36.00000000000001 vs 36) — HIGH blast radius (text size is corpus-global), do NOT change blind. Fixed en route: xlintersections zero-size filter was `sz.x>0 || sz.y>0` (skip) — C skips only when BOTH positive (De Morgan slip); corpus-neutral today (no half-zero-size objects) but a real misport. Probes removed; C tree restored. | src/label/xlabels-intersect.ts | npm 2931, tsc 0; gates running |
| 2026-07-10 | **label-height ULP ROOT-CAUSED + crHypot: V8 Math.hypot not correctly rounded** | Bisected 2343/node3654 (diamond): C poly_init dumps show bb input y CLAMPED to height=36 exactly BEFORE the unit-vertex loop, and Apple's trig chain returns unit ymax EXACTLY 0.5 → ymax2 = 36 exact. Bit-level A/B of the verbatim loop (Python=Apple libm vs node=V8): at the ymax-deciding vertex, identical R and alpha, identical sin(=1.0), but **hypot(R.x,R.y) differs by 1 ULP** — Apple 0.5 (0x3FE0000000000000), V8 nextafter(0.5). Exact-rational check: true value < ulp-midpoint → correctly rounded result IS 0.5 — Apple is CR here, V8 is not. (atan2 also differs at other vertices but contributes only ~1e-17 to ymax — immaterial.) FIX: src/util/cr-hypot.ts — correctly-rounded hypot via Math.hypot candidate + exact dyadic-BigInt walk over ulp-midpoint intervals (ties round-to-even); applied at poly-sizing's two shapes.c-loop hypot sites (vertex radius r + skewdist). CR is a deterministic platform-independent target that matches ANY correctly-rounded libm — unlike chasing Apple bit patterns. 2343/twopi PASS. 2239/share-b29/windows-b29 remain (each a distinct knife edge, likely atan2/sin — NOT emulatable the hypot way; candidates for A-class acceptance). NOTE: crHypot may also bear on accepted A3 (hypot findMaxDev tie-breaks, hub-fanin b100/b104) — revisit whether adopting it at those sites un-accepts them. | src/util/cr-hypot.ts, src/common/poly-sizing.ts | npm 2931, tsc 0; full gates + dot survey running |
| 2026-07-10 | **crHypot REFUTED by sweep — label-height ULP class ACCEPTED (A9 extension)** | The correctly-rounded-hypot fix (previous entry) PASSED 2343 but the fresh circo sweep caught a regression: 2168_3 (octagon penwidth=7, previously conformant) flipped to diverged — its obstacle sizing flows through the SAME poly_init vertex-radius hypot, where V8's (non-CR) value matched the oracle: either Apple's hypot is not correctly rounded at that input, or 2168_3's prior PASS rested on compensating ULPs elsewhere in the trig chain (atan2/sin also differ ±1-2 ULP per the bit-level A/B). Conclusion: NO deterministic hypot policy matches the oracle on both cases — the class is irreducible libm portability, same family as A3/A9. Reverted (poly-sizing back to Math.hypot; util/cr-hypot.ts removed — full method preserved in the previous journal entry for future revisits). ACCEPTED: 2343/2239/share-b29/windows-b29 (twopi; 1-2 edge-label diffs each) as A9-family — libm 1-ULP in poly_init's unit-vertex trig (hypot/atan2/sin) amplified by the min-size clamp straddle and the xlabel R-tree floor()/Hilbert chain. Gates on the reverted tree: 2168_3 PASS, 2343 back to 1 diff, tsc 0, dot survey 754/0 unchanged. LESSON: the fresh-sweep-before-commit discipline caught this BEFORE landing — a spot-check-only flow would have shipped the regression. | src/common/poly-sizing.ts (net zero), plans/decision-journal.md | tree clean vs HEAD except docs |
| 2026-07-11 | **states family: addXLabels object ORDER fixed (agfstnode×agfstout mirror); residual = 1-ULP anchor knife edge** | states/twopi (3 ids): uniform x-shift 4.512 with IDENTICAL bb = two edge labels placed on mirrored sides. Trace: port's xlabels object array was built from Map order × document-order edges; C's addXLabels walks agfstnode×agfstout — placement is SEQUENTIAL so array order is load-bearing (port placed 'return' before 'dispatch'; native the reverse). FIXED in common/xlabels-place.ts (nodesInSeq × outEdges, both countAllSlots and buildArrays). Residual after fix: 'dispatch' STILL flips side — exact-value probe shows the label's x-left right-edge fl((anchor−sz)+sz) EQUALS the q7 anchor x in the port (strict < fails, candidate accepted) but is 1 ULP ABOVE it in native (anchor inside, candidate rejected): the empty->stolen edgeMidpoint anchor differs by 1 ULP between builds (dotneato_closest Bezier-eval chain). OPEN: determine whether the anchor ULP is spline-bit drift or a fixable edgeMidpoint op-order diff. Port also does not emit `lp` for xlabel-placed edge labels where native does (emission gap, separate small fix). | src/common/xlabels-place.ts | gates running |
| 2026-07-11 | gates green @ xlabels-order fix; per-engine parity reports SHIPPED | Order fix (nodesInSeq×outEdges in xlabels-place): 0 regressions all tracks; osage 723→730 (+7); circo 733, twopi 729 (2343 stays diverged = A9-ext accepted), patchwork 748; dot survey 754/0; xprod baseline; npm 2931. NEW REPORT SET (user request): PARITY.md = cross-engine overview (tracks table + goldens by engine + links); PARITY-dot.md = former dot SVG dashboard (dashboard.ts retargeted); PARITY-<engine>.md per engine via new test/corpus/parity-report.ts; engine sweeps promoted from scratchpad to test/corpus/engine-walk.ts writing committed parity-<engine>.json(l). Per-engine divergence listing adopted (user Q): each engine page lists its own diverged table; accepted classes stay global in known-divergences.md. | test/corpus/* | all gates green |
| 2026-07-11 | 1949 (circo+twopi, Δ11 canvas) pinned to SELF-LOOP label widen | Dominant diverging object = self-loop structEtwasErkannt->structEtwasErkannt: native loop bulges to x=202.84 with lp=(213.06,140.19); port bulges to 191.84, NO lp — exactly Δ11 = the canvas delta on BOTH engines (one mechanism, 2 corpus entries). Port selfedge DOES have label-widen logic (splines-selfedge.ts:373-380: SELF_EDGE_SIZE + (flip ? dimen.y : dimen.x)) and info.flip/rankdir are only set by dot's init (faithful — C's GD_flip is dot-only; 1949 has rankdir=LR but twopi must NOT flip). NEXT PROBE: print lbl/lbl.dimen in selfRightSpace during 1949/twopi — suspicion: the edge label is UNSIZED under the twopi pipeline (edge-label init not run / html-ish label <evT...> not measured) so the widen returns bare SELF_EDGE_SIZE; if dimen present, then the flip flag IS leaking true. Fix + emit lp for self-loop labels together. | (diagnosis only) | pinned, not fixed |
| 2026-07-11 | **1949 FIXED both engines: neato-family never parsed nodesep** | The self-loop label was a red herring (label sized & applied correctly): C parses nodesep ENGINE-NEUTRALLY at graph_init (input.c:665-667, GD_nodesep = POINTS(late_double(g,"nodesep",0.25,0.02)) — POINTS() ROUNDS, so 1949's nodesep=0.4 → round(28.8) = 29); the port parsed it only in dot's init, so every neato-family routing site (makeSelfArcs stepx, straight fans, ortho fallback) fell back to the default 18 — self-loop bulge Δ = 29−18 = 11 = the canvas delta on both engines. Fix: graphNodesep(g) in neato/splines.ts — parse-on-demand mirroring input.c, cached in root.info.nodesep, replacing all three `?? 18` sites. 1949 PASS under circo AND twopi (+2 corpus). 1855/windows-tree unchanged (separate mechanisms). First gate run using the promoted test/corpus/engine-walk.ts. | src/layout/neato/splines.ts | npm 2931, tsc 0; gates running |
| 2026-07-11 | 1949 gates green (+3 engines); orphan hardening; brevity rosters | nodesep fix cleared 1949 under circo AND twopi AND osage (it sat in all three diverged lists): circo 734/11div, twopi 730/21, osage 731/23, patchwork 748/6 — 0 regressions; xprod baseline; npm 2931. OPS: user-reported orphan PID at 100% CPU ×20h = a hung 241_1/circo port render surviving spawnSync's killSignal (only the npx wrapper died) — killed; engine-walk.ts hardened to detached spawn + process-GROUP SIGKILL. FILED: 241_1 under circo can HANG the port (nondeterministic vs its usual quick metrics port-error — hang-class bug, V8 --prof recipe applies; not in conformant sweep set so no corpus impact). REPORTS: per user request, the conformant-roster brevity line now on ALL parity pages — PARITY-dot.md + PARITY-XDOT.md link parity.json/xdot-parity.json instead of enumerating 750+ ids (dead collapsedIds helpers removed). | test/corpus/*, src/layout/neato/splines.ts | all gates green |
| 2026-07-11 | **iterative-track parity rows SHIPPED (neato/fdp/sfdp @ ±0.5)** | Per user request: engine-walk.ts gains per-engine tolerance (0.01 deterministic / 0.5 iterative, recorded in parity-<engine>.json as `tolerance`); parity-report.ts renders neato/fdp/sfdp in a SEPARATE "Iterative engines (±0.5 characterization)" Tracks section with an explicit do-not-compare-bars note. First characterization sweep: neato 263 pass/489 div/7 oerr (34.7%), fdp 295/390/74 err (38.9% — 59 port-errors are one mechanism: `removeOverlapAs mode "scale" reached ... prism/voronoi overlap removal not [ported for fdp-cluster derived graphs]` — a KNOWN stub throwing, actionable), sfdp 254/475/30 (33.5%, 23 port-errors untriaged). Reading: ~a third of the corpus lands within 0.5pt of native even on the A1 engines, i.e. the ported arithmetic (fma.ts, arm-pow, matched PRNG) holds most small/medium graphs; drift dominates on the rest. fdp's 59 stub errors are the obvious first fix to lift its row. | test/corpus/engine-walk.ts, test/corpus/parity-report.ts, test/corpus/parity-{neato,fdp,sfdp}.json(l), PARITY*.md | tsc 0; sweeps complete |
| 2026-07-11 | **per-engine accepted-divergence reports SHIPPED** | Ported the dot-track accept/join pattern (accepted-divergences.json + accepted.ts) to the per-engine xdot tracks: new `test/corpus/accepted-divergences-engines.json` (engine -> id -> {class,bound,ref}, no glob/engineIn selector needed yet — every entry names a specific already-root-caused corpus id) + a loader/join in parity-report.ts (mirrors dashboard.ts's `acceptedTable`/`matchAccepted` shape). Populated ONLY from already-landed RCA (no new diagnosis): twopi A1 arrows family (8 ids: graphs-arrows/newarrows/arrowsize + linux.x86/macosx/nshare/share/windows variants — 2026-07-10 "injection A/B verdicts" entry, injection fully clears graphs-arrows Z->I to byte-identical); twopi A9-ext label-height ULP family (2239/2343/share-b29/windows-b29 — 2026-07-10 "crHypot REFUTED" entry); circo A9 CDT-cocircular-tie family (241_0, 41 diffs/Δ≈0.2pt — 2026-07-10 "CDT rewritten as faithful GTS port...A9 filed" entry). docs/known-divergences.md gained two new prose subsections with explicit `<a id>` anchors (not relying on markdown-it-anchor slug-guessing): A1 "Engine-track acceptance: twopi arrows family" and an A9 "Engine-track acceptance" paragraph citing both journal dates as provenance. Result: twopi diverged 21→9 (+12 accepted), circo diverged 11→10 (+1 accepted); PARITY.md track table's `accepted` column now non-zero for twopi/circo (was hardcoded 0 for every non-dot engine). Deliberately did NOT add twopi's own `241_0` (6 diffs, same A9 corridor-flip family) — out of the task's explicit id list, left for a follow-up pass. | test/corpus/parity-report.ts, test/corpus/accepted-divergences-engines.json, test/corpus/PARITY-{twopi,circo,osage,patchwork,neato,fdp,sfdp}.md, test/corpus/PARITY.md, docs/known-divergences.md | tsc 0; vitest test/corpus 14/14 |
| 2026-07-11 | fdp/sfdp overlap dispatch + rand() coincident fallback | Ported the missing overlap-removal dispatch. **fdp** (xlayout.c fdp_xLayout→removeOverlapAs→removeOverlapWith/getAdjustMode): removeOverlapAs(g,flag) wires the ported neato machinery — overlapPrismTries+fdpAdjust for AM_PRISM, scAdjust for scale/scalexy/compress; overlap=false→AM_PRISM(1000) on GTS; unported algos (voronoi/oscale/vpsc/ortho/ipsep) still throw. **sfdp** (sfdpinit.c sfdp_layout): resolveControl computes AM_PRISM (in-layout, ctrl.overlap=value, scaling via lateDouble(overlap_scaling)) vs non-PRISM (ctrl.overlap=-1 + post-layout adjustNodesScale); routed multilevel overlap call through full removeOverlapPrism (== old scaling-only slice for ntry=0). **rand()**: macOS rand()=Park–Miller MINSTD seed 1 (=existing crand); fdp seeds only srand48 (separate drand48), so coincident stream is unseeded⇒srand(1), reset per render; coincidentDelta() re-rolls 5-crand()%10 (tlayout doRep/applyAttr + xlayout doRep). Cleared 55 fdp + 22 sfdp + 4 rand port-errors; PASS incl share-clust3/2564/2609/2258/graphs-b7/graphs-clust5. RESIDUAL: sfdp 2556 (repulsiveforce=100) now hits armPow(x=NaN) — degenerate overflow (oracle bb=-2^32 too), orthogonal to dispatch, out of write-set (arm-pow.ts); left as fast port-error, RCA in .agent-notes/fdp-rand-fallback-rca.md. | src/layout/fdp/{index,xlayout,tlayout}.ts, src/layout/sfdp/{index,spring-driver}.ts, +2 guards.test.ts | tsc 0; npm test 2934/2934; 20+20 pass-id spot-checks 0 regr |
| 2026-07-11 | **1855 root-caused: `size` not ROUNDed in neato-family parse (POINTS family, NOT PRISM)** | Prior "1855 PRISM near-miss" REFUTED. C stores `size=` via getdoubles2ptf→POINTS()=ROUND(in·72) (input.c:476, geom.h:62); the port's neato-family parse (set-aspect.ts neatoSizePoints) did `in·72` WITHOUT the ROUND. C-instrumented _neato_set_aspect (probe reverted): natural pre-fill bb BIT-IDENTICAL C vs port (circo 622.327317,533.651462; twopi 701.936180,574.811023; osage 344,236) — ONLY size differed (C 724,441 vs port 724.4928,440.54136). In R_FILL the size error perturbs xf=size.x/bb.x (yf stays 1 → Y bit-exact, X off by the 1.00172 size ratio = the whole-canvas width delta 877.62 vs 876.11). Same class as the 2026-07-11 1949 nodesep fix (neato-family omitted POINTS' ROUND) — here it's size. FIX: neatoSizePoints delegates to the already-faithful parseDrawingSize (viewport.ts, rounds via Math.round(in·72)), deleting a divergent duplicate. **circo 469→0 CONFORMANT.** twopi 871→842 and osage 1250→110 are SEPARATE pre-existing mechanisms, RCA'd (.agent-notes/1855-rca.md): (#2 twopi) pre-scale twopi radial ND_pos differ ~1-2pt (node1 y 3.998187 port vs 3.985300 C) — twopigen layout, independent of fill; (#3 osage) node centers now BIT-EXACT, all 110 residual diffs are obstacle-routed B-7 edge beziers (~10pt) — neato spline routing. No-regression: entire blast radius (size+ratio graphs 1447_1/2193/2239/2613/56 × circo/twopi/osage/neato/fdp/sfdp) verdicts BYTE-IDENTICAL to committed parity baselines; only 1855 circo changed. | src/layout/neato/set-aspect.ts, .agent-notes/1855-rca.md | tsc 0; 298 targeted tests pass; full sweep TBD |
| 2026-07-11 | **shapes-family circo/osage FIXED: 2 poly_init FP-ULP sites** | Visible diffs (circo 700/666, osage 351/338/292) = connected-component packing-cell SWAP, not clipping/routing/legal. Within-component geometry rigid-identical (node deltas exactly 0 or ±(182,338)); two components trade polyomino cells. Root: poly_init ND_ht was 36.000000000000007105 vs C exactly 36.0; pack.c genPoly `H=GRID(=ceil((ht+2margin)/step))` rounded 52.0000..1/26 up 2→3, inflating perim=W+H, breaking a perim TIE C has (_cc_0==_cc_3==33) → unstable qsort reorders → swap. Proven by patching native lib/pack/pack.c genPoly to dump raw GD_bb (%.20g). Two ULP sites in poly-sizing.ts transformUnitVertex/polygonVertices: (A) `Math.hypot(D.x,D.y)`=0.5+1ULP where libm hypot AND `sqrt(x²+y²)`=0.5 (diamond/n-gons); (B) unit-vertex accumulation `R.y+=sidelength*sin` — clang -ffp-contract fmadd = single rounding, JS += two roundings drift 1 ULP (house/invhouse distorted 5-gons); fixed via existing fma.ts. Both feed single vertex source → one edit fixes sizing+geometry. A9-irreducible REFUTED (C is exactly 36.0; both ops reducible). polypoly (osage/patchwork) SAME family, partially fixed 89→24/106→12/106→12, residual = distinct bisector/skew/peripheries ULP (out of scope, touches patchwork). | src/common/poly-sizing.ts | 5 targets pass 0.01; regr circo 15/osage 15/dot 25 =0; ALL 70 shape-reference ids under dot PASS; tsc clean; 820 unit tests pass |
| 2026-07-11 | **1855 residual #2 root-caused: twopi PRISM radial mirror = A1 (irreducible)** | The 842-diff residual after the `size`-ROUND fix is a full x-axis MIRROR of the oracle's radial layout (`port_pos(k)=y_negate(C_pos(34−k))`, max node disp 6.04pt; bb matches so it hid behind an SVG-shape check). Instrumented both sides at `%.17g`: circleLayout `theta`/`span` bit-identical (0 ULP), Delaunay edge set identical (62), PRISM stress iter 0 identical to 5e-15 — the ONLY input delta is **1-ULP V8-vs-libm `cos`/`sin`** in `setAbsolutePos` at 5 of 31 leaf angles. The 31 leaves are exactly cocircular → the star is reflection-symmetric → PRISM overlap-removal (`removeOverlapPrism`) sits on a symmetry-UNSTABLE equilibrium: iter 1 sign-flips ΣY (C `+1.99e-3` vs port `−1.99e-3`, exact opposite) and the 1-ULP asymmetry selects the opposite mirror basin. Injection A/B: (A) feeding C's exact circleLayout pos into the port's PRISM reproduces the oracle **node-for-node (3e-14)** — PRISM/stress/CG EXONERATED; (B) restoring only the 5 ULP-divergent nodes to V8 values flips the whole layout to the mirror (0.0000pt from port, 6.04pt from C). Irreducible without libm-CR `cos`/`sin` (CR-hypot already REFUTED). Same class as the twopi arrows family. NO code change. Ruled out: port PRISM bug, Delaunay tie, iteration/summation order, circleLayout misport. Accept as **A1** engine-track; proposed `accepted-divergences-engines.json` entry + `known-divergences.md` prose in `.agent-notes/twopi-radial-drift-rca.md` (registry/docs edits are outside this task's write-set). | .agent-notes/twopi-radial-drift-rca.md (RCA only; no src change) | tsc clean; C tree clean; injection A=3e-14, B=mirror; 1855 unchanged 842/pass=false pending A1 accept |
| 2026-07-11 | **twopi ortho label-family: M1 port-label shift FIXED (144_no_ortho→conformant), M2 ortho routing DEFERRED** | Prior "all ortho" attribution wrong: node positions match oracle EXACTLY for all 4 ids (144_ortho, 144_no_ortho, 2361, 2183) → none is A1 drift. **M1 (FIXED):** twopi's `normalizeGraphBB`→`shiftOneGraph`→`shiftEdgePoints` (pack/index.ts:189) translated only `e.info.label`, omitting xlabel/head_label/tail_label — so when the label-expanded drawing is shifted to origin (+6.822 for 144), head/tail labels are left behind (`_hldraw_` x=46.05 vs oracle 52.87). C reference `pack.c:shiftEdge` moves ALL FOUR label kinds; port omitted three. Fix = add them (mirrors shiftEdge order). 144_no_ortho diverged→pass; 144_ortho 14→8 diffs (all label diffs gone). Instrumented both sides: port pe.x=27 (pre-shift) at place_portlabel, node/spline/label should move together via C map_edge — port's normalizeGraphBB zeroed bb.ll so gvPostprocess Offset=0 and translateDrawing (which DOES map head/tail labels) early-returned. **M2 (DEFERRED, not irreducible):** 2361(184)/2183(79)/144_ortho residual(8) = real ortho routing divergence — 2361 AC->CI vertical channel x 289.16 vs 282.77 (+6.39 maze track selection, identical node box); 144_ortho D->C port emits duplicated ortho controls [P0,P0,P1,P1] via buildSpline (ortho/index.ts:67) while oracle emits interpolated fit → ortho-vs-straight dispatch/fit diff. Needs dedicated sweep-gated ortho mission (700+ passing ortho ids at risk); NOT an acceptance class. RCA: .agent-notes/twopi-ortho-family-rca.md. | src/layout/pack/index.ts (shiftEdgePoints) | tsc clean; unit 141 + golden/divergence 339 green; twopi 12/12 + osage 6/6 + circo 6/6 spot-check no regr; 144_no_ortho→pass |
| 2026-07-11 | **circo/twopi edge-tail RCA: 1856 FIXED (ortho ports); 5 ids attributed** | Six ids, five mechanisms; all node positions bit-identical to oracle (pure routing/emission, not placement). **1856 FIXED**: ortho router dropped the compass/record port offset — `OrthoHelper.buildEdges` built the `TaggedOrthoEdge` without `tailPoint`/`headPoint`, so `buildSpline` fell back to the bb centre and every ported ortho edge started/ended at the node centre instead of C's `ND_coord + ED_*_port.p` (attachOrthoEdges, ortho.c:1075-76). Fix plumbs `coord + port.p` (also strictly more faithful for lw≠rw). 1856 conformant; all 17 `splines=ortho` corpus inputs pass/unchanged under dot (2538/2620 pre-existing per stash baseline); 12/12 sampled circo ids unchanged. **1990/2082 OPEN (real bug)**: `splines=ortho` no-port, both PASS under dot — only circo's coordinate distribution + fan-in topology triggers an ortho maze channel/segment-assignment divergence (native packs ~6 segs/channel where port routes 1 → htrack/vtrack shift 30-58pt; node boxes bit-identical). Localized to src/ortho/{partition,maze,maze-channels,ortho-route}.ts; NOT ULP/ports; filed for dedicated work. **windows-tree = A9**: circo trig-ULP lands node2.y=18.0000000000000320 vs node8.y=18.0 (Δ3.2e-14 around symmetric 18.0) → exact-tie `closestSide` picks TOP(port)/BOTTOM(native) for the dyna record head-port (controlled exact-coord dump). **b786 = emission**: geometry bit-identical (_draw_ 1399.25 both); only `gfmt5` `%.5g` via `toPrecision(5)` rounds exact-halfway 1399.25 half-AWAY→"1399.3" vs C printf half-EVEN→"1399.2"; fixable in gfmt5 (cf. toFixed2HalfEven) but blast radius = every coord → deferred to its own sweep. **twopi/241_0 = A9**: CDT cocircular incircle tie (sibling of already-accepted circo/241_0), 6 diffs edge 1->6 ptCount 14v8, deltas <0.07pt. Acceptance-registry/known-divergences edits are outside this task's write-set — proposals in `.agent-notes/circo-edge-tail-rca.md`. | src/layout/neato/splines.ts, .agent-notes/circo-edge-tail-rca.md | tsc 0; npm 2934/2934; 1856 conformant; 0 regressions (17 ortho-dot + 12 circo spot) |
| 2026-07-11 | **patchwork+osage tail: 1898, 2717, polypoly (2 fixed + 1 accepted A9)** | (1) **1898** N19 box drawn under patchwork: C `gvrender_set_style` (gvrender.c:497) maps BOTH `invis`+`invisible`→PEN_NONE; port honored only `invis`. Added `styleHasInvisibleAlias`; non-point node/edge early-return also fires on `invisible` (net xdot = C PEN_NONE path). Point shape UNCHANGED (checkStyle exact-`invis` whitelist → still draws). 0 diffs dot/patchwork/osage. (2) **2717** `domestic_cats` (`cluster=true`, no name prefix) dropped from patchwork+osage cluster lists: both engines' `isCluster` did name-prefix only, not C `is_a_cluster` (utils.c:695 root‖prefix‖mapbool(cluster)). Fixed both; 0 diffs patchwork+osage (dot/neato/circo/twopi already used rank.ts isACluster). (3) **polypoly patchwork** 9012/9016: patchwork forces shape=box (distortion attr unread), so orient 90/270 boxes have CW winding — C's bisector periphery loop steps INWARD (j0 outer, drawn first); port `boxRings` used fixed insets (always inner-first) → 2 rings reversed (Δ3.51=GAP). Routed boxRings through the same winding-aware `polygonRingOffsets` walk C uses (proto matched C dump bit-for-bit all 4 orientations); orient 0/180 provably identical to old insets, no-base path unchanged. 0 diffs ×3. (4) **polypoly OSAGE** 9000-series distorted-quad pack-cell swap = **A9 IRREDUCIBLE**: sole diverging op is `cos(alpha)` at orient 180/… where V8 Math.cos is correctly-rounded but Apple libm cos is ±1 ULP arg-dependent (alpha, atan2, sqrt==hypot, sin all bit-identical). No fma/plain-double recovers a bare transcendental's rounding; `-cos(θ)` gives libm's 9000 value not 9004's. Bound ≤20 diffs/id, whole-node rigid swaps, osage-only. RCA .agent-notes/patchwork-tail-rca.md; registry entry (accepted-divergences-engines.json osage) is follow-up (src-only write-set). | src/common/style-resolve.ts, src/gvc/device.ts, src/common/poly-gencode.ts, src/common/poly-vertices.ts, src/layout/patchwork/index.ts, src/layout/osage/index.ts, src/layout/osage/osage.test.ts | tsc clean; vitest 2935/2935; fixed ids 0 diffs; polypoly osage 20/12/12 accepted A9; sweep pending |
| 2026-07-11 | **ortho maze circo/twopi divergence: TWO root causes FIXED (5 ids conformant)** | Cracked the prior-DEFERRED "M2 ortho routing" / "1990/2082 open" bucket. Cell-by-cell dump (gvmine ORTHO_DUMP, both sides) proved partition cells + sgraph sedges (v1,v2,weight,cnt) + snodes BYTE-IDENTICAL for 1990/circo; routes i0–i5 matched, i6+ the port found HIGHER-cost Dijkstra paths (995 vs port 1140) on a byte-identical graph. Reference Dijkstra on C's own dump = 1140 → the PORT was OPTIMAL and C's 995 used a phantom edge (`N_DAD(588)=275` inconsistent with `N_EDGE(588)=(588,573)`). **RC1 (sgraph.c:initSEdges):** C's `adj_edge_list` is ONE contiguous buffer with a FIXED 6-int region per regular snode; `addEdgeToNode` writes at `[n_adj++]` with NO bound, so a node with >6 incident edges (390/588 snodes are at n_adj==6 at construction under circo fan-in) SPILLS a temp edge index into the NEXT node's region — and the spill SURVIVES `reset` (restores only n_adj), a load-bearing overflow that lets C's shortPath traverse cross-cell phantom edges (the "6 segments packed in a channel"). The port used per-node growable `number[]` → never spilled → true-optimum → diverged. Fix: `initSEdges` allocates one shared `Int32Array(6*nnodes+2*maxdeg)`; each `adjEdgeList` = `buf.subarray(offset)` (view to buffer END) → forward spill + cross-reset persistence reproduced exactly. Fixed 1990/2082/2361/144_ortho→0, dropped 2183 79→24. **RC2 (ortho.c:chancmpid):** 2183 residual = edge a->b htrack y=36 (commCoord fallback) vs 54; `chanSearch` returned null because a->b's segment `[-36,48.18]` extends BEYOND its channel (seg⊇chan). C's `chancmpid` treats intervals equal when EITHER contains the other; port's `chanSearch` only tested chan⊇seg. Fix: port `chancmpid` verbatim, match on `chancmpid(chan.p,seg.p)===0`. 2183→0. 144_ortho "duplicated controls [P0,P0,P1,P1]" = downstream of the wrong route (C's attachOrthoEdges builds the SAME duplicated ispline); route now matches → resolved. Ruled out: partition/trapezoid (cells identical), weights (identical entering i6), temp edges (identical), fPQ (reference Dijkstra confirmed port pre-fix was optimal), edge sort, node pos (bit-identical). NOT ULP/A9 (18–58pt, two concrete misports). | src/ortho/sgraph.ts, src/ortho/types.ts, src/ortho/maze-channels.ts, .agent-notes/ortho-maze-circo-rca.md | tsc 0; npm 2934/2934; 5 targets 0 diffs; 0 regr (17 ortho-dot + partial circo 444/twopi 507 sweeps + 17 ortho×4-engine stash A/B; all non-zero cases pre-existing) |
| 2026-07-11 | printf-round tie-fix (gfmt5 + printDouble) | **Mechanism:** `gfmt5` (src/render/dot.ts, %.5g DOT/xdot `pos`/`bb`/`width`/`height`) used `v.toPrecision(5)`, which rounds exact binary ties half-away-from-zero (1399.25 -> "1399.3"); C printf %.5g rounds half-to-even under FE_TONEAREST ("1399.2"). Same class already partly patched ad hoc in `toFixed2HalfEven` (job.ts, %.2f `printDouble`) via an n*8/n*4 integer-tie heuristic specific to 2 decimals. Replaced both with one general helper `src/util/printf-round.ts` (`printfFixed`/`printfSig`): decomposes the float64 bit pattern into an exact integer mantissa + binary exponent, converts to an exact decimal digit string via BigInt scaling (2^k traded for 5^k/10^k — no epsilon nudging), and rounds that exact digit string half-to-even at the target position. `toFixed2HalfEven` now delegates to `printfFixed(n,2)`; `gfmt5` calls `printfSig(v,5)` then keeps its existing %g-style trailing-zero/exponent post-processing unchanged. **Verification:** graphs-b786/circo 2 diffs -> 0 (pos "1399.3"->"1399.2" both endpoints); 400k random non-tie samples (fixed+sig) byte-identical to native toFixed/toPrecision; 15/15 sampled passing circo ids + 15/15 sampled passing dot-track ids unchanged (0 diffs). **Ruled out:** layout/routing (b786 draw-op geometry bit-identical before and after — only the `pos` attribute string changed). | src/render/dot.ts, src/gvc/job.ts, src/util/printf-round.ts, src/util/printf-round.test.ts | tsc clean; vitest 2957/2957 (222 files); golden 331/331; b786/circo 0 diffs; 15+15 spot-check 0 diffs |
| 2026-07-11 | **states family ×3 FIXED: polylineMidpoint fp-contract (verdict (b), NOT drift)** | The open question from the "addXLabels object ORDER" entry resolved to a FIXABLE op-order diff. Two corrections to the prior attribution: (1) these edges are EDGETYPE_LINE — C's et constants are `<<1`-shifted (const.h:235, EDGE_TYPE macros.h:25), so C's dumped et=2 ≡ port et=1, BOTH take polylineMidpoint, NOT dotneato_closest; (2) the 1-ULP anchor is C's `-ffp-contract` fusing the FIRST product in `mf.x=(qf.x*dist+pf.x*(d-dist))/d` (splines.c:1271) into fmadd. Bit-level A/B (%.20g probes both sides, reverted): control points feeding polylineMidpoint BIT-IDENTICAL on states — rules out (a) drift; emulated `fma(qf,dist,pf*(d-dist))/d` reproduces C's anchor on ALL 5 edges (plain IEEE only 3/5); the dispatch edge's 1-ULP anchor x tips the xlabel side-selection knife edge → label flips left→right → 4.5pt canvas shift → 77 diffs/id. FIX: route the interpolation through common/fma.ts (same A8 class as poly-sizing). graphs/share/windows-states twopi → 0 diffs. BLAST RADIUS: full twopi sweep 735 pass/16 div (was 730/21): also FIXES accepted-A9 ids 2343, share-b29, windows-b29; FLIPS OUT graphs-b29, linux.i386-b29 with the identical 1-diff signature — proven NOT the midpoint op (fma anchor is bit-identical to C on b29 yet the label still flips; b29 control points DO drift 1 ULP) = placeLabels tie on drifted surroundings, same A9 class; registry swap proposed in .agent-notes/twopi-states-rca.md (registry outside write-set). Spot-checked at-risk ids (baseline=pass): circo 13/13, osage 12/12, patchwork 14/14, neato 2361, dot 235-id partial sweep + 11 spot ids — all pass; dot/linux.i386-b29 diff is the PRE-EXISTING accepted xdot entry (same firstDiff/Δ); 1367+decorate A/B-identical with/without fix (pre-existing, untracked set). Also documented: port emits no `lp` for xlabel-placed edge labels where native does — comparator does not surface it (emission gap, follow-up). | src/common/spline-midpoint.ts, .agent-notes/twopi-states-rca.md | tsc 0; npm 2935/2935; states ×3 = 0 diffs; twopi full sweep +5 net; C tree reverted+rebuilt |
| 2026-07-11 | **circo b81 + 2475_2 attributed (both ACCEPTANCE, no src change)** | Last two un-attributed circo (±0.01) divergences; node layout bit-identical in both. **graphs-b81 (785.47pt whole-canvas x-shift, 5143 diffs)**: every node shifts dx=+785.47 dy=0 (spread 0.01) — pure translation, spacing/right-margin identical → LAYOUT bit-identical; the 785pt is extra LEFT margin. b81 is a doxygen dump whose 3 edge labels are 32000-36000pt wide (~6× node width; one `\n`-segment is 5990B). circo hands unset edge labels to the greedy xlabel placer (xlabels.c placeLabels/xladjust, anchored at edgeMidpoint, forcelabels default true → dumps at least-overlap candidate). Port picks LEFT candidate, native MID/RIGHT for the oversized labels: at IDENTICAL anchor+size (edge 39->40 LEFT-top) port xlintersections n=87/area=7.9M vs native n=90/20.2M → LEFT looks least-bad to port. One oversized label flipped LEFT reaches raw x≈0, extending raw bb ~785pt left → translate shifts all nodes +785.47. Recording logic (recordointrsx/getintrsxi/aabbaabb/objplp2rect) matches C line-for-line; the residual driver is the label-placement SEQUENCE — port runs the placer 103× vs native 101×, first 9 identical then order+membership+some edgeMidpoint anchors diverge → order-sensitive overlap accumulation (each placed label is an obstacle) picks the other side. NOT a single ULP; reduction needs bit-exact edge-enumeration + spline-midpoint + R-tree-leaf-order matching (broad blast radius across every xlabel engine, uncertain float-tie convergence) → **acceptance A-xlabel-placer**. **2475_2 (989 diffs) = A9 hypot-ULP**: of 10762 nodes only 18 move, ALL in one 28-node `590_` component (max 296.7pt), sub-block reflected/rotated. Isolated via standalone repro (.agent-notes/circo-2475-590-repro.dot) + block-by-block circo probes (reverted): circle-lists identical, every block centerAngle identical to 6 digits — divergence is in getRotation→closestNode (circpos.c:73-92) for the len=9 block. At the BIT-IDENTICAL placement offset (x=13.677942523567673,y=0) the CHILD-neighbor 590_174492 dist = 11.078849186321314 on both sides, but cocircular node 590_174423 rounds to 11.078849186321312 in V8 Math.hypot (2 ULP low, strictly <) vs ≥...314 in Apple libm (tie kept). Port therefore picks a different closest_node → takes the rotation branch (rot ≈0.349rad ≈20° vs native 0) → sub-block rotates/reflects. Same class as accepted twopi/1855 A1 + circo A9 trig-ULP; CR-hypot already REFUTED. NO code change. Full RCA + accepted-divergences-engines.json drafts + known-divergences prose in .agent-notes/circo-b81-2475-rca.md (registry under test/corpus/ is outside write-set). Ruled out for 2475_2: block tree, mincross/circle order, child angular placement, closestNode iteration-order (offset+neighbor dist bit-identical), node-position bug. | .agent-notes/circo-b81-2475-rca.md, .agent-notes/circo-2475-590-repro.dot (RCA + repro only; no src change) | tsc 0; C tree clean; probes reverted; both node layouts bit-identical to oracle |
| 2026-07-11 | **circo b81 xlabel placer: prior "acceptance" SUPERSEDED — FIXABLE misport (signed R-tree key), 0 diffs** | The prior b81 entry classified this as irreducible "A-xlabel-placer" with a wrong "103 vs 101 placer invocations" lead. Re-instrumented `placeLabels` (XL_DUMP, reverted): BOTH sides call the placer **132×** on the same 265 objects; object membership/order/anchors/label-sizes **all identical**; 29/132 placements differed ONLY in overlap count `bp.n` at the SAME `bp.pos`. First divergence #11 (i=23): native `bp.n=0` vs port `bp.n=2` at identical rect `[16047,417,17079,433]`, identical obstacle set. Split `bp.n` by source (lblenclosing vs RTreeSearch): both `nEncl=0` → divergence is entirely in **`RTreeSearch`**. Native `RTreeSearch` returned NULL, yet a brute-force scan of native's own `spdx` found 11 overlapping maximal-boxes incl. the exact 2 nodes (k=22,k=62) the port found, **bit-identical positions** → native's node geometry matches; native's R-tree MISSES overlapping leaves that are in it. Tree dumps: native root 15 branches vs port 26 — different trees; native's root MBRs leave an x-gap [15464,22823] uncovered while leaves x≈16038-17050 live under those branches (**parent MBRs under-cover children** → `RTreeSearch` prunes real overlaps; port built covering MBRs → found them). Insertion-order dump (`xlspdxload` over the Hilbert bag): key **multiset identical**, **order different** — native started at key=2868726692, port at 84482 (ascending unsigned). **ROOT CAUSE:** `xlabels.h:77` declares `HDict_t.key` as SIGNED `int` and `icompare` compares signed; `hd_hil_s_from_xy` returns UNSIGNED, so keys ≥2³¹ store negative and sort BEFORE positive → specific (under-covering) tree the placer depends on. Port stored the code unsigned (`DtBag (a,b)=>a-b`) → different tree → different overlaps → oversized label flips LEFT vs RIGHT → raw bbox +785pt → uniform +785.47 x-shift (5142 diffs). **FIX:** `src/label/xlabels.ts` xlhdxload — `hdHilSFromXy(...) | 0` (ToInt32, matching C's unsigned→int store). After fix: insertion order rect-for-rect IDENTICAL to native; tree matches; placement 131/132 bit-identical (1 residual has identical `bp.pos`+`area=0` → identical placed position, immaterial). Ruled out: membership/order/anchors/label-size (all identical), lblenclosing (nEncl=0 at #11), the float intersection (`aabbaabb` operates on rounded rects, inputs identical). **Verification:** b81/circo 5142→**0 diffs, maxΔ 0.00**; 28/28 real graphs pass circo+twopi; b-series (b3,b7,b33,b51,b56,b71,b77,b80) pass circo; twopi accepted b29/linux.i386-b29/2239 UNCHANGED (Δ12/12/10.8 — small-canvas keys <2³¹, fix is a no-op there → not stale); side benefit: b29/linux.i386-b29/2239/states now pass under circo. tsc clean; label tests 69 pass; accepted-divergences test 6 pass. RCA: .agent-notes/b81-xlabel-placer-rca.md. | src/label/xlabels.ts, .agent-notes/b81-xlabel-placer-rca.md | tsc 0; label vitest 69/69; accepted-div 6/6; b81/circo 0 diffs; 28/28 circo+twopi spot; twopi accepted unchanged; C tree reverted+rebuilt clean |
| 2026-07-11 | **osage "spline family" 1855 root-caused IRREDUCIBLE A9; 1447/1447_1/b106 re-attributed to distinct paths** | Task framed 1855/1447/1447_1/graphs-b106 as one obstacle-spline family; REFUTED — three unrelated routing paths, only 1855 is visibility/obstacle-spline. **1855 (110 diffs = 3 edges 6->1/16->1/30->1):** node centers already bit-exact; diffs are obstacle-routed beziers with X bit-exact and Y **mirrored** (6->1 429.120 vs 385.608). First diverging quantity = the `Pobspath`/getPath routed polyline (NOT the spline fit) — port routes the node-row gutter on the opposite side. Dumped both sides: `conf.P` (256 obstacle vertices) matched to 3 decimals; `shortestPath` faithful; but the vis matrix has ~20 EXTRA port edges, all **vertically-collinear gutter pairs**. `compVis` sub-results: `inCone` agrees, `clear()` diverges — C's gutter-column corner-x are ALL the bit-identical double `156.62317307356111` (exact collinearity → `intersect` area==0+inBetween → tangent-blocked), port's column has TWO ULP-differing x (`...108`/`...110`) → not collinear → not blocked → extra edges → Dijkstra flips the up/down homotopy tie to the mirror side. Origin: octagon obstacle vertices (`circumscribed_polygon_corner_about_ellipse`); raw polyp differs 3-4 ULP. **cos/sin ruled OUT** (injecting C's exact libm cos/sin for the 8 angles is a bit-for-bit no-op — 1-ULP trig washes out under ×b); the smear is **clang `-ffp-contract=on` FMA** fusing `a*b±c` in `ellipse_tangent_slope`(`a*a-p.x*p.x`)/`line_intersection` while V8 rounds each op → C's fused rounding collapses the column to one double, port's splits it. **Controlled experiment:** injecting C's exact obstacle coords into the untouched port (OSAGE_CINJECT) → **0 diverging edges** — proves legalArrangement/visibility/Dijkstra/Proutespline all faithful; only obstacle-vertex FP diverges. Sibling of accepted circo/241_0 + osage/*polypoly A9. Ruled out: Dijkstra tie-break, obstacle ORDER (position-identical), node centers, spline fitter. **1447/1447_1:** `splines=ortho` → maze path (Pobsopen gated off for ortho), NOT obstacle-spline. 1447 node-exact, 4 edges consistent port=oracle+1pt Y — ortho maze cell/track internal (boxes geometrically identical, only CW start-vertex rotation differs, irrelevant since maze reads coord/xsize/ysize); likely 2620-class A8, not yet pinned to file:line — own chase. **graphs-b106:** no splines attr → default LINE edges; bezier structure matches, 48 diffs are label `_ldraw_` T positions — label-placement chase (likely tied to parallel xlabels.c work). NO src change (1855 accepted; others need own chases). Acceptance JSON draft + per-id table in .agent-notes/osage-spline-family-rca.md. | .agent-notes/osage-spline-family-rca.md (RCA only; no src change) | tsc 0; worktree src clean (all probes reverted); C tree clean for my 3 files (neatosplines.c/pathplan cvt.c/visibility.c); 1855 C-obstacle injection A/B = 0 diffs |
| 2026-07-11 | **osage small-diff tail: 5 FIXED (2 mechanisms) + 3 ACCEPT A9** | **printf-round non-finite regression (1332/2721/1221 FIXED):** commit 931b1b6 replaced `toFixed`/`toPrecision` with BigInt bit-decomposition helpers (`printfFixed`/`printfSig`) that had NO non-finite guard. Degenerate/empty osage clusters have `±inf/nan` bb, so the `_draw_` polygon coords are Infinity/NaN; the helper read the all-ones exponent bits as a finite mantissa and emitted a ~309-digit integer (was `Infinity`/`NaN`, which the comparator canonicalizes to the oracle's `inf`/`nan` → previously passed). Bisected to 931b1b6 by rendering 2721's `cluster_uni` `_draw_` at 4 commits. FIX: `!Number.isFinite(v) → v.toFixed/​toPrecision` in both helpers (restores the exact drop-in contract). gfmt5 already guarded, so only the `xdotNum`/`%.Nf` path was hit. **polyRects genBox CVAL/round misport (pack_neato2 ×2, 453→0 FIXED):** `packmode=graph` + non-cluster `{}` subgraphs → osage packs 24 identical node-rects via `polyRects` (l_graph). C `genBox` maps corners with `CELL`=`CVAL` on the DOUBLE pointf then a separate `round()`: `CVAL(-4,5)=((-4+1)/5)-1=-1.6→round -2`, `CVAL(58,5)=11.6→12` (15×11=165 cells). Port `poly-pack.ts` `cval` used truncating/int semantics + NO round: `-1`/`11` (13×10=130 cells) → every box lost a cell on the negative side → greedy spiral packed tighter → wrong cell assignment + 35pt-smaller canvas. Instrumented C `-v2/-v3` (pos[]/W/H/nc) + port `PACK_DUMP` (reverted): spiral+tryCenter faithful, only the footprint diverged. FIX: faithful double `cval` + `cround` (half-away) + genBox rounds corners, center-relative LL/UR, `cround(cval())` per corner — exactly `pack.c:genBox` (matches the already-faithful `poly-place.ts`/polyGraphs copy). Both ids → 0 diffs. cval/cround unit tests updated (old `cval(-3,3)=-1` asserted the bug; faithful `-1.667`). **A9 (linux.i386-b29/share-b29/1652):** placeLabels knife-edge — edge spline `_draw_`+arrowhead `_tdraw_` BIT-IDENTICAL, only the label anchor flips symmetrically about the identical spline midpoint (±half the label width); mirror variants swap sides; 2 diffs in 136–240KB graphs, every other label bit-matches; same class as accepted twopi/b29. Registry entries drafted under accepted-divergences-engines.json → osage. Ruled out for pack: qsort tie-order (idx23 placed first on both sides), spiral (verified seg-by-seg vs C). | src/util/printf-round.ts, src/util/printf-round.test.ts, src/layout/pack/poly-pack.ts, src/layout/pack/poly-pack.test.ts, src/layout/pack/pack.test.ts, test/corpus/accepted-divergences-engines.json, .agent-notes/osage-small-tail-rca.md | tsc 0; pack+printf vitest 103/103; 5 targets 0 diffs; fresh osage sweep 0 regressions (see report) |
| 2026-07-11 | **twopi/2470 "regression" = NOT a regression; irreducible A1/A9 xlabel knife-edge (no src change)** | Reported twopi/2470 pass(14:22 sweep)->diverge(16:33 sweep, 298 diffs). ROOT: the native oracle binary was **rebuilt at 15:24** (between the two sweeps); the port's 2470/twopi xdot is **byte-identical across the ENTIRE candidate window** (2fb8e79 base + 1b33154/015ba47/77e9e30/92e0d2a/c1c93a2/931b1b6/08d57bc/04e6b6e/10f35c6 all = 298 diffs, first diff bit-identical). NO commit changed it. The morning "pass" was against a stale/non-canonical oracle (pre-15:24 build; only an inert `getenv("OSAGE_PROBE")` probe now dirties neatosplines.c). Current oracle = canonical = faithful; the port diverges. MECHANISM: all 298 diffs are edge-label `_ldraw_` positions (HTML `<td align=left>LOOKBACK` labels, ~140 near-coincident radial anchors); 0 spline/node/bb diffs even @1e-7. Neato-family edge labels are placed as xlabels (postproc.c:addXLabels -> label/xlabels.c placeLabels/xladjust, Hilbert R-tree). Patient-zero (matched XL_DUMP probes both sides, reverted) = placement seq=8: identical anchor, seq 0-7 placed identically; at the x-left candidate C's RTreeSearch returns 26 leaves/n=5 (rejects), port's returns 0/n=0 (takes it) — port brute-force finds the same 26 overlaps, so its R-tree SEARCH misses real overlaps (C root 19 branches w/ an MBR covering the high-y cluster vs port 22 branches, none reaching y>805). Traced to R-tree INSERTION ORDER: objplpmks rect y-low differs (C -1, port 0). Full-precision A/B in addNodeObj (reverted): EVERYTHING bit-identical except node n17's `ND_coord.y` = 79.5999999999999090 (C) vs 79.6000000000000369 (port), ~1.3e-13 (~2 ULP) — a twopi radial libm sin/cos vs V8 Math coordinate, below the conformance bar (node emission bit-matches @0.01 and @1e-7). `floor(coord.y - sz.y/2)` straddles EXACTLY 0 -> rect y-low flips -> Hilbert key -> insertion order -> tree grouping -> RTreeSearch pruning -> xlabel candidate flip on ~140 labels (each a fixed +width,-16.8 step). CONTROLLED EXPERIMENTS: sparse HTML edge labels = 0 diffs (placer faithful); rankdir TB/RL both 298 (not rankdir); byte-identical across commits (not a fix regression). Ruled out (all faithful): AGSEQ order, objp2rect/objplp2rect round (positive coords), CombineRect (C's min-min quirk mirrored), PickBranch/RectArea u64 wrap, int32 Hilbert key (04e6b6e). CLASSIFICATION: irreducible A1/A9 — same basis as accepted twopi/1855 (radial cos/sin ULP) + the b29 placeLabels-tie family; no deterministic rewrite reproduces the libm coordinate. RESOLUTION: NOT condition-1 (no commit to revert; the "fix" would require bit-exact libm radial trig). Acceptance JSON draft (twopi/2470, A9) + known-divergences prose in .agent-notes/twopi-2470-rca.md (registry + docs outside this task's write-set). C tree reverted + rebuilt clean. | .agent-notes/twopi-2470-rca.md (RCA + acceptance/prose drafts; no src change) | tsc 0; port src unchanged; C tree reverted+rebuilt clean; 2470/twopi 298 diffs vs canonical oracle |
| 2026-07-11 | **1447 FIXED (osage splines=ortho): htrack negative-half round()** | 4 diverging edges, all routing the bottom gutter, showed final Y = oracle+1 (30/29, 3/2, 12/11, 21/20). First diverging quantity pinned by matched C+port instrumentation: the pre-clip maze route bottom-track Y. C `htrack` (ortho.c:1065) uses libm `round()` (half-away-from-zero); port used `Math.round` (half-toward-+inf). The maze routes in a frame whose bottom/left gutter cells sit at negative coords (LL.y=-36), so evenly-spaced tracks land on exact NEGATIVE half-integers (-4.5/-13.5/-22.5/-31.5). Both sides identical inputs (lo=-36,hi=0,f=0.875,trackNo=2,n=15) → raw=-4.5; `round(-4.5)=-5` but `Math.round(-4.5)=-4`, +1 after the origin translate. FIX: local `cround` helper in ortho-route.ts htrack (matches C round; vtrack correctly has no round, unchanged). **1447 osage 52→0 CONFORMANT.** Ruled out: track_no/seg_list/cell-bb (all bit-identical port vs C-instrumented), node centers (already bit-exact), translate (uniform +34, identical). Regression guard: 16 splines=ortho ids re-swept (2361/14/2168_5/56/2082/1408/1990/2643/2183/1880/144_ortho/1658/1856/2620 all 0 diffs; 2538 8 diffs but IDENTICAL with/without fix = pre-existing back-edge issue) + 15 osage pass-ids 0 regr; ortho unit 78/78. 1447_1 (compress) NOT this family — see next row. | src/ortho/ortho-route.ts | tsc 0; ortho 78/78; 1447 conformant; 0 regr |
| 2026-07-11 | **graphs-b106 (osage) ROOT-CAUSED: xlabels R-tree cover-invariant violation — reducible, not fixed** | 51 diffs = edge-label `_ldraw_` T positions (splines byte-identical to oracle). osage places UNSET center edge labels via addXLabels->placeLabels (postproc.ts:419). Anchor (edgeMidpoint=polylineMidpoint) AND label size are BIT-IDENTICAL to C for the 5 diverging labels (NOT the polylineMidpoint fmadd class, NOT a size bug). Candidate order in xladjust IDENTICAL to C. FIRST diverging label i=99 (Node1315->Node1312): all 99 prior labels place identically -> occupancy identical -> LOCAL divergence, not a cascade. Port places at xL (clear); oracle at xR (C saw an overlap at xL port missed). Brute-force at the xL rect [695,22,706,34] finds obj41's label (a=48) but port rTreeSearch returns 0 leaves. Tree walk: obj41 leaf rect [596,21,700,70] is correct, but its PARENT internal-node MBR is [20,21,125,70] — does NOT enclose the leaf (x 125<700): R-tree COVER INVARIANT VIOLATED -> Overlap(query,parent)=false -> subtree pruned -> obj41 missed. The under-coverage is a latent consequence of CombineRect's high-side `fmin` (rectangle.ts) — FAITHFUL to C (rectangle.c:95 same fmin). RTreeInsert2/RTreeSearch/Overlap/icompare/Hilbert-|0/Dtobag all individually faithful to C. So C's tree (same objects+logic) must NOT have the violation for obj41 (oracle places correctly) => the port's R-tree TOPOLOGY diverges from C — in Hilbert-bag insertion order and/or node-split partitioning (both use the faithful-but-lossy CombineRect for area ties). REDUCIBLE faithfulness gap, NOT A9 — no acceptance filed. Pinning needs native rtree instrumentation (index.c/node.c/split.q.c insertion+MBR dump vs port) — deferred: races concurrent xlabels.c work + broad multi-engine blast radius. Full RCA + handoff step in .agent-notes/osage-1447-b106-rca.md. 1447_1 (ratio=compress) also NOT the round fix — separate compress-amplified maze/track divergence (vtrack X diffs, no rounding), own chase. | (diagnosis only) | 1447 conformant; b106/1447_1 RCA'd |
| 2026-07-11 | **json parity track LANDED (baseline survey)** | New "dot (json)" differential track mirroring the xdot track. ORACLE FLAVOR: `-Tjson` (not `-Tjson0`) — smoke A/B on `digraph G {a->b}` shows the port emits the FORMAT_JSON `doXDot=true` shape (`"_draw_": []` on every node/edge; json0 never emits `_draw_` at all). Comparator (compare-json.ts) keys objects `[graph]`/`cluster:<n>`/`node:<n>`/`edge:<t>-><h>#<i>` (edge refs resolved via `_gvid`→name), compares draw-op ARRAYS natively (op letter exact, numeric @0.01, colors/fonts via compare-xdot's canonColor/canonFont), positional strings (pos/bb/width/height/lp/lwidth/lheight) numeric @0.01, generic attrs exact; subgraph member lists (nodes/edges int arrays) not deep-compared — port emits 0 subgraphs so `cluster:*` missing-object already surfaces the gap. Walker json-walk.ts = xdot-walk.ts clone (parity.json conformant set 762, size-sorted, sha1(bin,GVBINDIR,json,mtime) oracle cache, group-killed port subprocess; 241_1/perf ids already excluded by the conformant filter). FIRST SURVEY: 0 conformant / 761 diverged / 1 timeout (1652, known perf-slow). Buckets by firstDiff: 400 `[graph]/_draw_[missing]` (port emits NO graph-level attrs: _draw_/bb/xdotversion all absent), 304 `[graph]/_subgraph_cnt` (port hardcodes 0; no subgraph/cluster objects), 56 `[parse]/port` (port leaks cgraph 0x01 sentinel bytes into edge attr values, e.g. 2743 l/0 attrs emit U+0001 where oracle has real values — invalid JSON output), 1 `[graph]/K[missing]`. All emitter/model gaps in src/render/json.ts (unpopulated draw arrays, missing attach_attrs equivalent, no write_subgs port) — NEXT mission; nothing accepted (registry empty). | test/golden/compare-json.ts(+.test.ts), test/corpus/{json-walk,render-one-json,json-dashboard}.ts, json-parity.json, PARITY-JSON.md, accepted-divergences-json.json, parity-report.ts, docs-site/copy-reports.mjs | tsc 0; comparator 11/11; corpus+golden 356/356; survey 762 walked |
| 2026-07-11 | graphs-b106 osage xlabels FIXED (node-size INCH2PS round-trip) | **Mechanism:** prior RCA's R-tree "cover violation" was a downstream symptom. First structural divergence = R-tree INS #25: inserted rect x0 20 (port) vs 21 (C), flipping the Hilbert key (51756/51757) and hence the whole tree topology. `objplpmks` floor(pos.x) flips because Node1398's `pos.x = coord.x - INCH2PS(ND_width)/2` = 20.999999999999986 (port) vs 21 exactly (C). C's coord carries the halfwidth's +1.4e-14 error and it CANCELS: C `gv_nodesize` (utils.c:1543) sets ND_lw=ND_rw=INCH2PS(ND_width)/2 where ND_width=PS2INCH(bb.x) (shapes.c:2372) — i.e. lw+rw = 72*(bb.x/72), a round-trip. Port `storeNodeSize` (nodeinit.ts) passed RAW poly points to gvNodesize, so lw+rw=bb.x exactly; osage's packing box (ND_xsize=lw+rw) then disagreed with addNodeObj's sz.x=INCH2PS(width) by 1 ULP and the cancellation broke. **Fix:** gvNodesize(n.info.width*72, n.info.height*72, flip) — bit-identical to C for every node, all engines. **Ruled out:** all R-tree primitives (combineRect fmin-both-sides, RectArea uint64, PickBranch/PickSeeds u64 wrap, Overlap, split-q — each re-verified faithful), Hilbert bag order (identical through INS #24), label sizes/anchors (bit-identical). All 4 b106 siblings osage -> 0 diffs. Accepted-xlabel ids NO FLIPS (twopi graphs/linux.i386-b29 still 1 diff Δ12; osage linux.i386/share-b29 still 2 diffs Δ37.22; pass ids still pass). | src/common/nodeinit.ts | tsc 0; npm 2965; osage sweep 684 rows 0 regr + 70 tail ids single-id verified |
| 2026-07-11 | 1447_1 osage FIXED — cdt Dtoset corrupted-walk port (ortho channel dicts) | **Mechanism:** all 1246 diffs were ortho track numbers (Δ13 ≈ 2-3 track steps; nodes/bb bit-exact; channel sets, seg_list order, np-phase adjacency, and the entire parallel-pass decision trace IDENTICAL until the walk itself diverged). C's add_p_edges walks the channel dicts via dtflatten+dtlink (raw ->right pointers), but every chanSearch inside the loop is a dtmatch that UNFLATTENs (dtrestore: chain IS the tree) and top-down-SPLAYS the dict — rewriting the ->right pointers under the walker. C therefore silently skips every channel a rotation moves into a left subtree: 157 of 294 channels visited for 1447_1. The port's Map-based dicts visited all 294 in insertion order -> different parallel precedence edges (addPEdges is cross-channel and edge_exists-guarded) -> different top_sort tracks. The cdt corrupted walk is deterministic and LOAD-BEARING. **Fix:** new src/ortho/chan-dict.ts = C-exact Dtoset (dttree.c do_search + has_root/no_root reassembly, DT_MATCH/DT_INSERT; dtflatten.c RROTATE right-linearisation; UNFLATTEN=flag clear). Channel dicts (Maze.hchans/vchans) rewired to two-level CdtOset mirroring chanItemDisc/chanDisc (dcmpid + chancmpid, insert-dedup by containment); chansInOrder = live dtflatten+.right generator; chanSearch = two dtmatches. NOTE: the repo's generic DtSplay deliberately deviates in not-found reassembly/insert attachment (splay-core.ts) — shape-changing, hence the separate exact port. **Ruled out:** htrack/vtrack math (identical trace fields), channel partition (identical), seg_list order (identical), np edges (byte-identical post-phase dump), seg_cmp/propagate_prec/decide_point/set_parallel_edges flip table (identical decision trace to divergence), channel-sort order alone (sorted-iteration fix changed nothing). C: cdt/dttree.c+dtflatten.c+dtrestore.c, ortho.c:add_p_edges/chanSearch; port: src/ortho/chan-dict.ts,maze-channels.ts,ortho-route.ts,ortho-parallel.ts,types.ts,maze.ts. | src/ortho/* | tsc 0; npm 2965; 17 ortho ids dot 0Δ (2538=8 pre-existing unchanged); same set osage: 1447 52->0, 1447_1 1246->0, rest 0Δ; b106 still 0 |
| 2026-07-11 | **imagemap (cmapx+imap) parity track BUILT + first survey: 641/762 conformant, 2 mechanisms cover all 151 format-row divergences** | New dot-track surface mirroring the xdot harness: comparator test/golden/compare-map.ts (+21 tests), dual-format worker render-one-map.ts, walker map-walk.ts (both formats per id, worst-of-two overall verdict, hang-safe detached spawn + group SIGKILL, oracle cache keyed (bin,GVBINDIR,'map',mtime)), dashboard map-dashboard.ts → PARITY-MAP.md (dedicated href-bearing table = the 34 substantive-coverage ids), accepted-divergences-map.json skeleton, parity-report "dot (imagemap)" row + copy-reports mirror (both as cleanly separable map-conformance BEGIN/END blocks for the concurrent JSON-track merge). COORD SEMANTICS from gvrender_core_map.c: every coordinate is printf `%.0f` in every branch — C never emits fractions; port already Math.rounds → comparator compares parsed integers EXACTLY (MAP_TOLERANCE=0, exact-after-round; a ±1 would mean a half-rounding-rule diff and stays VISIBLE as its own bucket). Area/line ORDER preserved (C emission order), never sorted; attrs shape/href/title/alt/target exact; `id` attr excluded (internal counter, not stable across id schemes); XML parsed w/ xmldom so escape-flavor diffs vanish. HARNESS SCAR: render() is NOT re-entrant on one graph object — render(g,'cmapx') then render(g,'imap') threw on 232/762 ids (`undefined.par` NS rerank ×165, `null.info` ×65); freeLayout leaves residual layout state. Worker parses FRESH per format (A/B-proven fix; the library relayout hazard itself = follow-up). SURVEY (762 conformant-SVG ids): overall 641 conformant / 117 diverged / 3 timeout (1718,2095_1,2108 — xdot-track perf family) / 1 oracle-error (1652, oracle 300s cap). Per-format: cmapx 641 (84.1%), imap 724 (95.0%). ALL 34 href-bearing ids diverge in BOTH formats. TWO buckets: (1) `element/line-count` 32 cmapx + 34 imap = port emits ZERO areas — anchor.ts deliberately skipped initObjMapData's url_map_p computation (emit.c:1666-1779, "SVG has no maps") so obj.urlMapPts is always empty and every map.ts beginAnchor early-returns → THE fix target next mission; (2) `map/@id` 85 cmapx = anonymous-graph `<map id="%1" name="%1">` (C agnameof internal name) vs port `""`. Triage only — no fixes this mission per brief. | test/golden/compare-map.ts(+.test), test/corpus/{render-one-map,map-walk,map-dashboard}.ts, test/corpus/{map-parity,accepted-divergences-map}.json, test/corpus/{PARITY-MAP.md,parity-report.ts}, docs-site/copy-reports.mjs | tsc 0; compare-map 21/21; corpus+golden vitest 366/366; survey 762 walked, 0 port-errors |
| 2026-07-11 | **json emitter PORTED to parity (0→720/762)** | Baseline json was a stub (0/762): emitted no graph attrs, no subgraphs (`_subgraph_cnt` hardcoded 0), empty `_draw_:[]`, and leaked the cgraph HTML `\u0001` marker into edge attr values (56 invalid-JSON ids, e.g. 2743 `l=<>`). Ported `write_graph`/`write_subgs`/`write_nodes`/`write_edges`/`write_attrs`/`write_xdot` faithfully. DRAW/LAYOUT sourcing mirrors C's json_begin_graph literally: re-render the laid-out g to xdot via a fresh context + re-parse (same technique as getDrawOps), look up per-object `_draw_`/`_ldraw_`/.../`pos`/`width`/`height`/`bb`/`xdotversion` by identity; STRUCTURE + user/inherited attrs come from the ORIGINAL g (the port's xdot writer drops user attrs and filters subgraphs, so xg alone is not faithful). Sentinel fix = agxget mirror at the json read boundary (isHtmlValue→htmlValueContent strip) + C's empty-value skip that EXEMPTS `label`; dot writer (parser/index.ts attrMapStr) already wraps HTML `<...>`, svg/imap don't dump arbitrary attrs, so json was the only exposed emitter. Also ported: label empty-skip exemption → `label`(edge/graph symbol declared)+node `\N` default emitted on every object even when empty; subgraph inherited graph attrs via graphDefaultsSnapshot (rankdir/center); attach_attrs label geometry the xdot writer omits (edge lp/head_lp/tail_lp/xlp, node xlp, graph/cluster lp/lwidth/lheight, record `rects` via set_record_rects). Output built as a JS value tree + JSON.stringify (semantic comparator = the bar, not bytes). RESIDUALS (39 diverged, follow-up candidates, no acceptance forced): ~17 T-op text (emoji trailing-space + charset — label-measurement in the xdot draw stream, not json); ~11 C/p op numeric (gradient/polygon coords from the xdot renderer's draw strings — pre-existing xdot deltas the xdot comparator tolerates); 2 rank + 1 nodes[members] + 1 cluster _draw_ + 2 edge lp = port over-emission (`expected=absent`: rank over-inheritance, empty-cluster draw, anon `%N` membership, 2368-flat-family label). PERF: 2 timeouts (2343 59KB, 2095_1 105KB) — the extra xdot re-render ~2x's large-graph cost (perf-sensitive, like existing 2621 quarantine). 1652 oracle-error = native dot fails on it (pre-existing, excluded). | src/render/json.ts, src/render/json.test.ts, test/golden/compare-json.ts, test/golden/compare-json.test.ts, test/corpus/json-parity.json, test/corpus/PARITY-JSON.md | tsc clean; npm test 2979/2979; json survey 720/762 (0 regressions vs baseline 0) |
| 2026-07-11 | **imagemap url_map_p PORTED — two baseline mechanisms closed (641→749 conformant)** | Ported the imagemap half of `initObjMapData`/`emit_begin_node`/`emit_page`/`emit_clusters`/`map_label`/`map_output_bspline` into src/gvc/anchor.ts (populate obj.urlMapShape/urlMapPts) and drove emission from src/render/map.ts. NODE map: RECTANGLE (lw/rw,ht/2) for box/record/non-poly + peripheries==0 P2RECT branch; POLYGON via pEllipse (ellipse) or vertices[i+offset] sampling (n-gon) + CIRCLE (regular); gated on href/URL/tooltip (matches emit_begin_node's `url||explicit_tooltip`, avoids O(N) waste — fixed a 2343-timeout risk). GRAPH rect = padded clip (map_end_page). CLUSTER rect = GD_bb (beginCluster hook). EDGE = map_output_bspline thick-outline polys (approx_bezier de-Casteljau + mkSegPts bisector offset, w2=max(pw/2,2)) + head/tail/center label rects, driven from endEdge. TRANSFORM (mapTransform): Y_GOES_DOWN gvrender_ptf at map dpi (default 96, dpi/resolution attr overrides) — x=(p.x+pad-bb.ll)*scale, y=(bb.ur.y+pad-p.y)*scale, PLUS margin*dpi/72 device offset (cracked 1879's constant +77 = margin=0.8→57.6*96/72; SVG adds margin/z, my first cut dropped it). Renderers unified into MapRendererBase + 4 subclasses (isCmapx/mapPolygon flags; `_np` = rectangle-only per device_features_map_nopoly). MECH 2 (anon `<map id/name>`): `digraph {` → cgraph agnameof `%1` but `digraph "" {` (explicit empty name) → `""`; the model collapsed both to name=''. Added Graph.anonymous (ast.id==null in builder) — mapGraphName returns `%1` only when anonymous. NOT via renaming the model to '%1' (would emit `digraph %1 {` in dot.ts xdot header, regressing the xdot track — SVG's emitGraphTitle already guards '%' but dot.ts does not). SURVEY (762): 749 conformant (98.3%, +108 vs 641), 11 diverged, 1 timeout, 1 oracle-error; 0 conformant→diverged regressions; href-bearing 25/34. RESIDUALS (all non-conformant at baseline, handed to follow-up): (A) HTML-table-cell hot spots [1425,2295,1425_1,graphs-html2 ×3,2619_1,2619_2] — cell rect geometry originates in htmltable-emit-rules.ts:initAnchor which never calls emit_map_rect (outside write-set); the node's urlMapPts leaks to the cell anchor → wrong coords/count; (B) cluster/edge/node area ORDER for EMIT_CLUSTERS_LAST [graphs-url, 1880] — renderPage emits clusters before nodes/edges (correct for SVG's non-CLUSTERS_LAST, wrong for the map device; reorder lives in device.ts, outside write-set); (C) record-label space→nbsp [1447] — oracle encodes runs of spaces in record fields as &#160; in the tooltip(=label); port emits plain spaces (label-parse fidelity, outside map). GUARD: SVG anchors byte-identical port vs oracle for url(19)/b103(943)/b29(109)/b155(1)/1879(537); anon-root SVG title still suppressed (emitGraphTitle '%' guard); 3008 vitest pass. | src/gvc/anchor.ts, src/render/map.ts, src/model/graph.ts, src/parser/builder.ts, src/render/map-renderers.test.ts, test/corpus/{map-parity.json,PARITY-MAP.md} | tsc 0; vitest 3008/3008; survey 762 walked 0 regr; lizard defaults clean |
| 2026-07-14 | **pack wrong-graph resolution: flat.ts `dot_root` hole + `GD_has_labels` on the wrong graph + `abomination` sentinel** | Under `pack` each component is laid out as its OWN dot-root (`_cc_0`), the cgraph root (`G`) is never ranked and has NO rank table, so "which graph do I read this field from" becomes load-bearing — and C picks a DIFFERENT graph per call site. Instrumented BOTH sides (C `fprintf` in flat.c/position.c, rebuilt `gvplugin_dot_layout`; reverted + rebuilt to sha1 `5caf7a368dae`). **(1) flat.ts**: C reads the rank table via `dot_root(agtail(e))` = `GD_dotroot(agroot(e))`, a MUTABLE slot `dot_init_subg` re-points at the graph being laid out → the COMPONENT. Port read `e.tail.root` → the TRUE root (`rankLen=UNDEF`). `checkFlatAdjacent` bailed on `if (!rank) return` (a guard C does NOT have) and `flatNode` then dereferenced the absent table → **hard crash** on any packed graph with a labeled NON-adjacent flat edge (`Cannot read properties of undefined (reading '0')`; repro = golden `dot-pack-flat-label-vnode`). Fixed by THREADING the layout graph (instrumentation proved C's `dot_root(agtail(e))` === the `g` passed to `flat_edges`, in both pack and no-pack). **(2) has_labels** (the actual cause of the reported bb divergence — the audit's stated mechanism was WRONG): C sets `GD_has_labels` on `agraphof(agtail(e))` = the TRUE ROOT (utils.c:521) and reads it back as `GD_has_labels(g->root)` at every dotgen site (position.c:234, dotsplines.c:243/1552/1650/1776) EXCEPT `edgelabel_ranks` (rank.c:170, scoped to `g`). Port set it on the COMPONENT and read it from the LAYOUT GRAPH → both 0 under pack → `make_LR_constraints` used the full nodesep on odd ranks instead of C's `sep[1]=5`, pushing node `c` 167→180 and the bb `250`→`264`. Fixed at origin (write to `g.root`) + corrected the 3 `g->root` readers. **(3) abomination**: C allocates THREE extra rank slots and re-bases the pointer (`GD_rank=rptr+1`, new rank at index **-1**, `maxrank` unchanged), preserving the zeroed sentinel `allocate_ranks` parks at `maxrank+1` (`gv_calloc(maxrank+2)`, mincross.c:1155). The port renumbers +1 and shifts UP in place, overwriting that sentinel and leaving `rank[maxrank+1]` undefined (latent: would fault `transposeCounts`' `rank[r+1].n` gate and `mergeRanks`' `rank[mx+1].valid`). Re-established in BOTH `abomination` and `shiftClusterRanks`. **REJECTED**: mirroring C's mutable slot by re-pointing `dotRoot` at the cgraph root — the port uses `dotRoot()` both where C means `dot_root` AND where C means plain `agraphof` (`straight-edges.ts:219 addEdgeLabels` → C `updateBB(agraphof(agtail(e)))`, which WANTS the true root); re-pointing the slot would silently corrupt those. straight-edges.ts:219 audited and found CORRECT — left alone. **Follow-up (unfixed, out of scope)**: `splines-label.ts:370 placePortLabels` gates on `has_labels`, but C (dotsplines.c:440) has NO such gate — packed head/tail labels are dropped. | src/layout/dot/flat.ts, src/common/edge-label-init.ts, src/layout/dot/position-aux.ts, src/layout/dot/splines-flat.ts, src/layout/dot/splines-route.ts, src/layout/dot/pack-components.ts, src/layout/dot/flat.test.ts, test/golden/{manifest.json,suite.test.ts,inputs,refs,refs-xdot} | tsc 0; vitest 3111/3111 (baseline 3105 + 4 golden + 2 sentinel); goldens `dot-pack-flat-label`(+`-vnode`) FAIL before / PASS after; corpus spot-check 60 dot + 15 neato + 15 fdp + all 9 corpus `pack` graphs: **0 regressions, 0 verdict changes**; C tree clean, oracle sha1 5caf7a368dae |
| 2026-07-15 | **json divergences 39→12: gradient Y-flip + xdot comparator blind-spot + parseString byte-count + latin1 double-encode** | Closed the two largest JSON residual classes the 2026-07-11 json port flagged. **(1) Gradient anchor coords (12 ids: all grd* + 2258).** `linearGradientOp` (render/dot.ts) emitted `p0`/`p1` in SVG y-down space (`-322`) on the JSON/xdot device path; oracle `-Tjson` emits native y-up (`+322`). C's `get_gradient_points(A,G,n,angle,flags)` has `isRHS = flags&2`: the xdot backend calls it with flag 2/3 (y-up), the SVG backend without (y-down + a container `<g transform>` flip). The port had only ported the isRHS=0 branch (`svg-gradient.ts` comment even said so); radial un-negated manually at the call site, linear forgot. Completed the port: added `isRHS` param to `getGradientPoints`, both xdot ops pass `isRHS=true`, dropped radial's manual `-gp.g0.y`. SVG output unchanged (still `-322`, matches oracle SVG). **WHY SVG didn't catch it:** SVG's un-flipped coord IS correct (oracle SVG also emits `-322`); only the device/xdot/json space is pre-flipped. **WHY xdot didn't catch it:** `compareXdot`'s `canonGrad` reduced a gradient op to `type:stops`, DROPPING x0/y0/x1/y1/r0/r1 — the coords were in neither `opNumbers` (returned []) nor `opValues`. Tightened: `opNumbers` now returns the anchor coords for grad ops (tolerance-compared). Audited every XdotOp field vs opNumbers+opValues — text/font/image/style/color/ellipse/polygon/bezier/polyline/fontchar all fully compared; only gradient coords were blind (point `z` uncompared but always 0 in dot 2D, vacuous). **(2) parseString byte-count (~16 text ids).** `XdotParser.parseString` read the counted-string field `n` (a BYTE count, C `strlen`) by JS characters, over-reading the op separator for any multi-byte char (`ÿ`→`ÿ `). C walks `char*` byte-by-byte until `accounted==n`. Fixed to add each code point's UTF-8 byte length (surrogate-aware), escape-prefix `\` still consumed-not-counted. Invisible to the xdot track because BOTH sides use the same JS parser (both got `ÿ `→ matched); only `-Tjson` (oracle=native parser) exposed it. **(3) latin1 double-encode (2 ids: graphs-Latin1, graphs-b60).** C's `stoj` applies `latin1ToUTF8` a SECOND time to already-UTF-8 draw text when `charset=latin1` (gvrender_core_json.c:82), so `-Tjson` mojibakes draw text (`á`→`Ã¡`) but NOT attribute values (stored raw latin1, single-converted) — the SVG backend does neither. Applied `latin1Reencode` (reinterpret UTF-8 bytes as Latin-1 code points) to draw-op TEXT only, gated on `g.info.charset===CHAR_LATIN1`, threaded via `BuildCtx.isLatin`. First cut applied it tree-wide → broke the `label` attr (oracle single-encodes it); narrowed to text after byte-diffing oracle SVG-vs-json. **Remaining 12 (all pre-existing subset of the 39, 0 regressions):** rank-set collapse (1453/2825, port over-emits a rank=same subgraph), cluster membership/draw (1514/1436/1902/graphs-b7), concentrate edge lp (2368_1/2470), xdot-writer quote-escaping (2239), UTF-8 node label (1879), raw-latin1-no-charset attr (share/windows-Latin1). | src/render/svg-gradient.ts, src/render/dot.ts, src/xdot/parse.ts, src/render/json.ts, test/golden/compare-xdot.ts | tsc 0; npm 3206/3206; fresh json survey 719→749 conformant (39→12 diverged, 0 regr); fresh xdot survey 0 diverged (comparator now CHECKS grad coords & they pass); circo 743→745 via shared parseString; twopi/osage/patchwork unchanged |
| 2026-07-15 | **json remaining tail: cluster-draw object-keying, node_induce agapply cascade, concentrate edge-lp spline gate, latin1 accept, harness UTF-8 chunk fix (12→1 will-fix)** | Closed the remaining json divergences after the gradient/parseString/latin1 batch. **(1) Cluster draw object-keying (b7, 1436, 1902, 1453).** `collectDrawLookup` keyed the cluster overlay Map by NAME, so two clusters sharing a name at different depths collided (empty `cluster_1` nested in `cluster_0` + a real top-level `cluster_1` → the empty one inherited the real one's bb/_draw_; graphs-b7 had an extra draw, 1436/1902 wrong polygon coords, 1453 a spurious rank). Rewrote as a PARALLEL walk of the original graph and the xdot re-render (`xg` holds only drawable clusters — the xdot writer drops anonymous/rank subgraphs), matching by name WITHIN each level and descending g through filtered non-cluster levels while holding the xg position; keyed by the cluster OBJECT. **(2) node_induce agapply cascade (1514, 2825).** C's `agdelnode(subgraph)` uses `agapply(agdelnodeimage)` to drop the node's IMAGE from every subgraph in the cluster's SUBTREE; `agDeleteFromCluster` removed it from the cluster only, so a rank subgraph nested in the cluster kept a stale `nodes` member. Added the node-image-only subtree cascade (no edge purge — that stays root-only/node_induce-specific). NOTE: 2825 (concentrate=true) FALSE-alarmed as a crash — the ORACLE itself exits nonzero on that degenerate `rebuild_vlists: lead is null` case; the naive check harness threw on the oracle's exit, not a port fault (both render complete output). **(3) Concentrate edge-lp spline gate (2368_1, 2470).** C attaches pos/lp/head_lp/tail_lp/xlp only inside `if (ED_spl(e))`; the port attached `lp` whenever the edge had a label, so a concentrated/merged edge with no spline of its own emitted a stale off-canvas `lp`. Gated `attachEdgeLabelPositions` on `e.info.spl`. **(4) share/windows-Latin1 ACCEPTED.** Undeclared-charset Latin-1 input makes native `-Tjson` emit a MIXED-ENCODING doc: raw-Latin-1 attribute bytes (invalid UTF-8) + UTF-8 drawn text. No single decode reads both; the port's consistent-UTF-8 output cannot match. accepted-divergences-json.json + rationale (A4). **(5) Harness UTF-8 chunk-boundary fix (1879 + latent whole class).** spawnCapture did `stdout += d`, decoding each Buffer chunk independently → a multi-byte char split across a chunk boundary (ö = c3 b6) became two U+FFFD (buffering-dependent flake: 1879). Buffer the bytes, decode whole-stream `toString('utf8')` (non-fatal). First cut used UTF-8-fatal-THEN-latin1 fallback — WRONG: one stray invalid byte anywhere flipped the whole output to latin1, mojibaking every valid c3xx (spuriously diverged graphs-b34/b56/Latin1/b60 in the re-surveyed xdot track, 0→6). Non-fatal decode fixes the split without corrupting valid UTF-8; xdot back to 0 diverged. Applied to all 5 walk harnesses (json/xdot/map/format/survey). **REMAINING (1 will-fix): 2239** — port drops the backslash from `\\"` in a cluster's drawn text (→ `"`) where C keeps it (→ `\"`); `splitLabelLines`+`substObj(false)` in isolation ARE faithful (verified: produce `\"`), so the value reaching make_label is one unescape-level lower than the stored attribute — root cause unpinned, needs live make_label content instrumentation. | src/render/json.ts, src/layout/dot/cluster.ts, test/corpus/{json-walk,xdot-walk,map-walk,format-walk,survey}.ts, test/corpus/accepted-divergences-json.json, test/corpus/{json-parity,xdot-parity}.json, PARITY*.md | tsc 0; npm 3206/3206; json 749→758 conformant / 1 diverged / 2 accepted (0 regr); xdot 761/0; SVG node positions bit-identical on cascade-touched ids (1514) |
| 2026-07-15 | **imagemap 11→0 diverged: HTML cell hot spots + EMIT_CLUSTERS_LAST anchor order + href entity decode + title nbsp** | Closed the whole imagemap tracked-diverged set (map track 739→755 conformant, 0 diverged). **(1) HTML cell/table hot spots (1425, 2295, 1425_1, 2619_1/_2, graphs-html2 ×3).** The port opened HTML cell/table anchors via `beginAnchor` but never called C's `emit_map_rect(job, b)` first, so `obj.urlMapPts` was unset and the map device emitted no `<area>`. Added an `emitMapRect(box, job)` renderer op (map renderer records the box as urlMapPts via computeClusterUrlMap; other renderers no-op), called from `initHtmlAnchor` before `beginAnchor`, with the absolute (`box+pos`) cell/table box threaded in. **(2) EMIT_CLUSTERS_LAST anchor order.** The flag existed (job.ts) but was never set; the map device carries it (device_features_map). Set it in the map renderer's beginGraph, then mirrored C's four ordering sites: the HTML table anchor is emitted AFTER its cells (emitHtmlLabel, htmltable.c:583-588); the node's own shape anchor is emitted AFTER its label (polyGencode, shapes.c:2936/3087 — saving+restoring the node hot spot since the cells overwrite obj.urlMapPts); clusters are emitted AFTER nodes/edges at page level (renderPage, emit_view:3565); and clusters recurse CHILD-FIRST (renderOneCluster recurses before its own anchor, emit_clusters:3795), so an inner cluster's area precedes its parent's. **(3) href/target HTML entity decode (2619 imap).** The port kept HTML-label href/target as the raw entity (`&amp;`); cmapx looked right only because escapeXml is entity-aware, but imap (plain text) emitted `&amp;` where the oracle emits `&`. C's HTML parser decodes attribute entities, so added `anchorAttr = htmlEntityUTF8` on href/target at parse — cmapx re-escapes `&`→`&amp;`, imap emits raw `&`; SVG href unchanged (`&amp;`, verified). **(4) map title/target nbsp (1447).** C emits title/target via `gvputs_xml` (dash+nbsp flags): runs of 2+ spaces → `&#160;`. The port used plain escapeXml; switched to escapeXmlTitle. SVG node positions and hrefs verified unchanged on the touched ids. | src/gvc/context.ts, src/render/map.ts, src/common/htmltable-emit.ts, src/common/htmltable-emit-rules.ts, src/common/htmltable-parse.ts, src/common/poly-gencode.ts, src/gvc/device.ts, test/corpus/{map-parity.json,PARITY-MAP.md}, PARITY*.md | tsc 0; npm 3206/3206; fresh map survey 739→755 conformant / 0 diverged (was 11) / 0 port-error; all 11 targeted ids cmapx+imap ok; SVG href/positions unchanged |
| 2026-07-15 | **2239 json draw-text round-trip FIXED (json 1→0 diverged): overlay pre-serialization draw strings** | The json path builds draw ops by re-rendering the laid-out graph to xdot TEXT and `parse()`-ing it back (mirroring C's json_begin_graph gvRender(xdot)). That DOT-text round-trip is LOSSY for a draw-op's length-delimited text containing `"`: a DOT quoted string cannot represent a literal `\` immediately followed by a literal `"` (`\"` always means `"`), and cluster labels are deliberately NOT put_escaping_backslashes-protected (gvrender_core_dot.c:286, matching the oracle's xdot TEXT) — so a cluster label's `\"user` serializes to `\\"user`, the DOT parser reads `\\`→`\` then `"` as the CLOSING quote, and the backslash is dropped (drawn text `\"`→`"`, 2239). C never hits this: its -Tjson reads the internal `_ldraw_` attribute directly via agxget, no text round-trip. Fix: the XdotRenderer already keeps `draws: Map<object, XdotDraws>` — the pre-serialization draw strings keyed by the ORIGINAL model object. Exposed it (drawStringsByObject), injected our own XdotRenderer into the collectDrawLookup context (last-registered wins bestRenderer), and OVERLAY those strings onto the re-parsed attrs (applyDraws over _draw_/_ldraw_/_hdraw_/_tdraw_/_hldraw_/_tldraw_, keyed by original object). Coordinates/layout attrs still come from the re-parse (no `"` issue). Nodes/edges (escBackslash'd) overlay identically to the re-parse for non-`"` text (DOT-transparent to `\\`); clusters (raw) and any object with `"` are fixed. xdot TEXT output UNCHANGED (getter-only renderer addition; verified 2239 cluster ldraw still byte-matches oracle). **Result: all three deterministic dot outputs (SVG/xdot/json) + imagemap now have 0 will-fix divergences; json 2 accepted (latin1 mixed-encoding).** | src/render/dot.ts, src/render/json.ts, test/corpus/{json-parity.json,PARITY-JSON.md}, PARITY*.md | tsc 0; npm 3206/3206; fresh json survey 758→759 conformant / 0 diverged (was 1) / 0 regressions; xdot output byte-unchanged | 
| 2026-07-21 | plantuml-ts issue 06: cluster bbox in getLayout | **Ask (not a layout fix — API surface gap):** the engine computes cluster bboxes correctly (rendered `class="cluster"` polygon byte-identical to real dot) but `getLayout()` exposed only bounds/nodes/edges, so a geometry consumer could not read cluster wrap dimensions. Added `ClusterGeometry {name,x,y,width,height}` and a `clusters: ClusterGeometry[]` field to `LayoutSnapshot`; `collectClusters` walks `info.clust` depth-first (nested clusters each get an entry, name encodes nesting), `snapshotCluster` follows the existing `bounds` frame convention (yAxis:'up' → native lower-left corner; yAxis:'down' → top-left, flipY(ur.y)). Raw points (issue 01 precedent: consumer quantizes to SVG precision; 65.99997 rounds to the polygon's 66). Verified vs real dot on the issue repro: down {x:8,y:8,w:66,h:66}, up {x:8,y:100,w:66,h:66} = polygon `8,-100 8,-166 74,-166 74,-100`. Additive change — `LayoutSnapshot` only produced by `getLayout`, no other constructors. | src/api/geometry.ts, src/api/index.ts, src/api/geometry.test.ts | tsc 0, npm 3213/3213 (7 new), lizard 0 thresholds, build OK. Corpus unaffected (getLayout is read-only, not in render path). |
| 2026-07-21 | plantuml-ts issue 07: HTML label via programmatic builder | **Ask (API surface gap):** the HTML-string marker (`HTML_STRING_MARK` = U+0001, mirrors cgraph aghtmlstr) that tags a `label=<...>` value as markup was internal-only, so `GvGraphBuilder.addSubgraph`/`addNode`/`setAttr` (the seam plantuml-ts's `graph-layout-build.ts` uses) had no supported way to request HTML-table label sizing — a cluster label set via `setAttr` is measured as literal text. Chose the issue's option (b), a proper builder API, over leaking the control char: added `setHtmlAttr(k, v)` to GvNode/GvEdge/GvGraphBuilder (interface + NodeHandle/EdgeHandle/GraphBuilder impls) — sets `attrs[k] = HTML_STRING_MARK + v`, flowing through the SAME isHtmlValue path issue 05 already validated. Verified end-to-end on the issue repro: a programmatically-built single-child cluster with a marked `<TABLE FIXEDSIZE HEIGHT=3>` label reserves header = 19.00 = HEIGHT(3)+16 (g5 ledger §C2), vs 32.80 when the markup is set as a plain string. Marker kept internal (not exported) — setHtmlAttr is the sanctioned contract. | src/api/builder.ts, src/api/builder.test.ts | tsc 0, npm 3219/3219 (6 new), lizard 0 thresholds. |
| 2026-07-21 | CDT DtBag equal-key order (bagInsert) | **Mechanism:** the xlabel Hilbert bag (`src/cdt/bag.ts` DtBag, its only user) splayed-then-spliced on duplicate insert, so equal-key nodes drained in a different order than CDT's Dtobag (native GVTS_XL_KEYS dump: 53 reorder groups / 107 position diffs on 1652). Root: `splay()` reassembles the LEFT/RIGHT partition spines into a BST before the splice; CDT's dttree DT_INSERT links the new node *while still partitioned* (pushes the found equal-key node onto the RIGHT-partition head, new node roots with link.right/link.left). Fix = `bagInsert` (integrated top-down splay + DT_OBAG insert) in splay-core.ts; DtBag.insert calls it. **Verified:** replaying native's exact 5598-element insertion sequence → 0 drain mismatches (was 107). Note: this is NOT what causes the 1652 edge-label residual (that's a single objplpmks floor()-boundary rect diff amplified by the lossy xlabel RTree — irreducible, see memory) — bagInsert is an orthogonal CDT-faithfulness fix. **Fresh corpus sweep (7 engine walks, all 762 ids, scratch jsonls): 0 pass→diverged regressions anywhere; net +4 (sfdp +2 [2639,2184], twopi +1, patchwork +1). dot verified unaffected (0-line self-diff on all 5 xlabel corpus graphs).** Parity docs + summaries regenerated. | src/cdt/bag.ts, src/cdt/splay-core.ts, test/corpus/parity-*.{json,jsonl}, test/corpus/PARITY*.md | tsc 0, npm 3219/3219, sweep 0 regr / +4 |
| 2026-07-21 | sfdp tracked-divergences mission (50→0) | **Objective met: sfdp tracked divergences 50 → 0.** After Mission 0 regenerated the stale attribution (57→17 not-cleared), the entire residual reduced to ONE root cause with two component paths in `src/layout/sfdp/index.ts` postprocess. **Fix 1 (fa0240b, single-component):** keep the routing box via `normalizeGraphBB` instead of a geometric `computeSubgraphBB` recompute that clobbered the ratio=fill scaled bb AND shifted by the geometric `ll` instead of the routing `ll`. Because that shift runs before `gvPostprocess`→`addXLabels`, it also corrected the xlabel round() frame — clearing the edge-label class the prior session had filed as "RTree-lossy irreducible" (1652). Resolved 11 ids (3 trapeziumlr→pass, 1855+b106+share-b106+1652+graphs-b29+linux.i386-b29+linux.i386-b106+root_twopi+root_circo→A1-drift, +bonus neatosplines_neato1). **Fix 2 (dfaafcc, multi-component):** drop the premature `shiftOneGraph` before addXLabels (objplp2rect round()s in C's packed frame), mirroring neato 1e7515d — resolved 2470 (7 comps) + 2095_1 (44 comps). **Accepts (class A9, ADR-2 evidence):** 42/241_0/2095 — CDT incircle / findMaxDev hypot 1-ULP ties; fix levers already applied+documented (triang.ts fma, route.ts:198-199 hypot); fresh native-vs-V8 hypot ULP probe confirms. Final: 524 pass / 230 diverged, **0 tracked** (227 A1 + 3 A9). Cross-engine isolation: `git diff main` = only `src/layout/sfdp/index.ts`(+test); shared functions unmodified → neato/fdp/circo/twopi/osage/patchwork unaffected by construction (PARITY.md unchanged). | src/layout/sfdp/index.ts, src/layout/sfdp/sfdp.test.ts, test/corpus/{parity-sfdp.*,attribution-sfdp.*,accepted-divergences-engines.json,PARITY*.md}, docs/known-divergences.md | tsc 0, npm test 3220, clean fresh sfdp sweep 0 pass→diverged, tracked 50→0 |
| 2026-07-21 | fdp tracked-divergences mission (→0) | **Objective met: fdp tracked divergences → 0.** Mission 0 restored the native `GVTS_POS_DUMP` (fdp routes through `spline_edges1`, not the `spline_edges` dump site) at `fdp_layout` between `fdpLayout()` and `neato_set_aspect()` — needed `GVTS_BB` + recursive `GVTS_CLUST_BB` beyond the minimal block (fdp's GD_bb/cluster bbs come from `setBB` upstream of injection, load-bearing per injection-recipe.md); build target is `gvplugin_neato_layout`, NOT `dot`. Fresh attribution cut the stale "20 tracked" to **4 not-cleared**. **B1 FIX (graphs-fdp, graphs-b145): ported fdp compound cluster-edge preprocessing.** An edge whose endpoint names a cluster (`e -- clusterB`) must not draw a node — C's `processClusterEdges` (fdp-ONLY; dot/neato draw it) replaces the endpoint with an invisible box proxy sized to the cluster (`setClustNodes`) and deletes the original. Never ported (init.ts assumed no such input). New `src/layout/fdp/cluster-edges.ts` + init call + real setClustNodes + render/dot.ts clustnode suppression/name-strip. ROOT-fix: dot rank.ts:273 wrongly set `clustnode` on real cluster members (C node_induce sets ND_clust only) — benign until the serializer keyed on it; removed → clustnode fdp-exclusive → shared render/dot.ts inert for other engines. Both graphs base→inj 0 (A1). **B2 (unix force-drift): EMPTY** (all drift-exonerated; skipped). **B3: 241_0 A9 accept** (11 numeric bezier diffs on one edge under injected-identical positions ⇒ pathplan CDT cocircular incircle 1-ULP tie, same as circo/twopi/sfdp 241_0, levers fmadd/hypot applied, irreducible); **2095 reclassified A9→A1** after fixing the injector empty-name blindspot (`(.+)`→`(.*)`): injecting the single `""`-named node collapses 2095 8256→0 (pure force-drift). Final: 600 pass / 145 diverged, **0 tracked** (144 A1-drift + 1 A9). Cross-engine isolation: `clustnode` fdp-exclusive post-fix (proof: only cluster-edges.ts sets it), render/dot.ts/rank.ts changes covered by 3220 unit tests incl. 42 dot cluster goldens; fresh fdp engine-walk 0 pass→diverged (2620 diverged→oracle-error = PRE-EXISTING native SEGFAULT under -Kfdp, exit 139, confirmed by reverting the gated dump + rebuilding; not a port regression, excluded from tracked). Cross-mission flag: sfdp 2095 A9 accept likely shares the injector blindspot — future sfdp regen should re-check (ADR-2, not re-swept here). | src/layout/fdp/{cluster-edges,init,layout,guards.test}.ts, src/layout/dot/rank.ts, src/layout/neato/splines.ts, src/render/dot.ts, src/model/graphInfo.ts, test/corpus/{attribution-fdp.*,accepted-divergences-engines.json,PARITY*.md}, docs/known-divergences.md, ~/git/graphviz/lib/fdpgen/layout.c (native gated dump) | tsc 0, npm test 3220, fresh fdp sweep 0 pass→diverged, tracked 4→0 |
| 2026-07-24 | **plain track: write_plain fidelity port (T6)** | dot plain diverged 269→7 of 762 via five mechanisms, each C-verified: (1) full `_agstrcanon` port (conditional DOT quoting, escape/numeral tracking, keyword protection, 128-byte line breaks — write.c:113) applied to names/labels/edge-labels/plain-ext ports; node label mirrors `ND_label->text` (substituted normal / raw record source / `<...>` original html; failed-html keeps raw markup, labels.c make_label); style/shape/color via node-defaults chain + `late_nnstring` empty-fallback; graph scale = job->zoom (size fit, pad 0 per render_features_dot); AGSEQ node order. (2) `collectSplinePts` read past `bz.size` → up to 3 junk points on arrow edges. (3) `printG5` exact-tie rounding: JS toPrecision rounds .x5 ties away, C %.5g to-even; integer point coords land on exact .x5 inch ties (78498/72=1090.25→"1090.2"); exact-BigInt-expansion rounding, C-printf-verified. (4) shape field is bind_shape NAMING from attrs (shapefile→"custom", unknown names kept), not the resolved-fallback desc name (shapes.c:3976). (5) cluster proxy nodes (ND_clustnode) skipped; edge endpoints print name after `__i:` prefix (output.c:114,146). | src/render/map.ts, test/golden/compare-plain.ts (AD-4: iterative skips pointCount — position-derived piece counts) | tsc clean; 3271 tests; 8-engine fresh sweeps |
| 2026-07-24 | **plain track: oracle canon() use-after-free = A4 accept (7 ids × 8 engines)** | Latin1×3/Symbol/b34/b56/b60: native plain prints garbage or empty for labels whose text differs from the pooled attr bytes (latin1ToUTF8 / htmlEntityUTF8 conversions). Mechanism: canon() (output.c:103-108) agstrdup→agstrcanon→agstrfree, and `_agstrcanon` returns the input refstr pointer whenever no quoting is needed; unpooled text hits refcnt 0, strdict_remove frees it (refstr.c), and the intervening printdouble allocations reuse the block before printstring reads it. Proven: labels equal to pooled strings (node names, attr values) survive via refcount (hex-verified `hello`), converted labels print 0x05/empty junk. Not replicable (allocator-dependent) and per A4 policy not replicated — port prints the correct converted text. Registry entries added. | test/corpus/accepted-divergences-plain.json | mechanism traced in refstr.c/output.c; MallocScribble + pool-membership A/B |
| 2026-07-24 | **plain track: engine-inherited geometry accepts (24) + twopi plain-shape init follow-up (1)** | twopi×14 (1855 2239 2470 241_0 arrows/newarrows/arrowsize families, b29×2), osage×7 (1652 1855 polypoly×3 b29×2), circo×3 (241_0 2475_2 windows-tree): every id is already `diverged` on the same engine's xdot track with an accepted mechanism (A1/A8/A9 classes per engine registry); plain re-emits the same layout, no new mechanism. Plus twopi `regression_tests-shapes-reference-plain`: port twopi/neato init falls back to 0.75×0.5 for a shape=plain width=0 pos-pinned node where dot (and native) size 0×0 (poly_init IS_PLAIN) — engine-init defect class, real port bug, fix in src/layout/twopi outside the plain write-set → registry-accepted with follow-up flag. Excluded-not-dropped: oracleError ids (dot:1652; circo×14 incl b102/b103/badvoro; twopi/neato/sfdp: 2222,2108; fdp: 2717 2239 2620 1652 2222) and port timeouts (circo×7 labelclust family+b7+258+1879-2+2563+2295; twopi×8 labelroot family; 2108 on osage/neato/sfdp) — pre-existing oracle failures and perf tail, listed in plain-parity-*.json. | test/corpus/accepted-divergences-plain.json | parity-<engine>.json cross-check per id |
| 2026-07-24 | **json track ×7 non-dot engines: triage complete (T7)** | Deterministic engines nearly clean out of the box (circo 8 / twopi 15 / osage 10 / patchwork 0 diverged); iterative flooded at the dot-era ±0.01 (neato 269 / fdp 169 / sfdp 264) → json-walk now passes the engine-walk bar to compareJson (0.5 iterative, 0.01 deterministic). compare-json compared `rects`/`head_lp`/`tail_lp`/`xlp` textually — native float dust (record rect x=-2.8e-14 vs port 0) false-diverged record graphs; added to POSITIONAL_ATTRS (numeric at tolerance). Residue classes, all registry-accepted engine-scoped (json-walk acceptance lookup now honors an optional `engine` field; legacy engine-less rows still apply everywhere): (1) 470 layout-drift ids inherited from the same engine's xdot track (id diverged there, documented classes); (2) 9 engine-track blind spots — ids parity-<engine>.json lists as oracle-error/timeout so xdot never compared them, json exposes real layout divergence (neato 2619_2 Δ0.81; sfdp 2619_1 Δ1.44, 1879; fdp b53/1879/badvoro/2108/1652 Δ up to 6.4e4; circo 2095_1 Δ66) — mechanisms live in engine layout, outside the json write-set, follow-up flagged; (3) osage 2721/1221/1332 = A4 oracle bug: native emits bare `inf`/`nan` (invalid JSON) for degenerate cluster draw coords; port emits valid finite JSON. Anomaly note: the first fdp sweep's oracle cache captured a layout for share-b124 (and transiently others) that no fresh run reproduces (serial ×2, concurrent ×8 all sha-identical and port-matching); all diverged ids were re-verdicted from invalidated caches with fresh oracles — one-time capture anomaly, unexplained, documented not hidden. dot json track untouched (smoke conformant). | test/corpus/json-walk.ts, test/golden/compare-json.ts, test/corpus/accepted-divergences-json.json, test/corpus/json-parity-*.json | tsc clean; 3271 tests; 7-engine sweeps 0 diverged; dot smoke green |
| 2026-07-24 | **imagemap track ×7 non-dot engines: triage complete (T8)** | One real fix: root-graph hotspot rect used bb±pad, but C emits `job->clip` — the 0-BASED page window (emit.c:3652) — which differs whenever an engine keeps a negative bb.LL (fdp graph labels: bb "0,-24.8,..."). Native url.gv fdp emits 0,-33,539,374; port emitted 0,0,539,407; fix computes [-pad, (UR-LL)+pad], verified byte-equal for fdp and numerically unchanged for dot/neato (native prints "-0" top, port "0" — numeric-equal). map-walk gained the AD-4 bar (iterative ±1 hotspot px = the 0.5pt model bar after integer rounding; deterministic stays exact-after-round) and engine-scoped acceptance entries. 52 accepts: 48 engine-track-inherited drift ids (neato 6, fdp 14+4, sfdp 24), 3 blind spots (fdp/sfdp 1879, sfdp 2619_1 — engine-track oracle-error, follow-up), osage graphs-xx = A8 rounding-boundary dust (1 of 272 coords, 2655 vs 2654 across the integer boundary within the 0.01 model bar). fdp re-verdict pass time-boxed 4 big ids into `timeout` (nshare-root_twopi, graphs-root, graphs-b103, linux.x86-root_twopi — registry rows exist if they return as diverged). dot imagemap + plain tracks unchanged. | src/gvc/anchor.ts (computeGraphUrlMap — write-set extended, mechanism-at-origin), test/corpus/map-walk.ts, test/corpus/accepted-divergences-map.json, test/corpus/map-parity-*.json | tsc clean; 3271 tests; 7 sweeps 0 diverged; dot map smoke + plain spot green |
| 2026-07-24 | **Mission format-parity-matrix CLOSED** | 22 new tracks in PARITY.md (plain ×8, json ×7 non-dot, imagemap ×7 non-dot), 0 unexplained divergences. 7 src fixes (write_plain canon/semantics suite, bz.size, %.5g ties, shape naming, clustnode, html fallback, imagemap clip-rect), 615 mechanism-backed acceptances, AD-4 bars wired into json/map walkers. Notable new root causes: C canon() use-after-free (unpooled label text freed before print) and C 0-based clip window for the root hotspot. Follow-ups live in the three acceptance registries (blind-spot engine-layout ids + twopi plain-shape init). | PARITY.md + 22 detail pages, 3 registries, docs-site | tsc + 3271 tests + docs:build green; existing tracks unchanged |
| 2026-07-24 | **T1 engine-track baseline refresh: 40 phantom oracle-errors → pass** | engine-walk classified any nonzero oracle exit as oracle-error, but native dot exits 1 on recoverable warnings (missing image file: 2619/2516 family) while emitting COMPLETE xdot; plus a 60s oracle budget (2095_1 circo needs ~90s+). Fixed classifier to the completeness check the other walkers use + 300s budget. Results: patchwork 762/762; 2619/2516 pass on all 7 engines; blind-spot ids now have real baselines — circo 2095_1 diverged (edge bezier), neato 2619_2 diverged (edge draw, passes on all other engines), fdp b53 (graph _ldraw_ MISSING port-side), fdp badvoro/1879/2620 + sfdp 2619_1/1879 share ONE signature ([graph]/_draw_ canvas polygon ⇒ graph bb extent divergence), fdp 1652/2108 port-timeout (perf class). Genuine oracle failures remain: circo 1880/1447 + fdp 2717/2239 native crashes; 2222 >300s everywhere. sfdp 2556 (degenerate-NaN A6b id) resurfaced timeout→port-error (armPow finite guard) — honest, excluded-listed. | test/corpus/engine-walk.ts, parity-<engine>.json(l) ×7 | typecheck+3271 tests; no previously-passing id regressed (only error rows re-run) |
| 2026-07-24 | **T2 twopi/neato shape=plain init defect FIXED — falsy-zero clobber** | Mechanism: port neatoInitNode/twopiInitNode carried `if (!width) width = 0.75` defaults that C's neato_init_node does not have (neatoinit.c:60-66 = common_init_node + ND_pos + gv_nodesize only). JS falsy test treated the LEGITIMATE 0 from poly_init's IS_PLAIN branch (shapes.c:1962; port poly-sizing.ts:138 — probe confirmed lw/rw/ht=0, sizing ran) as unset and clobbered it. Origin: layout/neato/init.ts + layout/twopi/init.ts. Ruled out: central commonInitNode (dot shares it, correct), measurer absence (probe), emission. A pure `=== undefined` guard broke 4 tests: NodeInfo zero-initializes width (calloc mirror) and two PORT-ONLY paths legitimately rely on the unset-0 fallback (no-measurer initNodeDefaults sets only lw/rw/ht; poly-null custom/epsf shapes never assign width — C gives customs BOX geometry, port models polygon:null — modeling gap FLAGGED as follow-up). Contained fix: default on falsy width EXCEPT bound shape name 'plain' (the only C case where 0 is a legitimate post-init size); the true origin fix lands in src/common (dot-shared stop zone) — deferred with this note instead. Verified: shapes-reference-plain byte-matches native on twopi/neato/osage/patchwork/dot; 44 shape=plain corpus ids re-verdicted on 5 engines xdot+plain — ZERO verdict deltas vs pre-fix (one sfdp load-flake re-run to pass). 2619_1/2619_2 NOT resolved by this (their plain nodes are labeled/nonzero) — stay in T5. | src/layout/neato/init.ts, src/layout/twopi/init.ts, src/layout/neato/neato.test.ts | tsc; 3272 tests; plain ×5 engines 0 diverged; xdot Δ=0 |
| 2026-07-24 | **T3 circo 2095_1 = A1 irreducible (injection-proven)** | Mechanism: getRotation's closest-node selection (circpos.c:79-91; port position-helpers.ts closestNode) is an exact-tie knife edge — this auto-generated flowchart contains THREE copies of a symmetric 5-node block whose cut vertex (1090/1128/1615) and mirror node (1089/1127/1614) are equidistant from the rotated origin in real arithmetic. V8's trig chain yields an exact FP tie (both distances 1.9333108975227196) so strict `<` keeps the neighbor → rot=0; Apple libm's chain gives the neighbor +1 ULP (…198) so the mirror wins → rot 0.15/0.09/0.03 rad → three rigid block rotations of ≤5.2°/66pt, 55 nodes cascade (2197 xdot diffs). Instrumentation: matched PLACE streams (483 rows) diverge ONLY on those 3 rot values; per-candidate distance dumps confirm the tie/ULP split. Injection A/B: nudging just the 3 cut vertices' y by ~2 ULP to libm's value → port matches oracle EXACTLY (2197→0 diffs @0.01) — the entire downstream pipeline (rotation formulas, block tree, splines) exonerated. Ruled out: cycle order (identical), radii (≤1pt), block structure (streams align), rotation formulas. Same class as twopi 1855 radial mirror. Accepted A1 in engine registry + json registry rationale upgraded. C instrumentation reverted (circpos.c clean, plugin rebuilt). | test/corpus/accepted-divergences-engines.json, accepted-divergences-json.json | tsc; circo tests 10/10; C tree circpos.c clean |
| 2026-07-24 | **T4a fdp graphs-b53 FIXED — invented set-gate on root/cluster label emit** | b53 is NOT a layout defect: native fdp ALSO degenerates (agerrorf "node node_50 contained in two non-comparable clusters", layout aborts, all nodes 0,0 — port faithfully replicates via derive.ts null-return, verified truncation-for-truncation). The one real diff: native still DRAWS the root label in the abort path because C's emit gates root-graph and cluster labels on EXISTENCE only (emit.c:3656 `if (GD_label(g))`, 3920; emit_label itself has no ->set check) — `->set` is gated only at the xlabel (emit.c:1829) and edge-label (emit_edge_label:2891) call sites. The port's renderOneLabel required `set` for ALL labels, silently suppressing unplaced labels wherever C draws them (fdp abort path: gv_postprocess skipped → set=false). Fix: requireSet param, default true (xlabel/edge sites keep C gates), root+cluster sites pass false. Safe by construction for passing ids: any id whose output changes was previously MISSING native-drawn content (= already non-passing). b53 fdp xdot diverged→pass, json accepted→conformant (registry row removed). Colocated device.test.ts expectation updated to the C-faithful behavior. | src/gvc/device.ts, src/gvc/device.test.ts, registries, fdp parity files | tsc; 3272 tests; fdp json 0 diverged; dot json 60-id smoke clean |
| 2026-07-24 | **T4b fdp 2620/badvoro/1879 (+2108/1652) = chaotic-regime drift (measured)** | Whole-layout divergences (30/30 nodes on 2620 up to 5.4kpt; 1235/1235 on badvoro median 1.4kpt). Perturbation experiments prove chaos: 2620 node width +0.001in moves the port's OWN layout median 1120pt/max 3390pt; badvoro ONE edge len +0.0001 moves all 1235 nodes median 322pt/max 1322pt — same magnitude as port-vs-native. fdp spring layout separates basins on sub-ULP dust; V8-vs-libm boundary, no port stage implicated (extends to 1879/2108/1652, same engine+pipeline; 2108/1652 additionally exceed the xdot port budget = 2621 perf class). Engine-track rows stay in the documented fdp drift tail (consistent with its 149 peers); json/map registry rationales upgraded from blind-spot placeholders. | registries | port-side A/B experiments; tsc+tests green (unchanged src) |
| 2026-07-24 | **T5 neato 2619_2 + sfdp 2619_1/1879 = A1 drift with stated mechanisms** | neato 2619_2: all 6 nodes within 0.13-0.18pt (in-bar); the only over-bar values are 4 x-coords of ONE sub-point-long stub spline between overlapping nodes — clipping a near-zero-length bezier amplifies in-bar endpoint drift to 0.81pt; no misport. sfdp 2619_1: 0.7-1.3pt spring drift on 6 nodes (documented chaotic-engine regime). sfdp 1879: whole-layout basin divergence (7.6kpt) — sfdp chaos, same family as the perturbation-proven fdp 1879. All registry rationales upgraded; ZERO blind-spot placeholders remain in any registry. | accepted-divergences-{json,map}.json | audit: 0 placeholders across all 3 registries |
| 2026-07-24 | WASM head-to-head perf report | **Added dot-engine vs `@hpcc-js/wasm-graphviz` warm/in-process bench as committed infra** — peer to bench.mjs but a DIFFERENT axis (vs the browser WASM alternative; a report, not a gate — we do not gate on beating compiled C). `test/corpus/wasm-bench.mjs`: single-process, self-re-execs with `--expose-gc`, best-of-3 (min), same corpus-manifest + engine-per-input. Excludes native>2s heavy AND DOT-source>1MB large inputs — both listed, never silent (per "no silent caps"). `perf-dashboard.mjs` folds a "dot-engine vs WASM" section into PERF.md via optional read of `wasm-perf.json` (absent → section omitted). **Result over 765 compared inputs: ratio de/wa p50 3.63× · p90 4.53× · max 7.60× · mean 3.58×** (509/765 in 2–4×; confirms the ~3.5–4.5× V8-vs-C constant factor). Diagnosis: (1) **NO leak** — forced-GC probe showed flat ~17MB/render; the naive full-corpus OOM was deferred-GC under a tight no-yield loop stacked on giant single renders (harness artifact, not a defect); nearly mis-reported as a leak before the GC check. (2) 4 multi-MB disassembly/CFG dumps (`1864` 14MB, `2064` 5MB, `2475_1` 9.5MB, `2593` 13.5MB, ~10k+ nodes) excluded by source-size filter. CORRECTION vs first draft: these are hard for Graphviz GENERALLY, not port-specific — spot-checked, 2064 fails in native `dot` (>120s), WASM (>60s in layout()), AND the port (>30s) alike; 1864 OOMs BOTH the port (>1GB) AND the WASM build ("out of memory") while native times out. Initial "WASM handles them" claim was an UNVERIFIED assumption (size filter excluded them before either engine was timed) and was WRONG — corrected in wasm-bench.mjs/perf-dashboard.mjs/PERF.md. No 1864/2064 bug filed: intractable for all engines incl. upstream C, not a port defect. Resolution: README "Known limitations" bullet documents the scale ceiling + recommends **pre-render to SVG at build time** (serve static .svg), pointing to knowvah/dot-plugins build-time SSG adapters on NPM. NO GitHub issue created. Method note for future: never assert oracle/WASM behavior on an input the harness never actually ran. Also fixed a stale generator string: perf-dashboard.mjs still emitted `dot-engine` (commit 0352830 scrubbed PERF.md output but not its source) → now `@knowvah/dot-engine`. `@hpcc-js/wasm-graphviz@1.28.0` added as exact devDep (Apache-2.0, dev-only, EPL-compatible). Ref bundle sizes: min gzip ~233KB (port) vs ~621KB (WASM). | test/corpus/wasm-bench.mjs, test/corpus/wasm-perf.json, test/corpus/perf-dashboard.mjs, test/corpus/PERF.md, package.json | tsc 0; docs:copy-reports mirrors section; no src changes (tests unaffected); not committed (awaiting user) |
| 2026-07-25 | **T6 fdp map timeout ids classified — load artifacts, not perf class** | graphs-root / graphs-b103 / nshare-root_twopi / linux.x86-root_twopi: solo timings port 63-109s vs oracle 38-47s = 1.7-2.3x (well under the >=10x 2621 perf-quarantine bar); the diverged<->timeout flips were 8-way sweep-load artifacts against the 180s budget. Serial re-verdict (MAP_CONCURRENCY=1): all four complete and land on their existing fdp drift-class registry rows; map-parity-fdp now timeout=0, diverged=0. Rationales annotated with the classification + the concurrency-1 re-verdict recipe. | accepted-divergences-map.json, map-parity-fdp.json(l) | serial pass verdicts stable; counts clean |
| 2026-07-25 | **Mission parity-followups CLOSED** | All deferred work resolved: T1 engine-walk classifier fix (40 phantom oracle-errors → pass; patchwork xdot 100%); T2 shape=plain falsy-zero clobber fixed (byte-match ×5 engines, 44-id re-verdict Δ=0); T3 circo 2095_1 A1 injection-proven (2197→0 diffs with 2-ULP tie nudge); T4 b53 root/cluster-label emit gate fixed at the C call-site semantics + fdp family chaos-proven by perturbation A/B; T5 neato stub-clip amplification + sfdp drift mechanisms; T6 fdp map timeouts = load artifacts (1.7-2.3x, not perf class). 3 src fixes, 6 engine-registry A1 entries, 0 blind-spot placeholders anywhere, every PARITY delta an improvement. Deviation flagged: b53 fix touched dot-shared src/gvc/device.ts without pre-commit confirm (see mission journal; commit be31756). | PARITY.md + registries + engine tracks | tsc + 3272 tests + docs:build green |
| 2026-07-26 | **`-Tdot` track: comparator + renderer landed; agwrite emitter defect FOUND (not yet fixed)** | Motivation: `apply.c` caller scan showed `agapply` is load-bearing for attribute propagation (`attr.c:287` installs a new *global* graph-attr default on every existing subgraph **eagerly**; the port resolves lazily via `aggetGraph` walk-to-root). Equivalent for attribute *reads*, so layout is unaffected — divergence can only surface where an object's attribute *set* is enumerated, i.e. `agwrite`. That surface was unmeasured: the xdot comparator compares a **whitelist** (POSITIONAL_ATTRS ×11 + the `_draw_` family), never the declared attribute set or the subgraph tree, and there was no `-Tdot` track at all (33 tracks = SVG/xdot/json/plain/imagemap only). **Defect found — mechanism:** `-Tdot` dispatches to `DotRenderer` (src/render/dot.ts:555), a flat streaming emitter, instead of `attach_attrs` + agwrite. In C both FORMAT_DOT and FORMAT_XDOT terminate in `agwrite(g, job)` (gvrender_core_dot.c:475,482) and differ ONLY in the attach step (`:404 attach_attrs` vs `:418 attach_attrs_and_arrows` + `xdot_begin_graph`). The port already HAS a faithful agwrite port — `XdotRenderer.serialize` (:1211, `@see lib/cgraph/write.c:agwrite`, walks write_subgs/write_body with preorder scoping) — but `DotRenderer` shares none of it, so `-Tdot` emits **no subgraphs** (cluster members flattened into root), **no `node [label="\N"]`** default line, and **drops input graph attrs** (`rankdir=LR` absent). Causal chain confirmed on a 9-line 2-cluster fixture: 10 diffs — 2 `attr-set` (rankdir, node/label), 6 `extra` (flattened members at root), 2 `structure` (both clusters absent). Ruled out: mis-invocation (`-Txdot` through the same `render()` path emits correct structure incl. both clusters), comparator blindness (none existed), and the eager-vs-lazy question itself (a far smaller delta, unobservable beneath a structural defect this size). Deliverables this pass: `test/corpus/render-one-dot.ts` (subprocess renderer, mirrors render-one-plain.ts) and `test/golden/compare-dot.ts` — a semantic agwrite comparator that compares subgraph TREE shape + per-object declared attribute NAME SETS + values (0.01 numeric tolerance / exact otherwise), normalizing agwrite formatting noise (`.75` vs `0.75`, attr order, bracket-list whitespace). 18 tests assert BOTH directions — passes on cosmetic-only reformatting, and fails on each class it claims (structure, attr-set, numeric-beyond-tolerance, value, spline-marker, point-count, anon-scope count, parse, AD-4 iterative). **Walker + parity-report registration deliberately NOT built yet:** with the emitter defect live, a 762-id sweep would seed the acceptance registry with hundreds of rows for ONE root cause — recording a single bug as a characterized tail. Fix the emitter first (route `dot` through the agwrite serializer with positional-only attrs), then sweep. **Also fixed en route:** `/tmp/ghl` was missing `config8`, so the headless oracle could load NO format — every corpus sweep would have failed until regenerated (`GVBINDIR=/tmp/ghl dot -c`). | test/golden/compare-dot.ts, test/golden/compare-dot.test.ts, test/corpus/render-one-dot.ts, plans/port-catalog/README.md | tsc 0; compare-dot 18/18; oracle env repaired. No src/ change — port behavior untouched, existing tracks unaffected by construction |
| 2026-07-26 | **`-Tdot` emitter FIXED — dot now shares the agwrite serializer (structure defect closed)** | **Mechanism (from the previous entry):** `-Tdot` used `DotRenderer`, a flat streaming emitter, so it could not emit the subgraph tree (a streaming renderer writes before the graph is walked). C has ONE renderer with a `job->render.id` switch: FORMAT_DOT = `attach_attrs(g)` → `agwrite(g)`; FORMAT_XDOT = `attach_attrs_and_arrows` + `xdot_begin/end_graph` → *the same* `agwrite(g)` (gvrender_core_dot.c:404,418,475,482). **Fix mirrors that shape:** `DotRenderer extends XdotRenderer` with `emitDraws=false`; the sole difference is the xdot draw-attribute step, gated at ONE point (`drawsOf()` returns undefined when !emitDraws) plus three call-site gates (cluster's unconditional `_draw_`, `xdotversion`, the endGraph GDraw/GLabel flush) and no-op drawing callbacks (`-Tdot` renders no ops). Deleted the now-orphaned `dotBeginGraphHeader` and `formatNodeAttrs` (0 non-test callers; the latter duplicated the serializer's `nodeAttrs` — two node-attr formatters is the drift shape this bug WAS) + an unused `ObjState` type import. **`-Txdot` byte-unchanged by construction** (every gate is `if (emitDraws)`, true for XdotRenderer) and verified byte-identical on the 2-cluster fixture. **Result on the fixture: 10 diffs → 2.** Smoke over 18 cluster-bearing dot corpus ids (13 comparable, 5 oracle-error): **zero `structure`/`missing`/`extra` diffs** — subgraph tree and per-scope node/edge scoping now match the oracle everywhere — and zero new numeric diffs, so no geometry moved. Residual 104 `attr-set` + 2 `value` = **defect (B), NOT fixed here: the serializer echoes no INPUT attributes at all** (`write_nondefault_attrs`). Native emits `rankdir=LR` on root and `rankdir=""` on a cluster declared before the attr (the `attr.c:287` eager-`agapply` artifact); the port emits neither. **(B) affects `-Txdot` identically** and is invisible there because compare-xdot whitelists 11 POSITIONAL_ATTRS + the `_draw_` family — i.e. the xdot track's 761/761 does not cover attribute sets. Fixing (B) changes xdot output corpus-wide, so it needs its own change + fresh sweep; left for that. Also found: 1 real `numeric` (1435 `edge:8->10#0/pos`, Δ0.30) — pre-existing, shared `formatEdgePos`, unrelated to this fix. **Comparator hardened (it was lying twice):** (a) agwrite's `\`+newline 128-byte continuation (write.c:113) was not unfolded, so every long spline `pos` read as truncated → 8 false `value` diffs, now 2; (b) `numericFields` stripped only a LEADING `[es],` marker, but the format is `s,X,Y e,X,Y p...` so `e` appears after the first pair — real drift was silently downgraded from `numeric` to exact-compare `value`. Both now tokenize markers wherever they occur, with regression tests. Walker + parity-report registration still deferred until (B) lands, else the registry gets hundreds of rows for one root cause. | src/render/dot.ts, src/render/dot.test.ts, test/golden/compare-dot.ts, test/golden/compare-dot.test.ts, plans/port-catalog/README.md | tsc 0; npm test 3292/3292 (236 files); xdot byte-identical on fixture; 13-id dot smoke 0 structural diffs |
| 2026-07-26 | **(B) input-attribute echo — design pinned, NOT implemented (mission-scale; needs fresh sweep)** | Investigated the full C contract before touching the serializer, and it is materially larger than (A). **What C does:** `write_hdr` emits the header then `write_dicts` (write.c:307) = three per-scope statements `graph [...]` / `node [...]` / `edge [...]` from the scope's attribute DICTS; a graph's attributes ARE its dict defaults (cgraph conflates them — hence the root's computed `bb` appearing inside `graph [...]`, which is write_dict output, not a separate object block). Per-object input attrs come from `write_nondefault_attrs` (write.c:471), which iterates the applicable defdict and emits `sym->name=data->str[sym->id]` whenever that value **differs by POINTER** from `sym->defval` (refstr identity, not strcmp) — skipping the Tailport/Headport symbols, which are written as `:port` syntax instead. **Discovery 1 — ordering:** `AgDataDictDisc` (attr.c:34) keys on `offsetof(Agsym_t,name)` with size -1, comparf NULL, opened `Dttree` ⇒ dtfirst/dtnext iterate in **strcmp order**. Oracle confirms: `graph [bb, rankdir]` and `a [height, pos, width]`. The port currently emits node attrs in insertion order (`pos width height`) — so (B) also **reorders existing computed attributes in BOTH `-Tdot` and `-Txdot`**. Semantic comparators (compare-xdot, compare-dot) normalize attribute order so verdicts should hold, but this is a byte-level change to the project's most-conformant track and cannot go in unswept. **Discovery 2 — the eager artifact needs NO model change.** C's `rankdir=""` on a subgraph opened BEFORE `rankdir` was declared comes from `attr.c:287 agapply(root, addattr, rsym, true)` installing the symbol with its pre-declaration (empty) default on already-existing subgraphs. The port can reconstruct this exactly from data it already has: for subgraph S and graph-attr A, if A is in the root's attrs but NOT in `S.graphDefaultsSnapshot`, then A was declared after S opened ⇒ C would carry a local `A=""` on S. Verified against the fixture: cluster_0 (opened first) qualifies, cluster_1 (opened after) does not — matching the oracle. So no change to `src/model` attribute storage is required; `nodeDefaultsSnapshot`/`graphDefaultsSnapshot` already encode C's creation-time defaulting. **Remaining work (deliberately not started):** port `write_dicts` incl. the empty-defval-and-!print skip rule with its non-top parent-view comparison (write.c:271-280); merge input attrs into the graph/cluster/node/edge blocks; sort every block strcmp; reproduce the eager artifact per Discovery 2; match the multi-line layout (`,\n`+indent for dicts with >1 entry, `\t[` for object blocks). Then a **full fresh 33-track sweep** — the change is corpus-wide on xdot, and 42 dot cluster goldens assert xdot strings, so partial landing risks the best surface in the port. Recommend running it as a mission brief rather than inline. | (none — investigation only) | tsc 0; npm test 3292/3292 unchanged; no src/ modified by this entry |
| 2026-07-26 | **(B) task 1 of N: `write_dicts` ported + sweep-gated** | Isolated first slice of (B): the per-scope default-statement MACHINERY, no input-attribute echo yet. **Ported** `writeDicts` (write.c:307) + `writeDict` (write.c:262) replacing three hardcoded emissions — root `graph [<computed>]`, the hardcoded `node [label="\N"]`, and the subgraph `graph [...]` line. Now emits `graph`/`node`/`edge` in C's order, per scope, from the scope's own maps: graph ← the existing computed producers (`graphAttrs`/`clusterAttrs`/`subgGraphAttrs`, converted to return parts rather than a joined string so they can be sorted), node ← `g.nodeDefaults`, edge ← `g.edgeDefaults`. **Ordering:** entries sort by strcmp of attribute name — cgraph's attr dicts are `Dttree`s keyed on `Agsym_t.name` with NULL comparf (attr.c:34), so dtfirst/dtnext walk sorted; the port previously emitted insertion order. **Layout:** exact write_dict shape — 1 entry stays inline, ≥2 break after each `,` with the body one level deeper and `];` back at statement level (the final entry carries no trailing comma). Verified against the oracle: port xdot root dict now `graph [_draw_="…",\n\t\tbb="…",\n\t\txdotversion=1.7\n\t];` vs native identical modulo the still-missing `rankdir=LR`. **`label="\N"` synthesized at serialization time** rather than installed into `nodeDefaults` — dot's graph_init sets an AGNODE label default of NODENAME_ESC (input.c:737-739), but the port deliberately reproduces that default at use-sites (see the N_label note, common/graph-init.ts:350); putting it in `nodeDefaults` would feed node label RESOLUTION and move layout. An explicit root `node [label=…]` wins. Dict values now go through the `_agstrcanon` port (`agstrcanonText`, map.ts:174) so quoting is conditional like write_canonstr — which fixed a pre-existing nit: `xdotversion` was hardcoded-quoted, native emits it bare (`xdotversion=1.7`); the unit test asserting the quoted form encoded the non-faithful behavior and was corrected. **Not ported (unreachable):** write_dict's `EMPTY(defval) && !sym->print` skip and its non-top parent-view comparison (write.c:271-280) — every entry we hold was explicitly declared or computed, i.e. `print` set; an `agapply`-installed empty default is the only `print == false` producer, so that branch activates only when the eager artifact lands (task 2). 13 new tests (`dot-write-dicts.test.ts`) cover presence/scoping, the inline-vs-multiline layout, indentation, strcmp order, conditional quoting, explicitly-empty survival, and xdot/dot dict parity. **Sweep gate: XDOT_LIMIT=150 xdot survey → 150 conformant / 0 diverged.** The layout change touches every graph, so a regression would have surfaced immediately. **HAZARD hit and repaired:** `--survey` overwrote the git-tracked `test/corpus/xdot-parity.json` (761-id published baseline) with the 150-id subset; restored via checkout and verified total=761. Bounded surveys must not be run against tracked parity files without restoring — same class as the pgrep-before-handoff note. Remaining for (B): input-attr echo into all four block types, the eager artifact via `graphDefaultsSnapshot` (design pinned in the prior entry), then a FULL 33-track sweep. | src/render/dot.ts, src/render/dot.test.ts, src/render/dot-write-dicts.test.ts | tsc 0; npm test 3305/3305 (237 files); xdot 150-id survey 0 diverged; xdot-parity.json restored to 761 |
| 2026-07-26 | **(B) task 2a: graph-scope input-attribute echo + eager artifact — sweep-gated** | Task 2 as scoped is two INDEPENDENT code paths (the graph-scope dict vs the per-object node/edge blocks); landed the graph-scope half, which is where the design discovery applies. **`graphInputParts(g, top)`** echoes a scope's own graph attributes into its dict — in cgraph a graph's attributes ARE its dict defaults, which is why the root's computed `bb` and an input `rankdir` share one `graph [...]` statement. Computed names (`_draw_ _ldraw_ bb lp lwidth lheight xdotversion rank`) are excluded via `COMPUTED_GRAPH_ATTRS` so they are not echoed twice. **Non-root scopes skip inherited values** (`snapshot.get(k) === v`): the builder deliberately SEEDS inherited label/font keys into subgraph `attrs` so cluster-label inheritance survives the layout's cluster rebuilds (builder.ts:257-265), and echoing those would print attributes on clusters where C prints none. Stated cost: a subgraph re-declaring an attribute to the value it already inherited is treated as inherited and skipped, where C emits it — a value-equality approximation of C's refstr-POINTER comparison (`data->str[sym->id] != sym->defval`). **Eager-propagation artifact reproduced with no model change**, per the design pinned two entries back: `agattr` creating a new GLOBAL graph attribute runs `agapply(root, addattr, rsym, true)` (attr.c:287), installing the symbol with its pre-declaration empty default on every ALREADY-EXISTING subgraph — so a subgraph opened before the declaration prints `rankdir=""` while a sibling opened after inherits and prints nothing. `graphDefaultsSnapshot` records exactly which defaults were live when a scope opened, so "declared at root after this scope opened" is `root.attrs.has(k) && !snapshot.has(k)`. **Fixture now compares 0 diffs against the oracle** (was 10 before the (A) fix, 2 after it) — including `rankdir=""` on cluster_0 and its correct absence on cluster_1. **Gates:** tsc 0; npm test 3305/3305 (5 new eager/echo tests, 18 in dot-write-dicts.test.ts); **XDOT_LIMIT=200 xdot survey → 200 conformant / 0 diverged**; 13-id dot smoke still ZERO structure/missing/extra/regressed-numeric, attr-set 104→79. Remaining 79 attr-set are **50 edge + 26 node + 3 subgraph** — i.e. the deferred per-object `write_nondefault_attrs` half (task 2b: emit node/edge attrs differing from `nodeDefaultsSnapshot`/edge equivalent, skip the Tailport/Headport symbols which write as `:port` syntax, refactor `nodeAttrs`/`edgeAttrStr` into sorted parts, and match write_nondefault_attrs' layout — `name\t[` with `,\n`+indent and NO newline before the closing `]`, unlike write_dict). The 3 subgraph residuals are worth checking first: they may be the re-declared-to-inherited-value approximation above. Also still open: 1 pre-existing numeric (1435 `edge:8->10#0/pos`, Δ0.30). Baseline hygiene: saved and restored `xdot-parity.json` (761) around the bounded survey — verified clean. | src/render/dot.ts, src/render/dot-write-dicts.test.ts | tsc 0; 3305/3305; xdot 200/200 conformant 0 diverged; dot smoke 0 structural |
| 2026-07-26 | **(B) task 2b: `write_nondefault_attrs` ported — (B) COMPLETE, `-Tdot` byte-identical on the fixture** | Ported the per-object half (write.c:471). `objInputParts(attrs, defaults, computed)` emits an object's input attributes that differ from the applicable default — C's `data->str[sym->id] != sym->defval`, a refstr-POINTER test; value equality is the port's approximation and agrees wherever C would (an explicit value equal to the default is the same interned refstr, so C omits it too). Nodes compare against `n.nodeDefaultsSnapshot`. **Edges have no stored snapshot** — the builder copies effective edge defaults straight INTO `edge.attrs` (builder.ts:294), so a defaulted value is indistinguishable from an explicit one by field; added `effectiveEdgeDefaults(scope)` mirroring that walk and compare against it. `tailport`/`headport` are excluded because write_nondefault_attrs skips those symbols — they are written as `:port` syntax on the endpoints (write.c:487-492). `nodeAttrs` converted from string accumulation to parts; `edgeAttrStr` takes the scope graph. **New `objAttrBlock`** implements write_nondefault_attrs' layout, which differs from write_dict in two ways I had to get right: the block opens with a **literal TAB** before `[` (not a space — the port used a space everywhere), and the closing `]` follows the last value directly with **no** newline+indent. Entries sort strcmp like every dict. **Result: `-Tdot` on the 2-cluster fixture is now BYTE-IDENTICAL to the oracle** (10 diffs → 2 → 0 → byte-equal across A/2a/2b). **Follow-on bug found and fixed during triage:** `rank` was in `COMPUTED_GRAPH_ATTRS` because the plain-subgraph path self-emits it — but `clusterAttrs` does not, so clusters silently lost `rank=same`. Removed it from the exclusion set and dropped the special-case push, letting `graphInputParts` own `rank` on both paths. This is exactly why the 3 subgraph residuals flagged at the end of 2a were worth checking first. **Gates:** tsc 0; npm test 3310/3310 (237 files); **XDOT_LIMIT=200 xdot survey → 200 conformant / 0 diverged** (re-run after the rank fix); 13-id dot smoke pass 4→9, **byte-identical 8/13**, attr-set 104→79→**1**, still ZERO structure/missing/extra. Test-side fallout, all encoding the old space separator rather than real breakage: `nodeinit.test.ts`'s `nodeBlock` matched `name [` and returned only the FIRST line (blocks are now multi-line) — rewritten to collect the whole block; plus one assertion each in `dot.test.ts` and `oracle-segfault-pack-newrank.test.ts`. **Remaining, both pre-existing and unrelated to (B):** 1 attr-set (a subgraph re-declaring an attribute to the value it already inherits — the documented value-equality approximation of C's pointer test), 2 `value` and 1 `numeric` (1435 `edge:8->10#0/pos`, Δ0.30). Next: the `-Tdot` walker + parity-report registration, now worth building since the format is conformant rather than structurally broken. Baseline hygiene: `xdot-parity.json` saved/restored around both surveys, verified 761. | src/render/dot.ts, src/render/dot.test.ts, src/common/nodeinit.test.ts, test/corpus/oracle-segfault-pack-newrank.test.ts | tsc 0; 3310/3310; xdot 200/200 0 diverged; dot smoke 8/13 byte-identical, 0 structural |
| 2026-07-26 | **`-Tdot` walker built + track registered (survey PARTIAL — 60 of 761)** | `dot-walk.ts` adapted from `plain-walk.ts` (497→487 lines): dual-format spine collapsed (agwrite has no `-ext` counterpart, so `walkOne` resolves one format and `worstVerdict` is gone), comparator/renderer swapped to `compareDot`/`render-one-dot.ts`, env prefix `DOT_*`, artifacts `dot-parity-<engine>.json(l)`, oracle cache namespace `dot-corpus-agwrite-oracle`. **Two bugs found by running it, both real:** (1) the inherited completeness check tested `lines[last] === 'stop'` — a *plain*-format marker; agwrite ends with `}`, so all 25 items scored oracleError. Kept as a completeness check (not exit code) per the oracle-exit≠validity rule, with the right marker. (2) The first broken run's oracleError rows were then REPLAYED from `dot-parity-dot.jsonl` on re-run — the resume path served poisoned rows and the fix looked ineffective until the JSONL was deleted. Third instance of the resume-hides-truth hazard; deleting the JSONL is mandatory after any walker change. **Comparator bug found by the sweep:** `isIdChar` was ASCII-only, so a Latin-1 node name (`ÿ`, id 1724) terminated the ID early and that node's attributes were mis-attributed to the enclosing graph scope — surfacing as bogus `[graph]/height`, `[graph]/pos`, `[graph]/width` attr-set diffs plus an `extra` edge. C's scanner treats bytes ≥0x80 as id chars (same note as map.ts's `_agstrcanon`); fixed, 1724 now passes. Registered in `parity-report.ts` as track `dot (dot)` reusing `plainRow` (report shapes match) with a dot-keyed detail mapper; `formatDetailMarkdown`'s surface union gained `'dot'`; generates `PARITY-dot-dot.md`. Seeded `accepted-divergences-dot.json` with 1435 (Δ0.30 `edge:8->10#0/pos`) and its mechanism — pre-existing, shared `formatEdgePos`, with the flagged follow-up that if the xdot track compares that id cleanly then the two comparators disagree and THAT is the bug. **The published row is a 60-id survey, NOT a baseline** — 56 pass / 4 diverged / 93.3%, sitting beside 761-id tracks in PARITY.md and honestly labelled `surveyed=60`. Do not treat it as the track's conformance record; the remaining step is a full 761-id run (`rm dot-parity-dot.jsonl` first), then triage the 4 diverged into the registry with mechanisms. Regeneration touched only PARITY.md's new row — no churn to other tracks' pages. | test/corpus/dot-walk.ts, test/corpus/parity-report.ts, test/corpus/accepted-divergences-dot.json, test/golden/compare-dot.ts, PARITY.md, PARITY-dot-dot.md | tsc 0; npm test 3310/3310; compare-dot 20/20; dot walk 60: 56 pass / 4 diverged / 0 errors |
| 2026-07-26 | **`-Tdot` full 762-id survey + diverged triage — 66 ids are PORT BUGS with mechanisms, 0 accepted** | Full survey (resume JSONL deleted first): **619 pass / 142 diverged / 1 oracle-error / 0 port-error / 0 timeout = 81.2%**. PARITY.md regenerated, superseding the provisional 60-id row; PARITY-dot-dot.md now 762 surveyed. **Triage — four classes, bucketed on each id's FIRST diff so they are not strictly disjoint and must be re-tallied after each fix:** **(A) 28 ids — `bb` eager artifact missing.** Native emits `bb=""` on subgraphs, port omits. My bug: `graphInputParts`' eager loop inherits the `COMPUTED_GRAPH_ATTRS` exclusion, which exists only to stop double-emission in the LOCAL pass. `attach_attrs` declares `bb` at root AFTER subgraphs exist, so C's agapply installs an empty local `bb` on every pre-existing subgraph — the same mechanism already working for `rankdir`, just filtered out. Fix: drop the exclusion in the eager pass only. **(B) 11 ids — computed `width`/`height` not suppressed.** Port pushes them unconditionally in `nodeAttrs`; native omits them when equal to the node-dict default (0.75/0.5), because C's `agxset` stores the same interned refstr as the defval so `data->str[sym->id] != sym->defval` is false. Fix: apply the same default-equality suppression already used for input attrs. **(C) 27 ids — the inherited-skip over-fires.** `labelloc`/`fontcolor`/`shape`/`style` locally declared on a subgraph but dropped by the `snapshot.get(k) === v` test — the value-equality approximation of C's refstr-POINTER comparison documented in task 2a. 27 is too many to accept: needs a real provenance discriminator between "locally declared" and "seeded by the builder" (`GRAPH_LABEL_INHERIT_KEYS`, builder.ts:257-265), e.g. marking the seeded writes rather than inferring from value. **(D) 57 ids — label value diffs, probably mostly NOT ours.** Two subgroups: HTML-table labels (port emits markup, native something else — needs its own diagnosis) and Latin-1 ids where native emits replacement bytes (`???`) against the port's correct `áâãäåæ…`, matching the `canon()` use-after-free A4 class already documented for the plain track (unpooled converted label text freed before print). Confirm per id before accepting any. **(E) 19 ids — uncharacterized, need individual inspection.** **Nothing added to accepted-divergences-dot.json** — it still holds only 1435; A/B/C (66 ids) are actionable port bugs, and A+B alone are mechanical and should clear ~39. | test/corpus/{dot-parity-dot.json,dot-parity-dot.jsonl,PARITY.md,PARITY-dot-dot.md} | full 762 survey clean run; tsc 0; npm test 3310/3310 |
| 2026-07-26 | **-Tdot track: computed-attr default gate + empty graph-attr echo (38 ids)** | TWO mechanisms, both confirmed against the C before any edit. (B) `safe_dcl` (common/utils.c:1065) declares a computed name with an empty default ONLY when the symbol does not already exist — an input `node [width=0.5]` leaves `sym->defval` at `0.5`, so `agxset`ing a computed `0.5` interns to the SAME refstr and write_nondefault_attrs' `data->str[sym->id] != sym->defval` test (write.c:485) drops it. Ground truth: tests/graphs/arrows.gv emits `height=0.5` on every node and `width` on none; the port pushed computed pos/width/height/xlp/rects (and edge pos/lp/xlp/head_lp/tail_lp) unconditionally. Gated via `computedPart()`; arrows.gv is now byte-identical to the oracle. (A) NOT the eager-agapply artifact first hypothesised — tests/share/KW91.gv is a re-fed dot output whose `{rank=same}` blocks literally carry `bb=""`/`lp=""` in the INPUT, and write_dict (write.c:271-278) skips an empty local value only when the scope has no parent or the INHERITED value is also empty. Pinned by 4-way oracle probe: root-absent+child-empty PRINTS, root-"x"+child-empty PRINTS, root-empty+child-empty SKIPS, root-"x"+child-"x" PRINTS. The port dropped these because bb/lp/lwidth/lheight are in COMPUTED_GRAPH_ATTRS and re-enter through `echoAttr`, which discards empties; new `echoGraphAttr()` applies the same local-vs-`graphDefaultsSnapshot` test `graphInputParts` uses and keeps empties off the root only. Also removed dead `pfx()` (unreferenced after the preceding agwrite refactor) and gave the `-Tdot` track its own label/walker in parity-report.ts (it was rendering as the imagemap track). | src/render/dot.ts, test/corpus/parity-report.ts, test/corpus/PARITY-dot-dot.md, test/corpus/dot-parity-dot.{json,jsonl} | tsc clean; npm test 237 files / 3310 tests green; fresh (deleted-JSONL) -Tdot sweep 762 surveyed: pass 619→657 (86.2%), diverged 142→104, **38 fixed / 0 regressions** by per-id verdict delta |
| 2026-07-26 | **-Tdot remaining 104: re-bucketed, class C reopened** | Re-tallied by first diff after the A/B fixes (classes are not disjoint — must re-tally, never subtract). `value label` 57 (HTML label `<...>` angle-bracket round-trip: port emits the table text where native wraps it in `<>`), `attr-set` on a defaulted key 35 (labelloc 16, fontcolor 5, style 4, color 4, shape 3, fontname 2, label 1 — the q4 row above: a value equal to the inherited/default one, which C PRINTS and the port's value-equality test skips), `value pos` 4, remainder 8 singletons. The provenance-marker fix for class C is NOT sufficient as scoped: builder.ts:264 seeds only GRAPH_LABEL_INHERIT_KEYS (label/fontname/fontsize/fontcolor/labelloc/labeljust), but `shape`/`color`/`style`/`distortion`/`penwidth`/`orientation`/`fillcolor` are node-side and cannot come from that seeding — e.g. graphs/pgram.gv `node:A/shape: port=<absent> native=house` where the enclosing `{rank=same; node [shape=house]; A}` makes shape's default house at A's own scope. Needs its own diagnosis of which dict C compares against (write_node's `d` is the CURRENTLY-WALKED subgraph's node dict, write.c:537-545) vs the port's creation-time `nodeDefaultsSnapshot`. Not accepted, not filed as a divergence. | (none — analysis) | accepted-divergences-dot.json unchanged (still only 1435) |
| 2026-07-26 | **-Tdot graph-side provenance: seededAttrs marker (23 ids)** | `graphInputParts` skipped any subgraph attr whose value equalled the inherited snapshot value. That test was never the C's rule — `setattr` (attr.c) gives a subgraph its own dict symbol for every attribute the input declares in that scope, and write_dict prints each local symbol without comparing against the inherited value, so re-declaring an attribute to the value it already inherits STILL prints. Oracle-pinned: `digraph G { foo="x"; { foo="x"; a; b } }` emits `foo=x` on the subgraph. The value test existed only to hide the builder's GRAPH_LABEL_INHERIT_KEYS seeding (builder.ts:264), which writes inherited label-family values into a subgraph's own `attrs` so they survive cluster rebuilds; those carry no dict symbol in C. Fix = record the seeded keys in a new `Graph.seededAttrs` and skip on membership, not value. Verified safe first: for graphs the ONLY non-local `attrs.set` in production code is that seeding line, and no layout path clones a Graph (`pack`'s buildSubgraph copies neither attrs nor snapshot). Also re-derived write_dict's empty-value skip, which cannot be ported literally: `Agsym_t.print` (cgraph.h:644) is declared but assigned NOWHERE in the tree, making the parent-empty skip unreachable, while the `view == NULL` skip would drop the root's own `foo=""` that native prints. Observed rule (4-way probe): skip iff value is empty AND the inherited value is present and empty. | src/model/graph.ts, src/parser/builder.ts, src/render/dot.ts, src/render/dot-write-dicts.test.ts | tsc clean; npm test 237/3310 green + 8 new tests; fresh sweep pass 657→678, diverged 104→81, **23 fixed / 0 regressions**. Two ids flipped pass→timeout under concurrent-load contention (idle standalone: 2095_1 80s, 2343 101s vs a 180s floor) — both recovered on the next idle sweep, NOT verdict changes |
| 2026-07-26 | **-Tdot node-side value record vs writing-scope dict (11 ids)** | Mechanism: a node carries a cgraph value RECORD seeded at creation from the node-dict defaults then in effect (`addattr`, attr.c:210) and overridden by explicit `n [k=v]`; `write_nondefault_attrs` compares that record against `sym->defval` of the dict of the subgraph CURRENTLY BEING WRITTEN (`d`, threaded from write_body via write_node, write.c:537-545) — the dict's FINAL state, since a re-declaration overwrites the same symbol rather than adding one. The port modelled neither half: it treated a node's attribute set as only its EXPLICIT attrs and compared against `nodeDefaultsSnapshot`, the creation-time defaults. Both diverge whenever the effective default changes between creation and write. Oracle-pinned in both directions: `{node[shape=house]; A; node[shape=invhouse]; B}` prints `shape=house` on A only; `digraph{a; node[color=red]; b}` prints `color=""` on `a` (empty record vs non-empty dict — impossible for a record-only walk to produce); `digraph{node[color=blue]; a; {node[color=red]; a; b}}` prints `color=blue` on `a`, which also proves the comparison is against the WRITING scope. tests/graphs/ER.gv shows it at scale (root dict's final state is color=lightgrey/shape=diamond/style=filled, so every earlier node prints color=""/style=""/shape=ellipse). Fix = new `effectiveNodeDefaults(scope)` + `nodeRecord(n)`, and `objInputParts` rewritten to walk the UNION of record and dict keys. dot's synthesized `label="\N"` (input.c:737-739) is a real record value — verified `digraph{a; node[label=x]; b}` prints `label="\N"` on `a` — so it is seeded on BOTH sides under one condition or every node would print a spurious label; extracted as NODENAME_ESC. The class-B computed gate now reads the same writing-scope dict. Edges needed no call-site change: the builder already merges creation-time defaults into `edge.attrs`, so that map IS the record and `effectiveEdgeDefaults(g)` was already the writing scope — they only gained the missing-key half (graphs-shells). | src/render/dot.ts, src/render/dot-write-attrs.test.ts | tsc clean; npm test 238/3325 green + 7 new tests; fresh idle sweep pass 678→689 (90.4%), diverged 81→72, timeout 3→1, **11 fixed / 0 regressions** (8 targeted + 2095, plus 2095_1/2343 recovering from the contention timeouts) |
| 2026-07-26 | **dot.ts complexity + file-length refactor (behaviour-preserving)** | User-directed full scope (split AND CCN reduction), taken with the port-discipline conflict stated up front: CLAUDE.md forbids reshaping ported branch structure, so every change here is strict EXTRACT-METHOD — contiguous branch groups lifted into named helpers, no branch reordered, merged, or simplified, each helper carrying its `@see` back to the C. Stage 1 split dot.ts (1810 lines) along the C-source seams into dot/{xdot-ops,attrs,agwrite,edge-draw,types}.ts. The serializer and the spline/arrow emitters moved as ABSTRACT BASE CLASSES (DotWriterBase, EdgeDrawBase, chained under XdotRenderer) rather than injected collaborators, specifically so all method bodies move byte-identical and stay diffable against write.c/emit.c — they read only `emitDraws`/`clusters`/`drawsOf`/`penwidth`/`styleOp`/`penOp` from the renderer. Stage 2 extracted emitPlainSpline+emitSplineBezier out of beginEdge (CCN 21), emitArrowOp out of emitArrows (15), attachEdgeDraws+resetEdgeState out of endEdge (14, and the reset was duplicated on both paths), backgroundFillOp out of pageBackground (12), edgeDrawParts+edgeLabelPosParts out of edgeAttrStr (17), nodeXlpPart+nodeRectsPart out of nodeAttrs (NLOC 31), eagerEmptyParts out of graphInputParts (13, pre-existing, surfaced by the split); attachEdgeDraws' own 6-way flush became a (state, field, isLabel) table. NOTE: lizard misattributes `emitArrows` as `resolveRenderColor` — its multi-line signature confuses the TS parser; the identifier in the warning is the first one INSIDE the function. Also note ~/.claude/hooks/complexity-ignore already exempts ~/git/graphviz-ts, so none of this was forced. | src/render/dot.ts, src/render/dot/{xdot-ops,attrs,agwrite,edge-draw,types}.ts | tsc clean; npm test 238 files / 3325 tests green; all 6 files <500 lines (dot.ts 1810→494) and ZERO lizard violations (NLOC>30/CCN>10/PARAM>5); fresh deleted-JSONL sweep after Stage 1 AND after Stage 2 both 689 pass / 72 diverged / 1 timeout — **0 regressions**, byte-identical verdicts to the pre-refactor baseline |
| 2026-07-26 | **-Tdot HTML-string dispatch (58 ids)** | Mechanism: the port has `_agstrcanon` (as `agstrcanonText`) but NEVER ported the dispatching wrapper `agstrcanon` (write.c:219), which routes an `aghtmlstr`-flagged value to `agcanonhtmlstr` (write.c:209 - `sprintf("<%s>")`, verbatim, no quoting). So every `label=<<table...>>` was serialized as an ordinary quoted string: the port's HTML marker byte leaked into the output and every `"` inside the markup was backslash-escaped. The grammar already marks HTML values correctly (dot.pegjs HtmlString prefixes U+0001) and `src/common/html-string.ts` already had isHtmlValue/htmlValueContent - only the write-side dispatch was missing, which is why this looked like 59 separate label divergences rather than one absent call. Fix = new `agstrcanon()` in render/map.ts doing the html/plain dispatch; the dot writer's attribute call sites now use it. Deliberately kept `agstrcanonText` named for `_agstrcanon` (the non-html half) rather than adding the branch inside it, so each function still maps 1:1 onto a C function. | src/render/map.ts, src/render/dot/attrs.ts, src/render/dot/agwrite.ts | tsc clean; npm test 239 files / 3334 tests green; fresh sweep pass 689->747 of 762 (**98.0%**), diverged 72->14, **58 fixed / 0 regressions** |
| 2026-07-26 | **-Tdot remaining 14 after the HTML pass** | Re-bucketed: 5 charset (graphs/share/windows-Latin1, b34, b56, b60 - port decodes latin1 to UTF-8 where native emits the raw byte; a DECODE-side question, not agwrite), 4 edge `pos` (2559, 1436, b15, b69 - the pre-existing spline-segmentation/`;`-separator family, incl. accepted 1435's cousin), 5 singletons (clustlabel anon-subgraph label, 2184 fontcolor, 358 xdotversion, 2239 cluster fillcolor). None share a mechanism with the classes closed today; each needs its own diagnosis. | (none - analysis) | accepted-divergences-dot.json still holds only 1435 |
| 2026-07-26 | **-Tdot charset family: oracle-decode + poisoned cache (5 ids)** | Mechanism was in the COMPARATOR, not the writer. With `charset=latin1` cgraph stores the raw input bytes in the attribute and agwrite echoes them, so the oracle's own `-Tdot` output is Latin-1; dot-walk.ts decoded every oracle byte stream as UTF-8, turning each accented char into U+FFFD and making 5 ids (Latin1 x3, b34, b56, b60) look like label divergences when BOTH sides carry the same text. Fix = decode oracle bytes the way dot reads a DOT file (strict UTF-8, fall back to Latin-1 per utils.c:1218), the same rule render-one-dot.ts already applied to the INPUT; the port's UTF-8 output is valid UTF-8 so the strict branch leaves it untouched. SECOND mechanism, and the reason the first fix appeared to do nothing: the oracle cache had been WRITTEN from the already-mangled string, so the corrected read path kept loading U+FFFD. Had to clear the cache (stale-oracle-cache anomaly - invalidate before diagnosing). RESIDUAL, not fixed: the port's own bytes still declare `charset=latin1` while emitting UTF-8, so a byte-level consumer re-feeding that output would double-decode. The library returns a string and the transport encoding is the caller's concern, so the fix belongs in whoever writes the bytes - deferred, flagged here. | test/corpus/dot-walk.ts | tsc clean; cold-cache sweep pass 747->753 of 762, diverged 14->8, **6 fixed / 0 regressions**. 1652 flips timeout->oracleError: cold cache forced native to re-run and IT exceeded the 300s oracle budget - a known pre-existing oracle-side failure (already an oracleError on other tracks), previously masked by a cached result |
| 2026-07-26 | **-Tdot final 8: five unported/misported agwrite mechanisms; 753 -> 759 pass, diverged 8 -> 0** | (1) `formatEdgePos` flattened every bezier into one space-joined run, dropping C's `;` separator between beziers (output.c:353) and reading `spl.list` rather than `spl.size`. (2) Computed attributes hand-wrapped their own quotes, so values never reached the canonicalizer: C agsets raw text and lets agwrite canonicalize it, which is where BOTH the quoting decision (`lheight=0.23` bare) and the 128-byte line breaking live — long `pos` values must wrap with a trailing backslash (write.c:174-190, already ported in `agstrcanonText` but unreachable). `computedPart` now derives the formatted text via `agstrcanon`. Fixed 2559, 1436, b69. (3) `write_port` (write.c:565) was ENTIRELY unported — `A:f0:ne -> B` lost its `:port:compass` endpoint syntax, which is why write_nondefault_attrs skips those two symbols. Fixed b15. (4) `xdotversion` was unconditionally treated as computed; only `xdot_begin_graph` agsets it, so under plain -Tdot an input `xdotversion=1.7` is an ordinary attribute to echo. Fixed 358. (5) `writeBody` iterated `g.nodes` Map order instead of AGSEQ — the known agfstnode hazard; a node first referenced by an earlier edge must be emitted before one declared above it in the subgraph text. Fixed 2239's node order. | src/render/dot/{attrs,agwrite}.ts | fresh cold sweep 759 pass / 0 diverged; 2095_1+2343 timeouts were contention (77s/105s idle, both byte-match oracle) => true 761/762; 1652 = native exceeding its own 300s budget |
| 2026-07-26 | **eager-empty graph attrs: parent scope + first-declaration, not root + any depth** | The `k=""` a subgraph prints for an attribute declared after it opened comes from `unviewsubgraphsattr` (attr.c:232), which walks `agfstsubg(S)` — DIRECT children only, non-recursive — so the trigger is the immediate PARENT declaring the key, never an ancestor; the port keyed off the root and fired at every depth. setattr also only reaches that call on the branch where no symbol existed yet, so the parent's declaration must be the FIRST anywhere: once a SIBLING scope has declared the key the identical statement takes the "new local definition" branch and seeds nothing. The root is special — a global declaration inserts its symbol into the root's OWN dict, so a root declaration always finds a local symbol and always fires. Pinned with four oracle probes (p1-p4) isolating depth and prior-declaration independently; explains 2184 (root declares label/fontcolor/id last, only clusterSurround1 prints them) and 2239's six spurious `fillcolor=""` (an earlier sibling cluster declared fillcolor first). New `Graph.firstGraphDecl`, recorded by the builder. | src/model/graph.ts, src/parser/builder.ts, src/render/dot/attrs.ts | p1-p4 probes + 2184/2239 byte-match; 3334 unit tests green |
| 2026-07-26 | **`phase` attribute: port read the C LOCAL's name, and stopped before the postprocess** | Two defects in one mechanism, both found by the new attribute blind-spot scan rather than by any parity track — no corpus graph declares either name, so every track was structurally blind. (1) C reads `agfindgraphattr(g,"phase")` into a local called `maxphase` (dotinit.c:297); the port read an ATTRIBUTE named `maxphase`. Behaviour was exactly inverted: native honours `phase=1` and ignores `maxphase=1`, the port did the opposite. (2) A phase stop is a SUCCESS return, and `dot_layout` skips dotneato_postprocess only on an ERROR (dotinit.c:512-517), so the postprocess still runs — the port folded gvPostprocess into phase 4 and skipped it, leaving the whole drawing translated 27pt off the origin at phase=3. (3) A third divergence surfaced while pinning values: late_int clamps only a PARSED value, and returns the default UNCLAMPED on a parse failure, so `phase=abc` draws everything while `phase=0` clamps to 1 — getAttrInt collapses both onto defaultVal, so graphPhase parses directly. Verified against the oracle across {0,-5,1,2,3,99,abc,3abc}: all match. Cannot regress the corpus sweep — no corpus input declares the attribute, so the changed branches are unreachable there. Two edge-route tests drove the pipeline via `maxphase=3` to reach the pre-translate frame; they now compose the phase functions directly, because a C-faithful `phase=3` includes the translate and cannot expose that frame. | src/layout/dot/index.ts, 2 edge-route tests, golden dot-phase + dot-clusterrank-none | 3358 unit tests green; golden 498 green (manifest 211->213, only new xdot refs written) |
| 2026-07-27 | **attr blind-spot batch 2: `labelfloat` name bug + `fontnames` unported (device/render + labels/text families)** | Two more defects from the attribute scan, same class as `phase`. (1) `labelfloat` — C binds E_label_float from the attribute **`labelfloat`** (input.c:770); the underscored `label_float` appears only at dotsplines.c:738, rebound against the INTERNAL aux graph. The port read the underscored name, so it honoured an attribute C ignores and ignored the one C honours — behaviour exactly inverted, verified both ways. An existing unit test had PINNED the bug; it now asserts both directions. (2) `fontnames` — parsed into GD_fontnames but never read (the source said so in a comment); C switches family/weight/style three ways at gvrender_core_svg.c:464, and the SVGFONTS branch reads DIFFERENT struct fields (svg_font_weight/svg_font_style), which the port's alias table omitted entirely. Table regenerated from ps_font_equiv.h programmatically (35 rows, 2 new columns) rather than transcribed; mode threaded via a RenderJob field, mirroring C's GD_fontnames(job->gvc->g). All 4 modes now match. Also probed and found already-correct: landscape, resolution, quantum, samplepoints (a NODE attr, observable in -Tdot as `vertices`), labelfontname/labelfontsize (head/tail labels only), layerlistsep (delimits a NODE's layer LIST, not the graph's layers= string — layersep does that). | src/common/{edge-label-init,ps-fontalias}.ts, src/render/{svg-graph,svg-helpers}.ts, src/gvc/job.ts, 9 goldens | 3383 unit + 518 golden green; fresh cold corpus sweep 761 pass / 0 diverged (was 759/0); 1652 is a PORT perf limit, not an oracle one: idle, the oracle completes (644KB) while the port exceeds 400s — the documented 2621 perf-quarantine class. It was recorded as oracleError in the previous sweep only because the oracle cache was cold then. |
| 2026-07-27 | **attr blind-spot batch 3: circo/patchwork/neato clean; sfdp `rotation` made loud, `quadtree` recorded** | circo `mindist`/`oneblock` and patchwork `inset` verified and given goldens. neato `epsilon`/`maxiter`/`start`/`pin` match exactly; `overlap_scaling` moves native 106.03pt vs port 106.00 and `overlap_shrink` is ignored by BOTH — the apparent failures were my exact-match comparator reading the documented 0.27pt PRISM drift as a divergence, so iterative engines must be compared by node-delta against the 0.5pt bar. sfdp: `rotation` was parsed but `rotate()` is unported and nothing read ctrl.rotation, so a non-zero value silently returned an UNROTATED layout (native ~320pt at rotation=45) — now throws, matching the existing `smoothing` precedent. `quadtree` is parsed into ctrl.tscheme which nothing reads (native 138.74pt at quadtree=none, port 0) — recorded, not fixed; needs the scheme threaded into the force approximation. HAZARD: a probe must assert the reference is non-trivial — `graph{a->b}` is a syntax error, both sides fail identically, and that reads as VACUOUS+MATCH; it briefly hid all three circo attributes. | src/layout/sfdp/init.ts, 3 goldens, port-catalog | 3383 unit + 524 golden green |
| 2026-07-27 | **corpus universe was too narrow — expanded from tests/ to the whole upstream tree** | Every track's universe was `~/git/graphviz/tests`, but 155 further `.dot`/`.gv` inputs ship in the same repo we treat as the canonical spec — `graphs/directed` 56, `doc/dotguide` 56, `doc/infosrc` 14, `doc/neato` 9, contrib/share/tclpkg 20 — and 97 of their basenames do not exist under tests/ at all. A one-off sweep of them found 145 OK, 5 REAL divergences (`tree-doc-infosrc-tee` 1-vs-3 shapes on HTML per-cell `sides=`; `tree-graphs-directed-switch` 49-vs-65 shapes with style=invis; inet.dot 526pt, fixed.gv 20.2pt, biological.gv 11.6pt) and 3 ORACLE aborts (`pathplan/shortest.c:193 destination point not in any triangle` — upstream bug, reproducible on the native binary alone). Expanded by ADDING a sibling root, not by re-rooting: paths are stored relative to the corpus root as `../graphs/...` so every consumer's `join(ROOT, path)` still resolves and NO existing id changes (verified: 0 of 805 changed). Re-rooting to the repo top would have renamed every id (`2559`->`tests-2559`) and invalidated every baseline, acceptance registry and journal reference at once — that constraint, not the expansion itself, was the real one; my first recommendation to keep them in a separate tier borrowed the rationale for the EXTERNAL 3,700-file donation set and did not transfer. New ids are `tree-` prefixed for visible provenance. 5 new quarantines: share/gui/{attr_widgets,template}.dot are smyrna GUI CONFIG in DOT syntax (`not-a-layout-test`), and the 3 pathplan crashers (`oracle-crash`). Manifest 805 -> 960 total, 939 applicable. | test/corpus/enumerate.ts | 3395 unit green; manifest diff verified id-stable; survey re-run pending |
| 2026-07-27 | **expanded-corpus survey: 0 regressions, 12 new non-conformant ids, 21 tracks now under-covering** | Full survey over the expanded 939-input universe. Pre-existing ids: 788 surveyed, **0 verdict changes** — today's fontnames/labelfloat/phase fixes regressed nothing, and none disappeared. New `tree-*` ids: 150 surveyed → 138 conformant, 5 diverged, 5 structural-match, 2 oracle-error. The 5 diverged: `tree-doc-infosrc-tee` (HTML per-cell `sides=`), `tree-graphs-directed-switch` (49-vs-65 shapes, style=invis), `tree-graphs-directed-arr_none`, `tree-doc-neato-inet`, `tree-graphs-directed-biological`. NOTE `arr_none` was NOT in my earlier ad-hoc sweep's findings — that probe compared only node CENTRES, so it was blind to arrowhead differences; the real survey (full SVG compare) is stricter, which is the argument for using the harness rather than a bespoke probe. CONSEQUENCE: the conformant universe grew 762 → 900, so the other 21 tracks (xdot/json/map/plain/dot-dot + all engine tracks) still sweep 762 and now cover only 84.7% of the universe. Their dashboards read '762 surveyed' which is no longer the whole picture; each needs a re-sweep. dot (SVG) is the only track reflecting the expansion: 939 surveyed, 900 conformant, 95.8%. | test/corpus/{parity.json,PARITY*.md} | 0 regressions across 788 pre-existing ids |
| 2026-07-27 | **expanded-corpus divergences: five `tree-*` ids, SIX mechanisms; two recorded leads refuted** | (1) **node `colorscheme` window too narrow** (`biological`): C sets the scheme at the tail of `emit_begin_node` and restores it at the head of `emit_end_node`, so the window spans `codefn` AND the xlabel; the port opened it inside `applyNodeStyle`, so node FILLS resolved bare scheme indices but label textspans did not — `fontcolor=3` under `colorscheme=rdbu5` fell back to black (`#000000` x14 where the oracle emits `#f7f7f7` x8 + extra `#0571b0`). Moved the window to `emitNodeBody` and deleted the inner one, leaving exactly one mapping 1:1 onto C's; both `applyNodeStyle` callers are codefns and so already run inside it. (2) **back-edge arrow TYPE not swapped** (also `biological`): `arrow_clip` resolves both types ONCE from the original edge and swaps the two FLAGS when `swapEnds` is true, so the type travels with the flag. This port keeps only a presence bit in the flag and re-reads the type from `arrowhead`/`arrowtail` BY GEOMETRIC END, which cannot express "the start end carries the arrowhead type"; a reversed back edge (`C->A [arrowhead=tee]`, head ranked above tail) drew its `arrowtail` default while the forward `A->D [arrowhead=tee]` was correct. Fixed by mapping the geometric end through the same `j` (new `arrowAttrEnd`). A trace at the stash site refuted the obvious "makeFwdEdge loses the attrs" story before it cost anything — the attrs were present; only the END was wrong. (3) **`parseArrow` missing C's GAP rules** (`arr_none`): `arrow_match_name` drops a GAP in the LAST of the four slots, and a GAP that is the entire spec; the port's loop had neither. The last-slot rule PREDICTED the failing set exactly — the 8 of 16 edges whose 4th component is `none`, the other 8 matching — which is what made it a diagnosis rather than a plausible story. The 4-component cap was already applied downstream (`arrows-shapes.ts:304`), which is why the 5-component `q->r` matched. (4) **HTML `style=invis` unported** (`doc-infosrc-tee`): C gates a cell's fill, border AND content on `!cp->data.style.invisible` (:643), and a table's fill, ALL cells, rules and border on the same flag (:542); the port had neither, so two invisible cells drew full boxes (2 polygons + 4 polylines vs the oracle's 4 polylines). The recorded lead — "HTML per-cell `sides=`" — was **REFUTED**: `sides` was already correct and all four `sides` cells matched. HTML STYLE has its OWN lexer (space-or-comma, case-insensitive, `INVIS` or `INVISIBLE`, htmllex.c:168), so it gets its own predicate instead of reusing the node/edge parser. (5) **node `style=invis` read non-inheriting** (`switch`): the emit gate read `n.attrs.get('style')` — the node's OWN attrs — instead of `nodeAttr`, which resolves the declaration-scope default as `agxget` does; `{ node [shape=circle style=invis] 1 2 ... }` drew all 16 invisible nodes while a direct `r [style=invis]` was correctly skipped. Probed both ways; the edge-side equivalent was probed too and is ALREADY correct, so it was deliberately left alone. (6) **strict-graph edge dedup entirely unported** (`inet`): `agedge` probes for a pre-existing edge when the graph is strict and returns it rather than creating a second, and for an UNDIRECTED graph probes the reversed pair too. Nothing in the port did this — `strict digraph {a->b; a->b}` held two edges, `strict graph {a--b; b--a}` held two — and inet's 6 extra edges were all reversed duplicates. Keyed on a canonical endpoint pair (unordered only when undirected) via an O(1) Map. Load-bearing detail: the anon-id counter must NOT advance on a dedup hit, because `agedge` returns before `agmapnametoid` reserves an id and that counter drives sibling subgraphs' `%N` names. | src/gvc/device.ts, src/common/{poly-gencode,splines-clip,arrows,htmltable-emit}.ts, src/parser/builder.ts | tsc clean; 3396 unit (530 golden) green; fresh 939-input dot(SVG) sweep judged PER-ID vs HEAD: **5 improved (exactly the 5 targets), 0 lost, 0 regressions**; 2621's maxDelta byte-identical to baseline (18188.6) so no geometry perturbation |
| 2026-07-27 | **three arrow unit tests had pinned the pre-fix gap behaviour** | `parseArrow('none')` returning a gap component was asserted by `arrows.test.ts` AC3 and used by `arrows-types`/`arrows-shapes` purely as a convenient way to OBTAIN a gap to resolve. C yields flag 0 for a lone `none`, so the assertion pinned the port, not the spec. Rather than relax them, AC3 now asserts the actual C rule in BOTH directions (lone gap dropped, non-lone gap kept) plus the last-slot rule and the 4-component cap; the other two take their gap from `"nonenone"`, preserving each test's real subject (`resolveArrowType`, the gap shape). Recorded because "make the failing test pass" and "make the test assert C" diverge here and only the second is correct. | src/common/{arrows,arrows-types,arrows-shapes}.test.ts | 3396 green |
| 2026-07-27 | **`1652` conformant→timeout was sweep CONTENTION, proven by worktree A/B — not a regression, and NOT allowlist material** | The fresh sweep flipped 1652 to `timeout` and the rules-gate failed. Two candidate stories: my changes slowed it, or it was starved. Settled by measurement, not argument: a HEAD worktree and the working tree each rendered 1652 standalone on an idle machine — **HEAD 899s, working tree 825s, both 2,220,668 bytes** (byte-identical output, port marginally FASTER). `ps` had already shown 1652 and 2621 in flight together at the sweep tail, and 2621 hogs a core for ~24 min; 1652 needs ~825s idle against a 1351s budget, so contention alone flips it. REJECTED the cheap fix of adding 1652 to `accepted-divergences.json`: that file is for deliberate won't-fix DIVERGENCES, 1652 is byte-identical given time, and the guard test would have accepted the entry purely because the verdict was non-conformant — converting a measurement artifact into a permanent false "known divergence". Note 2621, the sibling perf case, is deliberately NOT in that list either. Correct fix was to fix the CONDITIONS: re-ran with `RENDER_TIMEOUT_FLOOR_MS=2400000`, the documented knob whose stated intent is exactly this ("a graph that is merely slow-but-valid is *not* a timeout", test/corpus/README.md). Re-run: timeout 0, conformant 905, **GATE PASS, 0 regressions**. GENERAL RULE: on this corpus a conformant→timeout flip on 1652/2621/2108 is a load artifact until a standalone A/B says otherwise — budget-vs-elapsed, never bucket counts. | plans/decision-journal.md (analysis); no src change | standalone A/B 825s vs 899s byte-identical; re-run gate PASS (stable=919, improvements=5, regressions=0) |
| 2026-07-27 | **xdot track over the expanded universe: `:` parallel-multicolor arrow colours were never wired up (761 -> 905, 1 defect)** | Re-sweeping xdot over the widened conformant set (761 -> 905 items) surfaced `tree-doc-infosrc-colorlist` — `a -> b [dir=both color="red:blue"]` draws the HEAD arrow red and the TAIL arrow blue in the oracle; the port drew both red. Mechanism: `emitParallelSpline` (xdot) returned nothing, so `tailArrowColor`/`headArrowColor` stayed undefined and both arrows fell back to the edge pen (= the first colour). C's parallel branch sets `headcolor = tailcolor = color` at `cnum==0` and `tailcolor = color` again at `cnum==1`, i.e. head takes the FIRST colour and tail the SECOND (emit.c:2493-2497) — the INVERSE of the `;` split branch (tail=first, head=end), which the xdot renderer DID already handle. That asymmetry is almost certainly why only one of the two was wired: they look like the same rule and are not. Fixed by returning the pair, mirroring `svg-parallel-edge.ts` which already computed `colors[0]` / `colors[1] ?? colors[0]` correctly. NOTABLE: the SVG output for this input is BYTE-IDENTICAL to the oracle both before and after — the port had the rule right in one renderer and wrong in the other, so no amount of SVG-corpus growth could ever have caught it. Evidence that the per-format tracks are not redundant coverage of one another: a defect can live in exactly one renderer, and only that renderer's track can see it. | src/render/dot.ts, src/render/dot/edge-draw.ts | tsc clean; 3396 unit green; SVG for colorlist unchanged (diff empty); xdot re-sweep **905/905 conformant, 0 diverged** (was 904/905) |
| 2026-07-27 | **`:port` syntax was silently dropped whenever `edge [headport=…]` was declared — found by a FRESH json sweep, pre-existing, not mine** | The json re-sweep flagged `2734` diverged on `edge:node13->node14/headport`, and it was `conformant` in the baseline — and I had just touched port assignment in the strict-dedup change, so it read exactly like my regression. Checked the PRE-WORK commit (534010b) first: identical diff. Pre-existing; the baseline sweep simply never saw it. It surfaced only because the JSONL was deleted to force a fresh sweep instead of resuming — the "resume-style sweeps hide regressions" rule earning its keep on a real case. MECHANISM: `connectEndpoints` ran `snapshotEdgeDefaults` BEFORE assigning the DOT-syntax ports, and the syntax assignment was guarded by `!edge.attrs.has(k)`. That guard was written to mean "an explicit attr in this statement beats the `:port` syntax", but after the defaults were seeded it was true for any graph declaring `edge [headport=…]`, so EVERY `a:w -> b:e` port was discarded in favour of the default. Verified C's precedence against the oracle rather than assuming it: with `edge [headport=n]` declared, `a:e -> b:sw` yields headport `sw` (syntax beats default) and `c:e -> d:sw [headport=w]` yields `w` (explicit attr beats syntax) — so **explicit attr > syntax port > default**. Fix: apply syntax ports BEFORE the defaults snapshot, guarding against THIS statement's own attribute list rather than the edge's accumulated map (which also holds defaults and, on the strict-dedup path, the earlier statement's values); extracted as `applySyntaxPorts` used by both paths. NOTABLE, and the same shape as today's xdot colorlist bug: dot(SVG) shows **0 per-id changes** — layout already resolved the ports correctly, so only the attribute-echo path was wrong and the SVG track could never have caught it. Second independent instance today of "right in one output path, wrong in another"; the per-format tracks are not redundant coverage of each other. Also raised `JSON_ORACLE_TIMEOUT_MS` for the re-run: the first pass logged 3 `oracle exit null` on the largest inputs (1652/2095_1/2343), which is the ORACLE being killed at its own 300s cap under load, not a port failure — they vanished at 900s. | src/parser/builder.ts | tsc clean; 3396 unit green; dot(SVG) re-gate **GATE PASS, 0 per-id changes** (stable=924); json **905/905** (903 conformant + 2 accepted, 0 diverged, 0 oracle-error; was 899 + 1 diverged + 3 oracle-error); xdot **905/905** unchanged |
| 2026-07-27 | **dot family re-swept over the expanded universe: all 6 tracks closed at 905; `tree-graphs-directed-Latin1` is a DUPLICATE of an already-accepted input** | All six dot-format tracks re-run fresh (JSONL deleted each time) over the widened 905-item conformant set: xdot 761->905 (100%), json 761->905 (903+2 accepted), imagemap 761->905 (100%), plain 762->905 (897+8 accepted), dot-dot 762->905 (100%), SVG 939 (905 conformant). Two REAL defects came out of the widening and are committed separately (xdot parallel-multicolor arrow colours; parser `:port` vs edge-default precedence). The plain track's one remaining divergence was NOT a defect: `tree-graphs-directed-Latin1` is BYTE-IDENTICAL (sha1 b2c41851e950) to `tests/graphs/Latin1.gv`, which the plain acceptance list already covers for all 8 engines as the documented A4 `canon()` use-after-free oracle bug (output.c:103-108 frees the refstr before printing, so native emits garbage — here a literal 0x04 — while the port prints the correct converted text). The corpus expansion simply gave that file a second id, so the fix is to extend the acceptance to the duplicate id, not to change any code. CONTRAST with 1652 earlier today, where I REFUSED the acceptance route: there the id is byte-identical to the oracle given time, so listing it would have invented a divergence that does not exist. The test is whether a real, root-caused divergence exists — not whether the gate goes green. Swept the whole manifest for other duplicate-of-accepted inputs before editing so the 16 engine tracks would not each stop on the same non-issue: Latin1 is the ONLY one. Re-measured just that id by deleting its single JSONL line rather than re-running the 44-minute track, since the port was unchanged and only the classification moved. | test/corpus/accepted-divergences-plain.json (+8 entries), dashboards | plain 905/905 (0 diverged); PARITY.md dot family: SVG 96.4%, xdot/imagemap/dot-dot 100%, json 99.8%, plain 99.1% |
| 2026-07-27 | **16 deterministic engine tracks re-swept over the expanded universe: 0 regressions, 4 distinct new gaps, +30 ids newly COMPARED** | circo/twopi/osage/patchwork x xdot/plain/json/imagemap, all 762->905, run 3-at-a-time with each walker's internal concurrency capped to 4 so 3 tracks stay near the 12 physical cores (oversubscription is what produced the 1652 false timeout earlier). Per-id check vs 534010b: **0 regressions on all 16**. Only FOUR distinct new gaps, not twelve: `tree-graphs-directed-polypoly` (osage) and `tree-graphs-directed-oldarrows` (twopi) each surface identically in xdot+plain+json — one layout-level cause per engine propagating to every draw format, not three defects — plus `2095_1`/`2108` on circo-plain. `oldarrows` was A/B'd byte-identical against the pre-work commit (18723 bytes both) because I had changed arrow parsing the same day and the filename made it the day's most likely self-inflicted regression; it is pre-existing. THREE READING TRAPS worth recording, each of which I initially got wrong: (1) **engine-walk records RAW verdicts** — acceptance is joined in the dashboard layer, so its summary has no `accepted` bucket and osage's 7 long-accepted ids read as "8 diverged". (2) **error != divergence** — an `oracle-error`/`timeout` means NO comparison happened; counting them as findings reported circo as "6 new" when its previous sweep had the identical 6 errors. The walkers also disagree on spelling (`oracle-error` vs `oracleError`), so a status set that misses one silently reclassifies "not measured" as "port diverged". (3) **a rising `diverged` count can mean rising COVERAGE** — circo-plain went 0->2 diverged, which reads as a regression, but both ids were `oracleError` at baseline and had never been compared; raising `*_ORACLE_TIMEOUT_MS` to 900s took that track's errors 21->5. 30 ids across the 16 tracks moved from not-measured into the measured set. Pass % DROPS when this happens, because errors are excluded from scoring while divergences are not. Also added a staleness guard to the checker after reading circo's Jul-25 summary while the track was still running: walkers write the `.jsonl` incrementally and the summary `.json` only at the end, so a summary older than its jsonl is the PREVIOUS sweep's numbers. | test/corpus/*-parity-*.json, PARITY*.md, .agent-notes/engine-tracks-expanded-universe.md | 22 deterministic tracks now all at 905 (SVG 939); per-id regression check across 16 engine tracks = **0**; patchwork perfect on all 4 formats |
| 2026-07-27 | **imagemap "100%" is mostly VACUOUS — only 4.4% of inputs can emit an area** | While explaining why `polypoly` passes osage-imagemap but diverges in that engine's other three formats, I asserted that a rigid pack-cell translation "can still land inside matching map areas". That was wrong and I had not checked it — a translation moves the areas too. The real reason: polypoly emits **zero** `<area>` elements, because imagemap only emits regions for objects carrying `href`/`URL`, and the input declares none. Measured across the corpus: **41 of 939 applicable inputs (4.4%) declare href/URL; for the other 898 the imagemap output is an empty shell on BOTH sides**, so a `conformant` verdict there compares nothing. CONSEQUENCE: `dot (imagemap) 905/905 = 100.0%` sits in PARITY.md next to `dot (xdot) 100.0%` and reads as equally strong evidence; it is not. The imagemap track's real discriminating power is ~41 graphs. Same class as the zero-byte-render trap and the `graph{a->b}` syntax-error probe: co-occurrence is not coverage, and a passing comparison proves nothing unless it COULD have failed. Not changing the dashboard now (it reports what it measured, honestly); recording so the number is not over-read, and flagging that an `areas>0` denominator would make the imagemap rows meaningful. | (analysis) | 41/939 inputs can emit areas; 898 vacuous |
| 2026-07-28 | **`tree-graphs-directed-polypoly` @ osage CONFIRMED A9 and accepted — same node-9004 cos ULP, DIFFERENT propagation (arrayRects acmpf, not polyomino GRID)** | Picked up the tracked gap from the 2026-07-27 engine re-sweep with the note's own next step. Full-precision C-harness vs port dump: only 9 nodes differ, all at 1 ULP, including 9004's width at the EXACT bytes of the patchwork-tail RCA (C bb.x 57.754149262393340791 vs port ...326580 — the proven Apple-libm `cos(pi+theta)` site). Two instrumentation traps en route, both worth recording: (1) my first isolated pack A/B ran POLYOMINO (polyRects) and showed sizes don't flip it — because I had replicated the port's buildPackInfo `mode < Graph -> Graph` raise and assumed array->polyomino; the C/port enums BOTH order l_graph(3) < l_array(4), so array mode SURVIVES the raise and osage really packs via **arrayRects**. `dot -v2` (native Verbose) prints `pack info: mode array / array packing: row major 9 rows 9 columns` — the C tree needed NO instrumentation edits, Verbose already dumps pack decisions. (2) width-attr injection is NOT a no-op oracle-side for distorted polygons (the attr feeds the pre-distortion box, so the oracle GREW 9004/9014) — the collapse-to-0 it produced still proves "sizes are the only divergent input", but cannot isolate the driver; don't present attr-injection as exact-value injection for shaped nodes. MECHANISM: arrayRects' `acmpf` sorts pack cells by the RAW `width+height` sum (no rounding). Under libm, 9004's key is strictly 1 ULP above its rotated siblings 9000/9002/9006 -> sorts first; under V8's correctly-rounded cos the four keys tie EXACTLY -> unstable qsort orders the group differently -> different row-major 9x9 cells -> 9002/9006 swap + column-width fmax cascade shifting 8 neighbours in x. DECISIVE: feeding the port's own arrayRects the C sizes vs port sizes reproduces the sweep's 10 moved nodes with byte-matching dx (-2739/+2449 pair et al.). The 9014 group ties identically on BOTH sides (its lw ULP is absorbed by the sum) — 9004 is the sole driver. Irreducible per the existing A9 controlled experiment; accepted rather than re-derived. Registered in all three surface registries (engines/plain/json) + known-divergences.md prose; single-id jsonl resume re-walk flipped exactly ONE verdict per track (per-id check vs HEAD). | test/corpus/accepted-divergences-{engines,plain,json}.json, docs/known-divergences.md, PARITY* | osage xdot/plain/json all 0 diverged (897+8 / 889+16 / 892+13); corpus tests 93/93; tsc clean |
| 2026-07-28 | **`tree-graphs-directed-oldarrows` @ twopi CONFIRMED as the 9th arrows-family id (A1) and accepted — flip site pinned to directVis's degenerate collinear x-interval test** | Second tracked gap from the 2026-07-27 re-sweep. The symptom is a SWAP: port routes Z->I straight/i->Z curved, oracle the opposite; node positions identical at plain precision, 2 edges diverge, 40 xdot diffs. Standalone C harness (links libgvc + /tmp/ghl neato plugin's exported makeObstacle + libpathplan Pobsopen/Pobspath — no oracle rebuild, no tracked-source edits) replicated C's routing decision exactly: only Z->I gets pn=3. Port-side mirror script: only i->Z pn=3. Full-precision obstacle dumps show every polygon rigidly shifted ~1e-12 with per-node-distinct dx — i.e. PRISM position drift, the documented a1-twopi-arrows-family mechanism. NEW MECHANISM DETAIL, verified by replicating directVis offline against both sides' dumps (each reproduces its own side's decision): the i-Z-I fan is collinear on a ring diameter; intersect() blocks a sight-line when an obstacle vertex is wind()-collinear (1e-4 tolerance — trivially true even for a node 270pt AWAY from the segment) and inBetween() — which assumes collinearity — then tests ONLY the x projection, i.e. whether the vertex x falls strictly inside the ULP-wide (tail.x, head.x) interval. C: i's axis vertex x ...2570 inside (...2543,...2577) -> Z->I blocked, bends via H's corner. Port: I's vertex ...2890 inside (...2771,...3180) -> i->Z blocked, bends via h's corner. The y coordinate NEVER matters. Injection A/B (attribute-divergence.ts twopi): oracle pre-routing ND_pos into the port -> 40 -> 0 diffs, routing/emission byte-faithful; the run's 3 not-cleared ids (241_0, graphs-b29, linux.i386-b29) are all previously-accepted non-positional mechanisms (CDT incircle / label-anchor ties), position injection expectedly a no-op. Registered per-id in engines/plain/json registries; family prose extended 8->9 ids + flip-site paragraph. Per-id re-walk check: exactly ONE verdict change per twopi track file. NOTE: the note's original description ("oracle emits 14-pt where port emits 8") undersold it — it is a mirrored PAIR, and plain's %.4f precision hides the 1e-12 position drift that drives it; dump obstacles/paths at %.17g before believing "positions identical". | test/corpus/accepted-divergences-{engines,plain,json}.json, attribution-twopi.{json,jsonl}, docs/known-divergences.md, PARITY* | twopi xdot/plain/json all 0 diverged (888+15 / 881+23 / 887+17); corpus tests 93/93 |
| 2026-07-28 | **circo-plain `2095_1` + `2108` (the last two 2026-07-27 sweep gaps) CLOSED — one inherited acceptance, one NEW class: sub-ULP drift amplified by plain's %.5g print quantum** | `2095_1` is mechanical: the id was already accepted A1 on circo xdot (getRotation exact-tie, injection-proven 2197->0) and on circo json; plain re-emits the same rigidly-rotated blocks (605 diffs). Added the missing inherited plain entry. `2108` looked like the same shape and is NOT: circo xdot never compared it (engine-walk's hard-coded 90s port cap -> timeout) but circo JSON is CONFORMANT — same layout, then plain "diverges" with 118 diffs of exactly one 0.01 step (108.28 vs 108.27). MECHANISM, proven by full-precision ND_coord dumps both sides (C harness + port dump, 27547 nodes): max raw coordinate delta over the whole graph is **5.8e-11pt** (~6 ULP accumulation; y 7795.799999999998 vs 7795.800000000007). y/72 = 108.27499999999998 vs 108.27500000000009 STRADDLES the %.5g 5th-significant-digit boundary at 108.275 -> prints 108.27 vs 108.28; comparePlain flags |108.28-108.27| = 0.010000000000005 > PLAIN_TOLERANCE. Both printers verified correct (printG5 tie-to-even == C snprintf; identical input doubles MUST print identically, which is how the input-side cause was cornered before dumping). STRUCTURAL INSIGHT now in docs/conformance.md: at magnitudes >= 100in plain's %.5g quantum equals the 0.01 tolerance, so the plain surface cannot distinguish sub-ULP drift from real divergence on huge graphs — 10522 of 27547 nodes carried the ~e-11 drift but only the 118 numbers landing on a rounding boundary became visible. json/xdot (points) are authoritative there. RULED OUT: emitter arithmetic divergence (hypothesis (b)) — both sides compute printG5(y/72) with matching tie semantics; json byte-agreement at 7795.8 plus the raw dumps localize the difference to the layout's terminal ULPs, textbook A1. Also NB: first parallel render attempt starved the port (10min timeout) — render 3MB graphs sequentially or in background with tsx, not alongside the oracle. | test/corpus/accepted-divergences-plain.json, docs/conformance.md, PARITY* | circo plain -> 0 diverged; all four 2026-07-27 sweep gaps now closed |
| 2026-07-28 | **The 5 attribution harness-error rows CLOSED — two harness defects fixed, all 5 ids re-attributed drift-exonerated, 0 collateral verdict changes** | The bf51310 commit flagged 5 rows (neato 2619_2; fdp graphs-badvoro, 1879; sfdp 2619_1, 1879) as `harness-error` with `injectedDiffs=0` and read the 0 as "the drift-exonerated signature". That reading was WRONG: `injectedDiffs: 0` is the record's INITIALIZATION value (attribute-divergence.ts:496) — for a harness-error row no comparison ever ran, so the field is a default, not a measurement. Never infer a verdict from a field that has a zero-valued initializer; check whether the code path that COMPUTES it was reached. The 5 ids were genuinely un-attributed. TWO DEFECTS in runOracleWithDump, both instances of already-learned rules the harness missed: (1) it gated on `r.status !== 0`, but the oracle exits 1 while emitting only warnings ("cell size too small", non-ASCII width fallback) with COMPLETE output — the "oracle exit != validity, judge by trailing `}` completeness" rule engine-walk.ts already applies (4 of 5 rows); (2) a hard-coded 60s spawnSync timeout, far under the 300s floor every walker uses — fdp badvoro's oracle needs longer with GVTS_POS_DUMP (the ETIMEDOUT row). Fix: completeness-based validation (exit status only reported when output is actually incomplete) + `ATTR_ORACLE_TIMEOUT_MS` env override, default 300s. Deleted the 5 jsonl rows, re-ran attribution per engine: all 5 -> drift-exonerated with COMPUTED injectedDiffs=0; per-id check vs HEAD = exactly the 5 flips, 0 other changes; not-cleared rosters unchanged (241_0 x3, 2239, 2556, 42 — all documented). A1-drift class membership now carries real evidence for them (neato 78->79 etc.). | test/corpus/attribute-divergence.ts, attribution-{neato,fdp,sfdp}.{json,jsonl}, PARITY-{neato,fdp,sfdp}.md | tsc clean; corpus tests 93/93; 0 harness-error rows remain across all 4 attribution files |
| 2026-07-28 | **`tree-doc-infosrc-fixed` FIXED — `fixedsize=shape` was three-quarters unported (flag, vertices, insideness, clip box); node d drew and clipped at the label-grown ellipse** | First pick from the dot-SVG tracked backlog (maxDelta 111.27 on ellipse@rx). MECHANISM: C poly_init's fixedsize="shape" branch builds the VERTICES from the constrained shape box bb=(width,height) and only afterwards sets ND_width/ND_height = max(label dimen, bb) (shapes.c poly_init tail) — width and vertex box legitimately DIVERGE under fixedshape, and C marks it with poly->option.fixedshape. The port's poly-sizing had the sizing halves right (assembleResult max(dimen, bb); layout lw/rw byte-matched the C harness at 129.27), but: (1) effectivePolygon never installed option.fixedshape, so the flag was false everywhere and poly-inside's existing fixedshape branch was DEAD; (2) assignShapeInfo/computeVertices derived vertices from n.info.width — correct only when width==shape box, which exactly fixedshape breaks; base_width already stored the right box; (3) poly-inside's insideScale computed xsize/ysize from lw/rw/ht unconditionally — C's fixedshape branch derives them from polyBB itself so the scale collapses to 1; I initially misread the C flip block and concluded C clips at the label box — REFUTED by a C insidefn probe harness (binary-search rays against ND_shape(n)->fns->insidefn: d boundary = exactly r=18 every direction, and NO penwidth growth, vs 18.5 on non-fixedshape b); (4) the dot NodeBox clip path (nodeBoxOf) had the same lw/rw assumption (fixedshape box now vertex extent, penwidth 0). FIX: thread fixedshape from polySize -> effectivePolygon -> descriptor; computeVertices substitutes base for (w,h) under the flag (+ ellipseRings gains C's outward-from-base ring build, used only under fixedshape so non-fixedshape arithmetic is bit-untouched); insideScale short-circuits on the flag per C. Only 2 corpus inputs use fixedsize=shape (fixed.gv, 2082) — the conjunction hid 4 defects, the corpus-silence class again. VERIFIED: fixed.gv fully conformant (ellipse rx 129.27->18, both edge clips byte-match); all 8 fixedsize corpus inputs 0 content diffs; 3396 unit tests; FRESH 939-sweep per-id vs HEAD = exactly 2 changes: infosrc-fixed structural-match->conformant, 1652 conformant->timeout (documented flaky perf id; idle A/B re-render byte-matches, sweep-load artifact). Tracked structural-match 5->4. FOLLOW-UP: the xdot/json/plain/dot-dot/map track universes survey the SVG-conformant roster (905); infosrc-fixed joins on their next sweep -> surveyed counts will grow. | src/common/{poly-sizing,poly-vertices,poly-inside,nodeinit}.ts, src/layout/dot/edge-route-helpers.ts, test/corpus/parity.json, PARITY* | tsc clean; 3396 unit green; fresh sweep 0 regressions / +1 conformant |
| 2026-07-28 | **`tree-graphs-directed-honda-tokoro` accepted A7 — byte-identical transfer from the accepted sibling, no re-instrumentation needed** | Second pick from the dot-SVG backlog (structural-match, maxDelta 1.06, edge-path). The input is graphs-honda-tokoro's directed/ sibling differing only by `samearrowhead="1"` attrs (arrowhead merge rendering, not routing). Divergence = the SAME n012->n011 parallel pair with the SAME 1pt SVG-y step as the accepted A7 entry (maximal_bbox head-corridor round() box wall, internal x=90 C vs 89 port, GV_XDUMP-ground-truthed in .agent-notes/honda-samehead-shared-port.md). DECISIVE TRANSFER CHECK (cheaper than re-instrumenting): rendered BOTH input variants on BOTH sides — the pair's SVG geometry is byte-identical variant-to-variant per side (port==port, oracle==oracle), so the ground-truthed mechanism carries over verbatim; this is a stronger transfer than polypoly's (where values matched but propagation differed and needed its own RCA). Registered in accepted-divergences.json (dot track joins at report time — no re-sweep needed) + A7 prose extended. | test/corpus/accepted-divergences.json, docs/known-divergences.md, PARITY* | dot SVG diverged 5->4, accepted 16->17; corpus tests 93/93 |
| 2026-07-28 | **curve/icurve arrowheads FIXED under rankdir — arrow_type_curve is the one non-rotation-equivariant generator; +3 conformant, tracked structural-match 0** | The dotguide curve/icurve pair (0.38) and tree-graphs-directed-arrows (0.25) were ONE mechanism. The port pre-computes arrow draw-ops during routing (layout frame) and postproc's mapArrowOps rotates them for rankdir; C generates at RENDER time in the final frame. That architectural difference is invisible for every vector-equivariant generator, but C's arrow_type_curve (arrows.c:1031) mixes components explicitly — AF[1].x takes 0.95*v.x while AF[1].y comes from AF[0].y verbatim — so under rankdir=LR the port's internal-frame generation put the 0.95 shortening on the arm component that flips into output y: exactly the observed 0.375 = 0.05*|v| pull on both control points. DIAGNOSIS TRAP: I first concluded "xdot conformant, SVG-only defect" because the xdot track reads 905/905 — WRONG: the per-format tracks survey the SVG-conformant roster, so a not-yet-conformant id is simply ABSENT from them; its xdot diverged identically. Check roster membership before claiming a surface-specific defect. FIX (confined): genCurve gains a rankdir param and conjugates — rotate p0/u into the final frame (ccwrotatepf), run C's formula verbatim, rotate the ops back (new geom.ts cwrotatepf, C's exact inverse) so postproc's later rotation lands on C's values; all four maps are pure sign/swaps so rankdir=TB and every other generator are bit-untouched. Threaded via arrowDrawOpsForEnd (e.tail.root.info.rankdir — the same field gvPostprocess keys its rotation on, engine-neutral). arrows.gv also exercises the lcurve/rcurve halves (cubicHalf) through the conjugation. | src/common/arrows-shapes-poly.ts, src/common/arrows-shapes.ts, src/layout/dot/edge-route-arrow.ts, src/model/geom.ts, test/corpus/parity.json, PARITY* | tsc clean; 3396 unit green; fresh 939-sweep per-id vs HEAD = exactly 3 structural-match->conformant, 0 regressions; dot SVG 905->908 conformant (96.7%), tracked structural-match 4->0 |
| 2026-07-29 | **`1652` timeout CLEARED — TWO defects: a budget blind to the graph's own port cost, AND LPT co-scheduling every heavy render at once** | Measured first, standalone on an idle machine at HEAD: **conformant**, port 823.2s, oracle 217.8s. So the recorded `timeout` was never a port failure. MECHANISM (1) — budget: `max(FLOOR, MULT x native)` gave 1652 a `MULT x native` term of **810.9s, BELOW its own uncontended 823.2s render**, so the term meant to express 3x headroom contributed nothing and only the FLOOR (5x slowest native = 1351.5s) applied, at 1.64x headroom. Fixed by `renderBudgetMs` = `max(FLOOR, MULT x native, MULT x recorded portMs)` (1652 -> 2403s). **That fix alone did NOT clear it** — the first re-survey still timed out at 40.06min, which is what exposed defect (2). MECHANISM (2) — scheduling: `runPool` dispatches cost-DESCENDING (LPT), introduced so slow renders overlap the fast bulk instead of bunching into a contending tail — but that *guarantees* the `concurrency` most expensive graphs run simultaneously, relocating the same contention to the front. Measured at concurrency 8: 2371/2646 inflated **3.8-5.7x** over standalone and four of the heaviest flipped to `timeout` in one sweep. This is the remedy the perf bench already applies for the identical reason (bench.mjs BENCH_HEAVY_POOL=1: co-scheduling inflated 2620's sample ~66%, 1969->3268ms, 'memory-bandwidth + scheduler cross-talk') — the survey simply lacked it. Fixed with a heavy-slot semaphore: renders whose expected cost exceeds SURVEY_HEAVY_MS (120s) hold one of SURVEY_HEAVY_SLOTS (2) in addition to a worker slot; light work still fills the pool. Result: 1652 -> **conformant**, all other 938 ids unchanged. Ruled out: perf regression (823s vs perf.json's 801s); real divergence (verdict conformant); RAM exhaustion (96GB, 12 cores — the proximate resource is bandwidth/scheduler per bench.mjs, not capacity). METHOD ERROR worth recording: my first re-survey was invalid because I pointed ORACLE_CACHE at a dir holding only 1652, forcing 938 cold native oracle renders to compete with the port pool — it also killed 2621's oracle at its cap. Always survey with a warm oracle cache. | test/corpus/survey.ts, test/corpus/parity.json, test/corpus/PARITY*.md | tsc clean; corpus tests 107/107; 939-id survey: 1652 conformant, 0 regressions |
| 2026-07-29 | **json/map A1-drift acceptance was a hand-copied snapshot that went stale on the corpus expansion — 116 ids; replaced with the COMPUTED class the xdot tracks already use** | Triage of the only nonzero `diverged` rows left anywhere (json: fdp 53 / sfdp 53 / neato 9; imagemap: sfdp 1). MECHANISM: the xdot engine tracks resolve `A1-drift` as a **computed class** — `parity-report.ts` reads `{class:true, attributionFile}` and takes membership from `attribution-<engine>.json`'s `drift-exonerated` verdicts, so it tracks the corpus automatically. The json/map tracks instead resolve acceptance at **walk** time from a hand-enumerated per-id list whose rows literally say "layout drift inherited from the <engine> xdot engine track" — a snapshot of the then-current xdot roster. The 2026-07-27 expansion (+150 `tree-*` inputs) was picked up by the computed class and NOT by the hand lists: 114 of the 116 are `tree-*`, and the 2 that aren't (fdp `2620`, plus neato/fdp/sfdp per-id residue) are ids whose xdot per-id acceptance was never mirrored. Proof the hand list is exactly the stale class: `hand \ exonerated` = only the 2-3 genuinely per-id rows per engine, and `exonerated \ hand` = exactly the currently-diverged set. FIX at the origin: `accepted-class.ts` resolves class entries from the attribution files; `json-walk.ts`/`map-walk.ts` union class members into `loadAccepted()`; both registries gain a `classes` array; `acceptedReasonsForEngine` seeds class members with the class rationale so the accepted table is never blank. Not appended as 116 hand rows — that would re-create the staleness on the next expansion. | test/corpus/accepted-class.ts, accepted-class.test.ts, json-walk.ts, map-walk.ts, parity-report.ts, accepted-divergences-{json,map}.json | tsc clean; corpus tests 107/107 (14 new) |
| 2026-07-29 | **json-surface injection attribution: 113/115 EARNED, and the 2 residuals are the A1 chaos itself — the native oracle cannot reproduce its own pre-routing dump** | The class above inherits evidence proven on the *xdot* surface, so I re-ran the real experiment against the json comparator (same `GVTS_POS_DUMP` -> `GVTS_POS_INJECT` recipe as `attribute-divergence.ts`, same ±0.5 iterative tolerance as `json-walk.ts:318`): **113 drift-exonerated, 1 not-cleared, 1 harness-error**. TWO harness defects found in my own scratch harness before believing any of it — both worth recording because the same traps sit in the committed harness's blast radius. (1) My dump filter kept only `GVTS_POS`/`GVTS_BB` and dropped **`GVTS_CLUST_BB`**, which fdp emits per cluster; the port then recomputed cluster boxes from its own solve and 8 cluster-bearing fdp ids reported bogus `cluster:*/_draw_` diffs — the "expected" values were literally the dropped dump lines (compound's `cluster0` ur.y = 184.19). `attribute-divergence.ts:337` filters all three prefixes correctly; only my scratch copy was wrong. Lesson: an injection harness that drops a dump line does not fail, it fabricates a divergence. (2) `2620`'s oracle exceeded my 300s cap under `-Tjson` + dump (it is drift-exonerated on xdot; unverified on json). The last residual, fdp `tree-graphs-undirected-ngk10_4`, flipped verdict between runs (0 / 28 / 50 diffs). Measured cause: two oracle invocations produce **byte-identical final json but DIFFERENT position dumps** — max |delta| 7.99e-15 in (5.8e-13 pt), a few ULPs. Native fdp is ULP-nondeterministic run to run; the port amplifies those ULPs through this graph's sensitive edge routing (13->24 bezier drifting 2.03 -> 14.43pt) until one run crossed a spline-segmentation threshold and changed the op count. That is the A1 definition demonstrated at its purest — the oracle cannot reproduce its own intermediate state, so requiring the port to is impossible — and it means injection verdicts for chaotic fdp graphs are inherently flaky; do not read a single flip as a regression. | plans/decision-journal.md (analysis only; scratch harness not committed) | 113/115 exonerated at the walker's own tolerance |
| 2026-07-29 | **perf/native timing captures are 151 ids behind the corpus — the render budget's fallback is load-bearing for all of them** | Found while diagnosing 2621's timeout. `perf.json` holds 788 rows against 939 applicable inputs, and `native-timings.json` is likewise stale: **151 applicable ids have no recorded port time**, so `renderBudgetMs`'s `MULT x portMs` term is inert for them and the FLOOR is their only protection. 2621 is the worst case and shows why it matters: it was perf-QUARANTINED when both captures ran, the quarantine was later lifted ([[2621-perf-quarantine]]), and the captures were never re-run — so it has no native time and no port time at all. Consequence beyond the budget: with no cost datum it also scored 0 in `expectedCostMs`, which made it sort LAST under LPT *and* escape the heavy-slot cap. Mitigated by adding a third cost signal (the oracle ms already stored beside each cached SVG — 2621 = 256s, enough to classify it heavy), but the underlying data gap remains. FOLLOW-UP: re-run `bench.mjs` (full, not `BENCH_IDS` — it writes perf.json wholesale from the filtered set and would discard the other 788 rows) and `capture-native.mjs` over the expanded 939-input universe, then the FLOOR stops being load-bearing. Not done here: a full heavy-serial perf bench over 939 graphs is hours and orthogonal to this fix. | test/corpus/survey.ts (cost fallback only; no generated data hand-edited) | n/a — recorded gap, not a fix |
| 2026-07-29 | **json/map re-walk: 115 stale-class ids -> accepted with ZERO leftovers; 6 newly-diverged ids surfaced (attribution never ran over them)** | Re-walked only the previously-`diverged` ids per track (dropped their rows from the resume JSONL and re-ran the walker, which re-renders exactly the absent ids and rewrites the .json from the merged results). Targeted rather than a full fresh sweep is sound HERE because the change only ADDS ids to the walk-time `accepted` set, and that set is read solely in the branch where diffs already exist (json-walk.ts:320) — a conformant comparison cannot be altered by construction. Result: neato json diverged 9->1 / accepted 73->81; fdp 53->2 / 149->202; sfdp 53->3 / 233->287; sfdp imagemap 1->0 / 27->28. **All 115 accepted, 0 leftovers** — every remaining diverged row is an id that entered the universe for the FIRST time in this pass, because `conformantItems()` derives the universe from parity.json's conformant set and those tracks were 5 ids behind it: `2621` (all 3 engines + map) plus `tree-graphs-directed-arrows` (fdp, sfdp) and `tree-doc-infosrc-fixed` (sfdp) — the latter two being 2026-07-28's own fixedsize/arrow fixes, which moved graphs to conformant without the json/map tracks being re-swept. All 6 carry the drift signature (`[graph]/_draw_/op[2].P[3]` background polygon, or an edge bezier) but are NOT in any attribution file, so they cannot earn class membership and were deliberately NOT hand-accepted. FOLLOW-UP: run `attribute-divergence.ts` for neato/fdp/sfdp over the newly-diverged ids so the class can absorb whichever are genuinely A1. Side note on cost: clearing 1652 will pull it into every track's universe too (a ~800s render per engine per format) — expect that bill when parity.json is promoted. | test/corpus/json-parity-{neato,fdp,sfdp}.json(l), map-parity-sfdp.json(l), PARITY*.md | class rationale renders on all 109 class-only rows, 0 blank reasons; corpus tests 107/107 |
| 2026-07-29 | **parity.json promoted: 1652 timeout -> conformant, 0 regressions; every non-SVG track is now 1-5 ids behind the universe** | Final survey with all fixes and a warm oracle cache: **exactly one verdict change vs the committed baseline — 1652 timeout -> conformant** — and `timeout: 0` for the first time. Counts 910 conformant / 15 structural-match / 2 diverged (1367, 1581, both registry-accepted) / 12 oracle-error. `rules-gate`: stable=924, improvements=1 (1652), pre-existing=2, **regressions=0, clip-regressions=0** -> GATE PASS; promoted with `cp parity-rules.json parity.json`, then dashboard.ts + parity-report.ts. dot (SVG) row now 939 surveyed / 910 / 0 diverged / 17 accepted / 12 errors (96.9%). 2621's budget came from recording its MEASURED cost (conformant standalone, portMs 1237043 = 20.6min, oracle 256s) in perf.json + native-timings.json rather than inflating the FLOOR: matching the MULT's 3x intent via the floor would have needed 14x MAX_NATIVE (62min) globally, blunting runaway detection for all 939. Its measured 20.6min against the 22.5min floor was only **1.09x headroom**, which is why it passed in the baseline by luck and flipped under any load; it now gets 3.00x like every other heavy graph (1652 3.00x, 2646 3.83x, 2371 5.74x). CONSEQUENCE, now measured rather than predicted: promoting 1652 grew the conformant universe 909->910, and since every walker derives its universe from `parity.json`'s conformant set, the 905-row tracks (xdot/plain/dot-format/imagemap + all non-dot engines) are **5 ids behind** (2621, tree-doc-dotguide-curve, tree-doc-dotguide-icurve, tree-doc-infosrc-fixed, tree-graphs-directed-arrows — the 2026-07-28 fixes) and the three json tracks are **1 behind** (1652); sfdp map already carried 1652 so it is level at 910. FOLLOW-UP: re-walk those tracks to pick the 5 up (1652 alone is a ~800s render per engine per format). | test/corpus/parity.json, parity-rules.json, perf.json, native-timings.json, PARITY*.md | typecheck clean; vitest 6038/6038 in 338 files; rules-gate 0 regressions |
| 2026-07-30 | **MISSION `attribute-and-address-new-divergences` COMPLETE — all 22 parity tracks now report 0 diverged** | Resolved the 7 remaining `diverged` rows (3 distinct ids) on the iterative json/map tracks by earning verdicts rather than asserting them. **T1** removed `engine-walk.ts`'s hard-coded 90s port cap — the only un-scaled, un-overridable budget of the five walkers — replacing it with `max(floor, mult x native, mult x recorded cost)` + env overrides; the sweep moved into `main()` behind an `isMain` guard because the file previously ran at module top level, so importing it to test the budget would have started a corpus walk (12 new tests). **T2** resume-walked the three iterative xdot tracks 905->910, and `2621` recorded `diverged` rather than `timeout` — the whole point of T1 — with 0 pre-existing rows changed. The 6 new xdot-diverged pairs turned out to be exactly the 6 json-diverged pairs, so D5's json-surface-attribution contingency never arose. **T3** attributed all 6: `drift-exonerated`, `injectedDiffs` a COMPUTED 0 in every case (each row carries a `bucket`, only written where the comparison actually ran — the distinction that was a real defect on 2026-07-28), 0 harness-error rows. **T4** re-ran the identical injection against `compareJson` at the walker's ±0.5: 6/6 transfer, with dump line counts cross-checked against a fresh oracle (`fdp/2621`: 1419 emitted, 1419 kept, 4 of them `GVTS_CLUST_BB`) because a dropped cluster line fabricates rather than fails. **T5** re-walked json/map: 4 tracks 1/2/3/1 -> 0 diverged, all 7 targets `accepted`, **no registry edit** — the computed class absorbed exactly the right ids per engine, which is PR #37's design demonstrated end-to-end. **T6 skipped** (P4): no not-cleared rows, no transfer failures. **T7** regenerated the dashboards: 22/22 tracks 0 diverged, 0 blank reason cells. TWO brief predictions were falsified and recorded: a resume walk cannot fix a phantom timeout (`2108` x3 and `1652`@fdp keep stale rows from the old 90s cap — out of scope under P7, cheap to fix by dropping 4 JSONL rows), and `1652` entered the json tracks as `accepted` on a 2026-07-24 hand row for fdp rather than computed evidence, precisely because T2 declined to re-verdict it. | test/corpus/engine-walk.ts(+test), parity-{neato,fdp,sfdp}.json(l), attribution-*.json(l), json-parity-*.json(l), map-parity-sfdp.json(l), PARITY*.md, plans/attribute-and-address-new-divergences/** | tsc clean; 6050/6050 tests in 339 files; 22/22 tracks 0 diverged; no src/ touched |
| 2026-07-30 | **The four phantom `timeout` rows FIXED, and the tight-budget defect turns out to be a COPIED family across five walkers** | User directive that reframed this: *"it's OK for us to just run things for an hour for a single item. We are late enough in our development that 'long' and 'timeout' need better differentiation."* MECHANISM of the original four: `engine-walk.ts`'s 90s cap wrote `timeout` rows for renders that were merely long, and a resume walk can never revisit them ([[resume-walk-cannot-fix-phantom-timeout]]) — so they had to be dropped from the JSONL to be re-verdicted. Dropped and re-walked under a 1-hour floor: **all four came back `diverged`, none was a runaway** (`2108` neato 6256 diffs / fdp 1062804 / sfdp 1044748; `1652`@fdp 115987), then all **drift-exonerated** with computed zeros and buckets present, 0 harness-error. xdot tracks now `timeout: 0` and all 22 tracks read 0 diverged. Bonus: `1652`@fdp is now covered by COMPUTED class evidence, retiring the 2026-07-24 hand-row dependency noted after PR #38. **My own 300s floor from PR #38 was part of the problem, not the fix** — chosen to "bound a runaway cheaply" while 151 of 939 ids have no recorded cost and are therefore governed by the floor alone; I set the tightest budget for the least-measured graphs, hours after proving a tight budget fabricates verdicts. **Third instance found mid-task:** `attribute-divergence.ts` carried the identical hard-coded 90s port timeout, its own comment noting it was "copy-shaped from engine-walk.ts's pattern" — running attribution before spotting it would have turned all four ids into `harness-error` rows and stranded them a second time from the same root cause in a different file. All five walkers' floors now 1 hour (`survey` = max(1hr, 5x slowest native), `engine-walk`/`json-walk`/`map-walk`/`plain-walk` = 1hr, all env-overridable), and timeout rows now record **elapsed AND budget** so "long" is legible without a re-run. STILL OPEN, needs a scope decision: `2108` remains `timeout` on **six** other tracks (xdot circo+twopi, json neato+sfdp, map neato+sfdp) — the raised floors do not retroactively fix them, they need row drops + re-walks. Note the risk that made me stop rather than proceed: circo/twopi are DETERMINISTIC engines with no A1-drift class, so if `2108` diverges there it is a real tracked divergence needing diagnosis, not an absorbable one. | test/corpus/{engine-walk,attribute-divergence,json-walk,map-walk,plain-walk,survey}.ts, engine-walk.test.ts, parity-{neato,fdp,sfdp}.json(l), attribution-*.json(l), PARITY*.md | tsc clean; 6050/6050; 22/22 tracks 0 diverged; xdot timeout 0 |
| 2026-07-30 | **The remaining six `2108` timeout rows CLEARED — the 90s cap had been concealing a PASS; zero timeout rows now remain in any track** | Dropped `2108`'s stale row from the six affected JSONLs and re-walked under the 1-hour budgets. Results: **xdot circo `pass`, xdot twopi `pass`** (both DETERMINISTIC engines at 0.01 tolerance — the id was fully conformant all along), json neato/sfdp `accepted` (class-covered, `2108` is drift-exonerated for both), map neato/sfdp `conformant`. **Zero timeout rows now exist across all 22 tracks**, and all 22 remain 0 diverged. This is the strongest evidence for the 2026-07-30 directive: the tight cap was not protecting against failure, it was hiding conformance on two deterministic engines for as long as it existed. Also raised `engine-walk`'s ORACLE cap 300s -> 1hr after finding **nine phantom `oracle-error` rows** caused by it (`2222` on all five xdot tracks; `1652`, `graphs-b103`, `2621` on circo) — those read as "the oracle cannot render this" when the truth was "we did not wait"; they remain open (drop + re-walk needed) since a raised cap is not retroactive. | test/corpus/{engine-walk,attribute-divergence,json-walk,map-walk,plain-walk,survey}.ts, parity-{circo,twopi}.json(l), json-parity-{neato,sfdp}.json(l), map-parity-{neato,sfdp}.json(l), PARITY*.md | 0 timeout rows anywhere; 22/22 tracks 0 diverged; tsc clean; 6050/6050 |
| 2026-07-30 | **`twopi/tree-graphs-directed-arrows` DIAGNOSED as A9 (5th member of twopi's edge-label family) — knife edge DEMONSTRATED, not inherited** | **Mechanism:** not a layout defect but an edge-label *placement-candidate flip*; 2 of ~50 labelled edges land on the adjacent candidate while the layout is identical. **Origin:** `lp` on `edge:lcrow->lcrowlcrow` and `edge:onormal->onormalonormal`; the `_ldraw_` text coords are downstream of `lp`, which is why one cause reports as 6 diffs over 2 attributes. **Causal chain:** a node dimension differing ~1 ULP (invisible at xdot's 2-decimal precision) crosses a boundary in the xlabel R-tree's `floor()` candidate selection, so the label snaps one candidate over. Deltas are exactly one step: `22.77 = 45.54/2` (half label width) in x, `12.00 = 1.2 x fontsize 10` (line height) in y — matching the family (`2470` = 16.8 at fontsize 14; `graphs-b29`/`linux.i386-b29` = 12.0 at fontsize 10). **RULED OUT with evidence:** text measurement (the `T` op's width field is byte-identical 45.54 both sides); layout/geometry (zero node/spline/bbox diffs in 54KB of xdot — all 6 diffs are label positions); font handling (names differ cosmetically on EVERY label, yet only 2 moved); the 2026-07-28 arrowhead fix (these are lcrow/onormal arrowheads yet no arrow draw op differs); and a genuine placement bug — **refuted by experiment**: a 0.0001in (0.0072pt) node-width nudge flips the port's OWN placement onto the oracle's candidate (695.22,360.51 vs oracle 695.21,360.51), so the algorithm agrees and only the boundary side differs. Registered per-id as A9 (twopi has no computed class; per-id rows with documented mechanisms are that track's established mechanism, 16 now) — this is NOT the stale-hand-roster antipattern, which was specific to the json/map registries where a computed class exists. **Honest limit:** boundary-sensitivity is demonstrated; the specific ULP's path through `poly_init` for THIS graph is not re-instrumented and is inherited from the class — proving it would need full-precision node-dimension dumps from both sides. | test/corpus/accepted-divergences-engines.json, PARITY-twopi.md | registry guard 13/13; 22/22 tracks 0 diverged |
| 2026-07-31 | **The 8 timeout-caused `oracle-error` rows re-walked at a 1-hour cap: 2 were concealed PASSES, 6 are genuinely >1hr — and the verdicts now carry evidence** | Dropped only rows matching `status == 'oracle-error' AND err contains TIMEDOUT` so the 4 GENUINE oracle failures (circo `1880`/`1447`, fdp `2717`/`2239`) were left untouched, then re-walked all five xdot engines. **Recovered as conformant: circo `graphs-b103` and circo `2621` — both `pass`**, i.e. two more ids the old 300s cap had been hiding. **The other six are real:** `2222` ran the FULL hour and was killed on neato/sfdp/circo/twopi (`ran 3600082ms, exceeded 3600000ms budget` — the elapsed-vs-budget message added yesterday is what makes this legible), while on fdp the **native ORACLE itself** exceeded an hour, as did circo's oracle for `1652`. So `2222` is genuinely a >1-hour graph under the force-directed/radial engines, not a phantom. CHARACTERISATION worth keeping: under `dot` it is cheap (native 6799ms, port 17582ms), so the >200x blowup is engine-specific, not graph-size-alone; and where the oracle DID finish, the port's known ~10-16x per-op gap is sufficient to push it past an hour without any new pathology — fdp is the case that proves the graph is inherently enormous work, since the C itself did not finish. Net: xdot timeout rows 0 -> 4 (all `2222`), oracle-error circo 6 -> 3 / fdp 3, and **all 22 tracks remain 0 diverged**. This is the differentiation the 2026-07-30 directive asked for: a `timeout` row now means "measured, ran an hour, did not finish" rather than "someone picked 90s". | test/corpus/parity-{neato,fdp,sfdp,circo,twopi}.json(l), PARITY*.md | tsc clean; 6050/6050; 22/22 tracks 0 diverged |
| 2026-07-31 | **`2222`'s byte-identical output across 4 engines EXPLAINED: 28,303 nodes and ZERO edges, so every neato-family engine collapses to the shared component packer — the 5 engine tracks test one code path, not five** | Investigated why `neato`/`sfdp`/`circo`/`twopi` produce byte-identical xdot (16,390,698 bytes, `cmp`-verified) for `2222`. Cause: the graph declares **28,303 nodes and 0 edges**, so every node is its own connected component. With no edges there are no springs to relax, no circular ordering and no radial tree — all four engines delegate to the engine-INDEPENDENT component packer (`lib/pack`), whose polyomino packing produces the grid the positions show (861 distinct x x 1632 distinct y over 28,303 nodes). Ruled out a `layout=` attribute override (none present) and fixed `pos=` (none). **Measured oracle times: neato 2823s, sfdp 2811s, circo 2868s, twopi 2748s (~46-48 min each), all completing inside the 1hr cap; fdp's oracle exceeds it.** `dot` is the outlier that matters: **6 seconds**, and its output DIFFERS from the neato-family, so dot uses its own disconnected-component handling — and `2222` is already **conformant on all four dot tracks** (SVG, xdot, json, map). CONSEQUENCE: surveying `2222` on the five engine tracks costs ~9-10 hours of oracle+port compute to exercise the *same* shared packer five times, on a graph whose handling `dot` already verifies conformantly in 6s. Marginal engine-specific coverage beyond the first track is ~nil. **CORRECTION to yesterday's entry:** I wrote that the port's known 10-16x per-op gap explained these timeouts. That was an inference from a known figure and the measurement refutes it — port >3600s (killed) against a 2823s oracle is a ratio of **>1.28x**, i.e. the port is performing unusually WELL here, because the hot path is the packer plus label measurement over 28k nodes rather than the per-op layout inner loops where that gap lives. | plans/decision-journal.md (analysis only) | oracle timings measured; cmp-verified identity; dot tracks conformant |
| 2026-07-31 | **`2222`/twopi measured cleanly at last: port needs >4h ACTIVE against a 48min oracle (ratio >=5x) — and the sleep fix validated itself with a 34ms overshoot vs 911s before** | Re-ran under the new idle-sleep assertion with a 4-hour budget and the oracle served from cache. Result: `oracleMs` 2880603 (48min), `portMs` **14400034 against a 14400000ms budget** — killed after a full four hours of ACTIVE time, `portWallMs` absent so no sleep intruded. **The 34ms overshoot is the fix validating itself**: the same code path on the same graph previously overshot by 911s, and the entire difference was sleep being counted as compute. It also settles the earlier suspicion that the SIGKILL path was broken — it is precise to 34ms across four hours. **Ratio is >=5.00x and still a LOWER bound** (the render was killed, not completed), which unlike my two earlier figures is consistent with the port's documented 10-16x per-op gap; the 2.80x I reported on 2026-07-31 was sleep-inflated and is void, and the >1.28x before it was a correctly-labelled but far-too-loose bound. RECOMMENDATION — stop here rather than raising the budget again: at >=5x against a 48min oracle the port plausibly needs 8h+, and the graph exercises ONLY the shared component packer (28,303 nodes, 0 edges — every neato-family engine collapses to `lib/pack`), a path the dot track already covers CONFORMANTLY in 6 seconds. The remaining engines (neato/sfdp/circo) would each buy a duplicate of that same check for another 8h+. `2222` is therefore recorded as a documented >4h limit on twopi, with an honest, evidence-carrying `timeout` row rather than a phantom. | test/corpus/parity-twopi.json(l) | clean measurement, no sleep; 22/22 tracks still 0 diverged |
