<!-- SPDX-License-Identifier: EPL-2.0 -->

# T3 — circo 2095_1

## Context
105KB input, ~1000+ nodes. json track: 2197 diffs, maxDelta 66pt, first diff
`edge:1083->1084#0/_draw_/op[1].b[0]` — edge geometry. circo is
deterministic: this must be fixed or proven irreducible (AD-2). T1 should
have produced a fresh xdot baseline (the old oracle-error row was stale —
`-Kcirco -Txdot` exits 0, verified 2026-07-24).

## Task
1. If T1's xdot re-verdict PASSES for 2095_1, re-verdict json/map (the Δ66
   may have been a stale-oracle-capture artifact — invalidate caches first).
2. Else diagnose: compare fresh xdot per-node/per-edge; find the FIRST
   divergent object; instrument the C circo pipeline (block placement /
   spline routing) and dump actuals; state the mechanism.
3. Fix (contained to `src/layout/circo/` or edge routing it owns) or prove
   irreducible via injection A/B (feed C intermediates into the port);
   registry rationale upgraded either way.
4. Re-verdict circo plain/json/map/xdot: 0 regressions.

## Write-set
- `src/layout/circo/*` (or routing module owned by the mechanism — STOP if
  it lands in dot-shared code)
- `test/corpus/accepted-divergences-{json,plain,map}.json` (2095_1 rows)
- circo parity summaries/JSONLs (re-verdict)

## Read-set
- T1 journal note; `~/git/graphviz/lib/circogen/`; memory notes
  `.agent-notes/circo-*.md`, `ortho-maze-circo-rca.md`
- C-harness recipe: repo journal + `.agent-notes/` (gvplugin instrumentation)

## Acceptance criteria
- Given the diagnosis, when finished, then 2095_1's registry entries carry a
  mechanism (fix landed with 0 regressions, or injection-proven irreducible).
- Given a fix, when circo tracks re-verdict, then 2095_1 passes and no other
  circo id regresses.

## Observability / rollback
N/A. Reversible.

## Quality bar
typecheck + tests green; mechanism journaled before fix; `timeout -s KILL`
discipline on the big input.
