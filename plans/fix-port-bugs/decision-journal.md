<!-- SPDX-License-Identifier: EPL-2.0 -->

# Decision journal — fix-port-bugs

| When | Task | Decision | Why |
|------|------|----------|-----|
| 2026-07-29 | batch | Task agents do not commit; orchestrator commits one commit per task after batch gates, staging each task's write-set separately | Four parallel agents sharing one working tree: concurrent `git commit` races on the index lock, and full-suite runs mid-batch would cross-contaminate with sibling red tests. Agents ran typecheck + scoped vitest; orchestrator runs the definitive full suite. |
| 2026-07-29 | T1 | Fix applied per spec: `parseString` returns `null` on input exhausted before the declared byte count (src/xdot/parse.ts:~124, was `break`); comment corrected to cite xdot.c:139-141 | Locked analysis confirmed; red `parseXDot("T 0 0 0 50 5-ab")` returned partial success, green after fix. 71/71 scoped tests. |
| 2026-07-29 | T2 | Fix applied per D2: `attrBool` (src/common/record.ts:347) delegates to `mapbool` (src/layout/dot/rank.ts:70); local `parseInt` fallback deleted | `parseInt('abc')`→NaN, `NaN !== 0` inverted C's default-false (utils.c:336 gv_isdigit guard). Red: fixedsize="abc" clamped to 0.75; green: expands to ≈3.0. 77 record tests pass. |
| 2026-07-29 | T3 | Fix applied per D3: `TH`→`TR` normalized at token emission (htmltable-lex.ts `normalizeTag`, both open and close paths; self-closing free via close-synthesis); dead `t.tag === 'TH'` disjunct removed from processRowToken | C htmllex.c:614,669 dispatches TH as T_row/T_end_row. Hang confirmed pre-fix via 5s subprocess timeout (exit 124) on `<TABLE><TR><TH>x</TH></TR></TABLE>`. 212 htmltable tests green. |
| 2026-07-29 | T3 | Spec's repro example (`<TH>` directly under `<TABLE>`) is NOT the hang shape — top-level TH fell through parseTable's default consume, yielding `rows: []` fast; the hang requires TH nested inside an open `<TR>` (reaching processRowToken). Mechanism and fix unchanged — not stop condition 3 | Agent verified both shapes empirically; wrote separate tests for the spec example (row-synonym result), the TR structural-equivalence property, and the true nested-TH hang repro, with comments documenting the distinction. |
| 2026-07-29 | T4 | Fix applied per D1: `delete()` (src/cdt/bag.ts:91) splays key to root then walks splaying `next()` by identity, unlinks at root via `removeRoot` (folded from `unlinkNode`, parent-branch dead); `findByIdentity`/`unlinkNode` removed (grep-verified unreferenced) | Port's left-only spine walk missed RIGHT-threaded duplicates; bagInsert verified faithful to dttree.c:223-231. Mechanism validated by probe script before applying. 78/78 cdt tests green. |
| 2026-07-29 | T4 | C's `dtsearch` OBAG group-normalization sub-loop (dttree.c:167-181, back-walk to first group member before the forward walk) NOT ported — D1 locked scope to splay-search + next()-walk, and the acceptance scenario passes without it | Logged as a known scope bound: if a future caller hits an equal-key group where the splay lands mid-group with the target BEFORE the landing point, the forward-only walk would miss it. No production caller today. Revisit if DtBag gains one. |
| 2026-07-29 | T4 | splay-core.ts left untouched (empty diff) — the "inserted to the LEFT" false claim lived in bag.ts's module doc, corrected there; splay-core's bagInsert doc already correctly described RIGHT-threading | Write-set allowed a comment-only splay-core edit; none was needed. |
| 2026-07-29 | merge | Post-mission merge saga: PR #29 (opened 11:43:51Z at ab15caf, pre-0baa426) was squash-merged to main at 11:44:19Z — 28s later, before its CI failed (the 4 hardcoded-scratchpad ENOENT tests in splines.branch.test.ts, which 0baa426 fixes). Result: main CI red, and the squash (a0da0c1) diverged history from this branch, making PR #30 CONFLICTING/DIRTY so its CI could not even start. Resolution: verified tree(a0da0c1) == tree(ab15caf) (both e76083e — byte-identical, an ancestor of this branch), then `git merge -s ours origin/main` (1652535) — provably lossless since main held nothing this branch lacks. PR #30 became MERGEABLE; merging it un-reds main | Squash on a mission branch did exactly what pr-workflow.md warns: destroyed shared history and the per-task commit IDs on main. PR #30 must merge with a MERGE COMMIT. |
| 2026-07-29 | batch | **STOP — mission stop conditions 1 and 6 triggered.** T1's truncation fix unmasked 6 pre-existing failures in xdot test files outside every task's write-set: src/xdot/misc.branch.test.ts (:68, :171), xdot-parse.test.ts (:187), xdot-serial.test.ts (:110), each ×2 via the xdot.test.ts barrel. Fixtures `"F 12 10-Helvetica"` (name is 9 bytes) and `"I 0 0 100 50 8-img.png"` (name is 7 bytes) declare byte counts one too large — malformed per C xdot.c:138-142, previously accepted only via the exact lenient-`break` bug T1 fixed; misc.branch.test.ts:68 even pinned the divergence (expects the serializer to re-emit the corrected count 9). Full suite: 6 failed / 6015 passed; typecheck clean. Nothing committed — awaited human decision. | Stop condition 1: files outside all write-sets need changes. Stop condition 6: previously-passing tests fail for a reason other than the T4 inversion. |
| 2026-07-29 | T1 | Human approved amending T1's write-set: fixture counts corrected (`10-Helvetica`→`9-Helvetica` at misc.branch.test.ts:68,:171 and xdot-parse.test.ts:187; `8-img.png`→`7-img.png` at xdot-serial.test.ts:110). misc.branch.test.ts:215 intentionally keeps `10-Helvetica` with an explanatory comment — trailing input satisfies the count there (C consumes 10 bytes, "Helvetica " incl. space), so it is valid input | Fixtures encoded the lenient-parse divergence; C is the spec. After correction: typecheck clean, full suite 6021/6021 green. Included in the T1 commit (b52df39). |
| 2026-07-29 | gates | Per-task commits: T1 b52df39, T2 faea147, T3 c1a3db3, T4 970d644 — each verified to touch only its (amended) write-set. Coverage gate: 95.55% stmt / 90.64% branch / 97.36% fn / 96.88% lines, all ≥90 thresholds | Commit-per-task discipline; write-set verified via `git show --name-only` per commit. |
| 2026-07-29 | gates | Per-id delta audit of the refreshed baseline (bucket counts hide flips): 4 improvements structural-match→conformant, 1 flip conformant→timeout (1652). 1652 investigated per diagnosis mode: standalone re-render completes in 969s user / 16:57 wall with a full 2.2MB SVG (not a hang); 1652.dot uses zero features touched by the four fixes (no records, no fixedsize, no HTML labels; T4 has no production caller); its accepted-divergences rationale already classifies it "exceeds the xdot-track port time budget; perf class, see 2621", and survey.ts's LPT-dispatch comment names 1652 (~735s standalone) as the case that blows its 5x-native budget under 8-way contention. Verdict: load-marginal budget flake, NOT a code regression — stop condition 5 not triggered | Mechanism stated with evidence before accepting the gate's "pre-existing" bucketing; per bucket-fix-rebucketing discipline. |
| 2026-07-29 | gates | Improvement attribution: tree-doc-infosrc-fixed is a GENUINE T2 improvement — fixed.gv uses `fixedsize=shape`, old attrBool("shape") returned true (parseInt→NaN≠0), fixed mapbool returns C's false, moving the port onto native sizing. The other three (tree-doc-dotguide-curve, -icurve, tree-graphs-directed-arrows) use no features in this mission's diff — unexplained pass-class drift (structural-match→conformant, old maxDelta ≈0.38), suspected baseline-generation environment difference; both verdicts are pass-class so no action | Honest attribution — do not claim unearned improvements; logged for future baseline archaeology. |
| 2026-07-29 | gates | Committed the sweep-refreshed test/corpus/parity-rules.json as the new baseline (908/15/3/1/12, 1652=timeout) | Baseline is git-tracked by convention; carrying 1652 as timeout is safe — a future low-load sweep shows it as a pass-class improvement, and rules-gate already treats it as a pre-existing tracked gap either way. |
| 2026-07-29 | gates | Fresh corpus sweep (deleted parity-rules.json) run via `npx -y tsx` because `tsx` is on neither node_modules/.bin nor this shell's PATH (npm scripts assume an interactive-shell global). No project files changed by the workaround. Result: 908 conformant / 15 structural-match / 3 diverged / 1 timeout; rules-gate: stable=923, regressions=0, clip-regressions=0; pre-existing 1367/1581/1652/2621 and clip-watch 1314 unchanged. **GATE PASS — 0 regressions** | Two prior background runs failed exit 127 on `tsx: command not found`; diagnosed (tsx absent everywhere, not a PATH-ordering issue) before switching invocation. Environment fix, not a code change. |