<!-- SPDX-License-Identifier: EPL-2.0 -->

# T3 — Apply scrub to src/ + test/ (comments, describe strings, golden banners)

## Context

Repo: /Users/scottseely/git/graphviz-ts, branch chore/dot-engine-scrub.
All src/test occurrences are comments, `@see` lines, describe-strings, and
7 stale golden-ref SVG banner lines — ZERO behavior may change. The runtime
banner already emits `@knowvah/dot-engine` (src/render/svg-graph.ts:37);
the golden comparator ignores the banner line (pack-components.test.ts:35).
Read [../decisions.md](../decisions.md) D1/D2/D3 and
`plans/dot-engine-scrub/tools/dry-run-report.md` first.

## Task

1. Run `node plans/dot-engine-scrub/tools/scrub.mjs src test`.
2. Verify printed counts equal the pinned report for these trees exactly;
   mismatch → STOP.
3. Verify the 7 `test/golden/refs-port/*.svg` banners now read
   `<!-- Generated by @knowvah/dot-engine -->` and nothing else changed in
   those SVGs (`git diff` = one line each).
4. Verify remaining occurrences in src/ + test/ are protected-path lines
   only (e.g. `test/corpus/zz-rebucket-tmp.ts` REPO constant,
   `@see plans/graphviz-ts-port/...` pointers).
5. `npm run typecheck` then `npx vitest run` (full suite) — both must pass
   with zero failures; entry.test.ts describe-strings change is name-only.

## Write-set

src/**, test/**.

## Acceptance criteria

- Given the sweep, when done, then typecheck exits 0 and the full suite is
  6021/6021 — proving edits were behavior-inert.
- Given refs-port/*.svg diffs, then exactly one banner line per file.
- Given remaining src/test hits, then 100% are protected-path lines.
- Given `git diff --name-only`, then only src/ and test/ changed.

## Observability: N/A. Rollback: Reversible.

## Boundaries

- Any test failure after the sweep = the script hit code: STOP immediately,
  report, do not hand-fix.
- Never touch tools/scrub.mjs. Never run over plans/, docs/, or CLAUDE.md.
- Return journal text in your report; do not write decision-journal.md.

## Commit

`chore(scrub): rename graphviz-ts to dot-engine in src/test text (T3)`
