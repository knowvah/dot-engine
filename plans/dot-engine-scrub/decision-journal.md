<!-- SPDX-License-Identifier: EPL-2.0 -->

# Decision journal — dot-engine-scrub

| When | Task | Decision | Why |
|------|------|----------|-----|
| 2026-07-29T10:45-05:00 | T1 | Added a third protected pattern (`git-graphviz-ts`, dash-flattened form) to scrub.mjs beyond D2's two, sentinel-protected identically | 3 real Claude Code project/scratchpad path occurrences that rule-b would corrupt (`.../git-graphviz-ts/memory/` → `.../git-@knowvah/dot-engine/memory/`); with it the replace count lands exactly on the survey's 407 (push-forward: newly discovered variant, rule added + logged) |
| 2026-07-29T10:48-05:00 | T1 | Flagged scope gap: PUBLISHING.md (3) and comparisons/a2-font-metrics (2) hold `graphviz-ts` occurrences in no task's write-set | T4's repo-wide identity grep as specced would count them and fail stop condition 4 |
| 2026-07-29T10:53-05:00 | T2 | Staged only the 266 files scrub.mjs actually changed under plans/docs/CLAUDE.md (git diff --name-only filtered), not a broad `git add plans docs CLAUDE.md` | T3 was concurrently modifying src/test in the same worktree and decision-journal.md had an orchestrator edit outside T2's write-set; broad add would leak them into T2's commit |
| 2026-07-29T11:05-05:00 | T3 | Proceeded when golden banners came out as bare `dot-engine` rather than the `@knowvah/dot-engine` in the task-spec acceptance wording | Banner text has no trailing slash so D1's bare rule fires, not the specifier rule; pinned counts matched exactly, comparator ignores the line, and scrub.mjs is locked |
| 2026-07-29T11:10-05:00 | orch | Amended the 7 refs-port banners to `@knowvah/dot-engine` in fix commit 5ad1ecc4 (typecheck + 6021/6021 re-verified) | D3's premise is the refs are stale relative to the runtime emitter (svg-graph.ts:37), which emits the scoped name; T3's batch-spec acceptance names that exact string. Count identity unaffected (neither form contains `graphviz-ts`) |
| 2026-07-29T11:35-05:00 | T4 | Gate results: count identity EXACT (16 = 9 git/ + 4 plans-port + 3 git-dash; 0 graphviz-TS; 0 non-protected lines); coverage 95.55/90.64/97.36/96.88; fresh sweep 939 inputs GATE PASS regressions=0 (pre-existing 1367/1581/1652/2621 unchanged; 1652 is the known timeout class; clip-watch 1314 pre-existing) | Mission quality gates, logged per protocol |
| 2026-07-29T11:36-05:00 | T4 | Restored test/corpus/parity-rules.json after the fresh sweep: regenerated file differed ONLY in the generatedAt timestamp, all per-id verdicts byte-identical | Spec says commit "if it changed"; intent is capturing verdict changes — timestamp-only churn would muddy the docs-typed PR diff for zero information |
| 2026-07-29T11:36-05:00 | T4 | Ran the fresh sweep as `rm -f test/corpus/parity-rules.json` (actual PARITY_OUT location, resolved relative to survey.ts) rather than the brief's literal root-level `rm -f parity-rules.json` (a no-op path) | Honors the brief's fresh-sweep intent; survey.ts holds no resume state in that file (oracle-only cache), so the sweep was fresh either way |
| 2026-07-29T11:00-05:00 | orch | Resolve the T1-flagged gap by scoping T4's identity grep to T1's pinned universe (`plans docs CLAUDE.md src test`), documenting the 5 out-of-scope occurrences in closeout + PR body; write-sets unchanged | PUBLISHING.md's 3 are semantically load-bearing legacy-npm-package references (`npm deprecate graphviz-ts` must name the old package); root comparisons/ is not in the objective's enumerated scope; decisions.md pins the remainder to "T1's dry run", making the five-tree universe authoritative over T4's repo-wide grep phrasing |
