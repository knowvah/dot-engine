<!-- SPDX-License-Identifier: EPL-2.0 -->

# Mission execution journal

Per-batch execution decisions for this mission. **Corpus fixes and accepted
divergences go to the repo-root `plans/decision-journal.md`** (project
convention) — this file is only for mission-flow decisions (batch order changes,
scope calls, blocked-and-waiting notes).

| Date | Batch/Task | Decision / Note | Gate result |
|------|-----------|-----------------|-------------|
| 2026-07-24 | B1 plan | 4 parallel typescript-pro agents (T1/T2/T3/T4), disjoint write-sets, no deps. Agents do not commit; orchestrator commits per-task after batch gates (typecheck+test). | pass |
| 2026-07-24 | B1/T1 | Comparator compares edge `pointCount` structurally in iterative mode (not a coord per AD-4 reading). WATCH: if plain triage floods neato/fdp/sfdp with pointCount diffs, relax to deterministic-only. Ports + malformed-line-skip also per agent judgment. | typecheck+3270 tests green |
| 2026-07-24 | B2/T5 | plain-walk modeled on map-walk's dual-format worst-of-two verdict; camelCase verdicts per contract; flat `{divergences:[{id,engine,reason}]}` registry; survey-only mode (no stop-on-first — contract doesn't ask for it). Early neato 60-id probe: 45 pass / 15 diverged, all 15 non-numeric (label/style/shape/color) — AD-4 iterative wiring confirmed; T6 has real work. | typecheck+3270 tests green |
| 2026-07-24 | B3/T6 order | Stopped the 8-engine sweep after dot (759/762 ids) — full inventory already visible; fixes invalidate pre-fix sweeps, so apply pinned fixes first then run all 8 fresh (halves wall time; oracle caches persist either way). pgrep-verified no renderers before src edits. | n/a |
| 2026-07-24 | B3/T6 write-set | Extended write-set with `src/render/map.test.ts`: colocated unit tests of the exact helpers being fixed; gate `npm test` green is unsatisfiable otherwise. Judged write-set oversight, not scope change. | n/a |
| 2026-07-24 | B3/T6 done | 5 fix commits (canon port, bz.size, %.5g ties, shape naming/clustnode/html-fallback, comparator AD-4 pointCount) → dot 269→0 unexplained. 81 registry accepts: 7-id oracle canon() UAF ×8 engines (A4), 24 engine-track-inherited geometry (twopi 14 / osage 7 / circo 3), 1 twopi plain-shape init port bug (out of write-set, follow-up flagged). Errors/timeouts listed in journal, none silent. All 8 sweeps: diverged=0. map.test.ts + compare-plain(.test).ts folded into write-set (colocated tests of changed code). | typecheck+3271 tests+8 sweeps 0 diverged; json/map dot smoke unchanged |
| 2026-07-24 | B4/T7 write-set | T7's premise "structural comparison already suppresses coord drift" is false for json (compareJson fixed ±0.01) — iterative sweeps flooded (neato 269 / fdp 169 / sfdp 264 diverged). Extended write-set with `test/corpus/json-walk.ts` (T3 file, 1-line): pass the established engine-walk bar (0.5 iterative, 0.01 deterministic) to compareJson. AD-4 compliant; comparator untouched. | pending re-sweep |
| 2026-07-24 | B4/T7 done | No src fixes needed (json emission already conformant) — all work was harness bars + comparator blindness + acceptances (482, engine-scoped). Write-set extended: json-walk.ts (tolerance + engine-scoped accepts), compare-json.ts (rects/head_lp/tail_lp/xlp positional) — enabling fixes without which AD-4/comparison were unsound. Stale-oracle-capture anomaly documented in repo journal; resolved by cache invalidation + fresh double-run oracles. | typecheck+3271 tests; 7 sweeps 0 diverged; dot smoke green |
| 2026-07-24 | B5/T8 done | One real fix (root hotspot = 0-based clip window; origin in src/gvc/anchor.ts — write-set extended per diagnosis.md fix-at-origin, journaled) + map-walk AD-4 bar + engine-scoped accepts (52). 4 fdp big ids drifted diverged→timeout between passes (listed); registry rows retained. | typecheck+3271 tests; 7 sweeps 0 diverged; dot map+plain unchanged |
| 2026-07-24 | B1/T3+T4 | `render-one-json.ts`/`render-one-map.ts` already engine-parameterized since their initial commits — no edit needed (write-set unused, logged not silent). json/map walkers gained JSONL resume (didn't exist before; spec's "keep current dot JSONL path" was vacuous). dot oracle invocation kept byte-identical; engine-keyed oracle caches. map engine-mode reuses dot-scoped accepted-divergences list for all engines pending T8. | typecheck+3270 tests green |
